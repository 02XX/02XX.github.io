<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>1017 Queueing at Bank</title>
    <url>/2021/05/12/1017-Queueing-at-Bank/</url>
    <content><![CDATA[<h2 id="question"><a class="markdownIt-Anchor" href="#question"></a> Question</h2>
<p>Suppose a bank has <em>K</em> windows open for service. There is a yellow line in front of the windows which devides the waiting area into two parts. All the customers have to wait in line behind the yellow line, until it is his/her turn to be served and there is a window available. It is assumed that no window can be occupied by a single customer for more than 1 hour.</p>
<p>Now given the arriving time <em>T</em> and the processing time <em>P</em> of each customer, you are supposed to tell the average waiting time of all the customers.</p>
<span id="more"></span>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input file contains one test case. For each case, the first line contains 2 numbers: <em>N</em> (≤104) - the total number of customers, and <em>K</em> (≤100) - the number of windows. Then <em>N</em> lines follow, each contains 2 times: <code>HH:MM:SS</code> - the arriving time, and <em>P</em> - the processing time in minutes of a customer. Here <code>HH</code> is in the range [00, 23], <code>MM</code> and <code>SS</code> are both in [00, 59]. <strong>It is assumed that no two customers arrives at the same time.</strong></p>
<p>Notice that the bank opens from 08:00 to 17:00. Anyone arrives early will have to wait in line till 08:00, and anyone comes too late (at or after <strong>17:00:01</strong>) will not be served nor counted into the average.</p>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each test case, print in one line the average waiting time of all the customers, in minutes and accurate up to 1 decimal place（精确到小数点后一位）.</p>
<h3 id="sample-input"><a class="markdownIt-Anchor" href="#sample-input"></a> Sample Input:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">7 3</span><br><span class="line">07:55:00 16</span><br><span class="line">17:00:01 2</span><br><span class="line">07:59:59 15</span><br><span class="line">08:01:00 60</span><br><span class="line">08:00:00 30</span><br><span class="line">08:00:02 2</span><br><span class="line">08:03:00 10</span><br></pre></td></tr></table></figure>
<h3 id="sample-output"><a class="markdownIt-Anchor" href="#sample-output"></a> Sample Output:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">8.2</span><br></pre></td></tr></table></figure>
<hr>
<h2 id="notice"><a class="markdownIt-Anchor" href="#notice"></a> Notice</h2>
<ul>
<li>It is assumed that no two customers arrives at the same time.</li>
</ul>
<ul>
<li>at or after <strong>17:00:01</strong></li>
<li>不被服务的人数不被算在总数中</li>
</ul>
<h2 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h2>
<ul>
<li>记录每个窗口服务结束的时间</li>
<li>下一个客户到达的时候选择服务结束时间最早的窗口，记为T<sub>min</sub>
<ul>
<li>其中如果到达时间比结束最早的还要早，那么他就要等待<strong>至</strong>T<sub>min</sub>，窗口服务的时间=T<sub>min</sub>+需要服务的时间</li>
<li>如果到达的时间比结束时间晚，那么他不需要等待，直接被服务，窗口结束时间=他到达的时间 + 需要服务的时间</li>
</ul>
</li>
</ul>
<h2 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;climits&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">cus</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> atTime;</span><br><span class="line">    <span class="keyword">int</span> severTime;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">transform</span><span class="params">(string t)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> HH, MM, SS;</span><br><span class="line">    HH = ((t[<span class="number">0</span>] - <span class="string">&#x27;0&#x27;</span>) * <span class="number">10</span> + (t[<span class="number">1</span>] - <span class="string">&#x27;0&#x27;</span>)) * <span class="number">3600</span>;</span><br><span class="line">    MM = ((t[<span class="number">3</span>] - <span class="string">&#x27;0&#x27;</span>) * <span class="number">10</span> + (t[<span class="number">4</span>] - <span class="string">&#x27;0&#x27;</span>)) * <span class="number">60</span>;</span><br><span class="line">    SS = ((t[<span class="number">6</span>] - <span class="string">&#x27;0&#x27;</span>) * <span class="number">10</span> + (t[<span class="number">7</span>] - <span class="string">&#x27;0&#x27;</span>));</span><br><span class="line">    <span class="keyword">return</span> HH + MM + SS;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">compare</span><span class="params">(cus A, cus B)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> A.atTime &lt; B.atTime;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">sum</span><span class="params">(vector&lt;cus&gt; customersTime, <span class="keyword">int</span> K)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> wait = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">sort</span>(customersTime.<span class="built_in">begin</span>(), customersTime.<span class="built_in">end</span>(), compare);</span><br><span class="line">    <span class="comment">//服务完后的窗口时间</span></span><br><span class="line">    <span class="function">vector&lt;<span class="keyword">int</span>&gt; <span class="title">windowsTime</span><span class="params">(K, <span class="number">28800</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> customer : customersTime)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(customer.atTime == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="comment">//找到最早服务完的窗口</span></span><br><span class="line">        <span class="keyword">int</span> min = INT_MAX, index = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">size_t</span> i = <span class="number">0</span>; i &lt; windowsTime.<span class="built_in">size</span>(); i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(windowsTime[i] &lt; min)</span><br><span class="line">            &#123;</span><br><span class="line">                min = windowsTime[i];</span><br><span class="line">                index = i;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> waitTime = min - customer.atTime;</span><br><span class="line">        <span class="keyword">if</span>(waitTime &gt; <span class="number">0</span>) <span class="comment">//来的比最早的窗口早</span></span><br><span class="line">        &#123;</span><br><span class="line">            wait += waitTime;</span><br><span class="line">            windowsTime[index] += customer.severTime * <span class="number">60</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="comment">//来的比最早的晚</span></span><br><span class="line">        &#123;</span><br><span class="line">            windowsTime[index] = customer.atTime + customer.severTime * <span class="number">60</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> wait;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> N, K;</span><br><span class="line">    cin &gt;&gt; N &gt;&gt; K;</span><br><span class="line">    </span><br><span class="line">    <span class="function">vector&lt;cus&gt; <span class="title">customersTime</span><span class="params">(N)</span></span>;</span><br><span class="line">    <span class="function">vector&lt;<span class="keyword">int</span>&gt; <span class="title">waitTime</span><span class="params">(N)</span></span>;</span><br><span class="line">    <span class="keyword">int</span> severPeople = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        string A;</span><br><span class="line">        <span class="keyword">int</span> P;</span><br><span class="line">        cin &gt;&gt; A &gt;&gt; P;</span><br><span class="line">        <span class="keyword">int</span> t = <span class="built_in">transform</span>(A);</span><br><span class="line">        <span class="comment">// if(t &lt; 28800)</span></span><br><span class="line">        <span class="comment">// &#123;</span></span><br><span class="line">        <span class="comment">//     remainTime += 28800 - t;</span></span><br><span class="line">        <span class="comment">//     t = 28800;    </span></span><br><span class="line">        <span class="comment">// &#125;</span></span><br><span class="line">        <span class="keyword">if</span>(t &gt;= <span class="number">61201</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        customersTime[i] = cus &#123;t, P&#125;;</span><br><span class="line">        severPeople++;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    cout &lt;&lt; <span class="built_in">setiosflags</span>(ios::fixed) &lt;&lt; <span class="built_in">setprecision</span>(<span class="number">1</span>);</span><br><span class="line">    cout &lt;&lt; <span class="built_in">sum</span>(customersTime, K) / (<span class="built_in"><span class="keyword">double</span></span>(severPeople) * <span class="number">60</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>1020 Tree Traversals</title>
    <url>/2021/05/13/1020-Tree-Traversals/</url>
    <content><![CDATA[<h3 id="question"><a class="markdownIt-Anchor" href="#question"></a> Question</h3>
<p>Suppose that all the keys in a binary tree are distinct positive integers. Given the postorder and inorder traversal sequences, you are supposed to output the level order traversal sequence of the corresponding binary tree.</p>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input file contains one test case. For each case, the first line gives a positive integer <em>N</em> (≤30), the total number of nodes in the binary tree. The second line gives the postorder sequence and the third line gives the inorder sequence. All the numbers in a line are separated by a space.</p>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each test case, print in one line the level order traversal sequence of the corresponding binary tree. All the numbers in a line must be separated by exactly one space, and there must be no extra space at the end of the line.</p>
<h3 id="sample-input"><a class="markdownIt-Anchor" href="#sample-input"></a> Sample Input:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">7</span><br><span class="line">2 3 1 5 7 6 4</span><br><span class="line">1 2 3 4 5 6 7</span><br></pre></td></tr></table></figure>
<h3 id="sample-output"><a class="markdownIt-Anchor" href="#sample-output"></a> Sample Output:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4 1 6 3 5 7 2</span><br></pre></td></tr></table></figure>
<hr>
<h3 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h3>
<ul>
<li>使用递归的思想构建比较容易想出来</li>
<li>先找到中序序列的分解线<code>pLine</code>，同时记录已经遍历的节点数<code>count</code>，这个个数便于分开后序序列。</li>
<li>注意不能用<code>pLine</code>代替<code>count</code>若节点某一子树为空，则会出错，具体看代码中的注释。</li>
</ul>
<h2 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> data;</span><br><span class="line">    node* lchild;</span><br><span class="line">    node* rchild;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function">node* <span class="title">build</span><span class="params">(vector&lt;<span class="keyword">int</span>&gt; &amp;inOrder, vector&lt;<span class="keyword">int</span>&gt; &amp;postOrder, <span class="keyword">int</span> inBegin, <span class="keyword">int</span> inEnd, <span class="keyword">int</span> postBegin, <span class="keyword">int</span> postEnd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(inBegin &gt; inEnd || postBegin &gt; postEnd)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//找到左右子树分割线</span></span><br><span class="line">    <span class="keyword">int</span> pLine = inBegin;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(postOrder[postEnd] != inOrder[pLine])</span><br><span class="line">    &#123;</span><br><span class="line">        pLine++;</span><br><span class="line">        count++;</span><br><span class="line">    &#125;</span><br><span class="line">    node *root = <span class="keyword">new</span> <span class="built_in">node</span>();</span><br><span class="line">    root-&gt;data = postOrder[postEnd];</span><br><span class="line">    <span class="comment">//不能用pLine代替count,可能会越界,注释的两条为错的。比如inOrder = &#123;1,2,3&#125;, postOrder = &#123;2,3,1&#125;</span></span><br><span class="line">    root-&gt;lchild = <span class="built_in">build</span>(inOrder, postOrder, inBegin, pLine - <span class="number">1</span>, postBegin, postBegin + count - <span class="number">1</span>);</span><br><span class="line">    <span class="comment">// root-&gt;lchild = build(inOrder, postOrder, inBegin, pLine - 1, postBegin, pLine - 1);</span></span><br><span class="line">    root-&gt;rchild = <span class="built_in">build</span>(inOrder, postOrder, pLine + <span class="number">1</span>, inEnd, postBegin + count, postEnd - <span class="number">1</span>);</span><br><span class="line">    <span class="comment">// root-&gt;rchild = build(inOrder, postOrder, pLine + 1, inEnd, pLine + 1, postEnd - 1);</span></span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">levelOrder</span><span class="params">(node* root, <span class="keyword">int</span> N)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    queue&lt;node*&gt; q;</span><br><span class="line">    q.<span class="built_in">push</span>(root);</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; q.<span class="built_in">front</span>()-&gt;data;</span><br><span class="line">        count++;</span><br><span class="line">        <span class="keyword">if</span>(count &lt; N)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(q.<span class="built_in">front</span>()-&gt;lchild != <span class="literal">nullptr</span>)</span><br><span class="line">            q.<span class="built_in">push</span>(q.<span class="built_in">front</span>()-&gt;lchild);</span><br><span class="line">        <span class="keyword">if</span>(q.<span class="built_in">front</span>()-&gt;rchild != <span class="literal">nullptr</span>)</span><br><span class="line">            q.<span class="built_in">push</span>(q.<span class="built_in">front</span>()-&gt;rchild);</span><br><span class="line"></span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> N;</span><br><span class="line">    cin &gt;&gt; N;</span><br><span class="line">    <span class="function">vector&lt;<span class="keyword">int</span>&gt; <span class="title">inOrder</span><span class="params">(N)</span>, <span class="title">postOrder</span><span class="params">(N)</span></span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        cin &gt;&gt; postOrder[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        cin &gt;&gt; inOrder[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    node* tree = <span class="built_in">build</span>(inOrder, postOrder, <span class="number">0</span>, N - <span class="number">1</span>, <span class="number">0</span>, N - <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">levelOrder</span>(tree,N);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>1021 Deepest Root</title>
    <url>/2021/05/26/1021-Deepest-Root/</url>
    <content><![CDATA[<p>A graph which is connected and acyclic can be considered a tree. The height of the tree depends on the selected root. Now you are supposed to find the root that results in a highest tree. Such a root is called <strong>the deepest root</strong>.</p>
<span id="more"></span>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input file contains one test case. For each case, the first line contains a positive integer <em>N</em> (≤104) which is the number of nodes, and hence the nodes are numbered from 1 to <em>N</em>. Then <em>N</em>−1 lines follow, each describes an edge by given the two adjacent nodes’ numbers.</p>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each test case, print each of the deepest roots in a line. If such a root is not unique, print them in increasing order of their numbers. In case that the given graph is not a tree, print <code>Error: K components</code> where <code>K</code> is the number of connected components in the graph.</p>
<h3 id="sample-input-1"><a class="markdownIt-Anchor" href="#sample-input-1"></a> Sample Input 1:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">5</span><br><span class="line">1 2</span><br><span class="line">1 3</span><br><span class="line">1 4</span><br><span class="line">2 5</span><br></pre></td></tr></table></figure>
<h3 id="sample-output-1"><a class="markdownIt-Anchor" href="#sample-output-1"></a> Sample Output 1:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td></tr></table></figure>
<h3 id="sample-input-2"><a class="markdownIt-Anchor" href="#sample-input-2"></a> Sample Input 2:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">5</span><br><span class="line">1 3</span><br><span class="line">1 4</span><br><span class="line">2 5</span><br><span class="line">3 4</span><br></pre></td></tr></table></figure>
<h3 id="sample-output-2"><a class="markdownIt-Anchor" href="#sample-output-2"></a> Sample Output 2:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Error: 2 components</span><br></pre></td></tr></table></figure>
<h3 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h3>
<p>直接DFS即可</p>
<h3 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> vector&lt;vector&lt;<span class="keyword">int</span>&gt;&gt; Graph;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">DFSTravel</span><span class="params">(Graph &amp;G, vector&lt;<span class="keyword">bool</span>&gt;&amp;visited)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DFS</span><span class="params">(Graph &amp;G, vector&lt;<span class="keyword">bool</span>&gt; &amp;visited, vector&lt;<span class="keyword">int</span>&gt; &amp;deepestNode, <span class="keyword">int</span> v, <span class="keyword">int</span> height,<span class="keyword">int</span>&amp;tempH)</span></span>;</span><br><span class="line"><span class="keyword">int</span> maxHeight = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> node; cin &gt;&gt; node;</span><br><span class="line">    <span class="function">Graph <span class="title">G</span><span class="params">(node + <span class="number">1</span>, vector&lt;<span class="keyword">int</span>&gt;(node + <span class="number">1</span>, <span class="number">0</span>))</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; node - <span class="number">1</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> n1, n2; cin &gt;&gt; n1 &gt;&gt; n2;</span><br><span class="line">        G[n1][n2] = G[n2][n1] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">vector&lt;<span class="keyword">bool</span>&gt; <span class="title">visited</span><span class="params">(node + <span class="number">1</span>, <span class="literal">false</span>)</span></span>;</span><br><span class="line">    vector&lt;<span class="keyword">int</span>&gt; deepestNode;</span><br><span class="line">    <span class="keyword">int</span> c = <span class="built_in">DFSTravel</span>(G, visited);</span><br><span class="line">    <span class="keyword">if</span>(c &gt; <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Error: &quot;</span>&lt;&lt;c &lt;&lt;<span class="string">&quot; components&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; node + <span class="number">1</span>; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> height = <span class="number">0</span>, tempH = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">1</span>; j &lt; node + <span class="number">1</span>; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                visited[j] = <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">DFS</span>(G, visited, deepestNode, i, height, tempH);</span><br><span class="line">            <span class="keyword">if</span>(tempH &gt; maxHeight)</span><br><span class="line">            &#123;</span><br><span class="line">                maxHeight = tempH;</span><br><span class="line">                deepestNode.<span class="built_in">clear</span>();</span><br><span class="line">                deepestNode.<span class="built_in">push_back</span>(i);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(tempH == maxHeight)</span><br><span class="line">            &#123;</span><br><span class="line">                deepestNode.<span class="built_in">push_back</span>(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!deepestNode.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in"><span class="keyword">int</span></span>(deepestNode.<span class="built_in">size</span>()); i++)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; deepestNode[i] &lt;&lt; endl;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">DFSTravel</span><span class="params">(Graph &amp;G, vector&lt;<span class="keyword">bool</span>&gt;&amp;visited)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> componets = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> h = <span class="number">0</span>, t = <span class="number">0</span>;</span><br><span class="line">    vector&lt;<span class="keyword">int</span>&gt; d;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; <span class="built_in"><span class="keyword">int</span></span>(visited.<span class="built_in">size</span>()); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        visited[i] = <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; <span class="built_in"><span class="keyword">int</span></span>(visited.<span class="built_in">size</span>()); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(!visited[i])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">DFS</span>(G, visited, d, i, h, t);</span><br><span class="line">            componets++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> componets;  </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DFS</span><span class="params">(Graph &amp;G, vector&lt;<span class="keyword">bool</span>&gt; &amp;visited, vector&lt;<span class="keyword">int</span>&gt; &amp;deepestNode, <span class="keyword">int</span> v, <span class="keyword">int</span> height, <span class="keyword">int</span>&amp;tempH)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    visited[v] = <span class="literal">true</span>;</span><br><span class="line">    height++;</span><br><span class="line">    <span class="keyword">if</span>(height &gt; tempH)</span><br><span class="line">    &#123;</span><br><span class="line">        tempH = height;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; <span class="built_in"><span class="keyword">int</span></span>(visited.<span class="built_in">size</span>()); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(G[v][i] != <span class="number">0</span> &amp;&amp; !visited[i])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">DFS</span>(G, visited, deepestNode, i, height, tempH);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Bash变量</title>
    <url>/2021/05/12/Bash%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<h1 id="bash变量"><a class="markdownIt-Anchor" href="#bash变量"></a> Bash变量</h1>
<h2 id="用户自定义变量本地变量"><a class="markdownIt-Anchor" href="#用户自定义变量本地变量"></a> 用户自定义变量（本地变量）</h2>
<h3 id="变量"><a class="markdownIt-Anchor" href="#变量"></a> 变量</h3>
<ol>
<li>在Bash中，变量的默认类型都是字符串类型，如果要进行数值运算则必须指定变量类型为数值型</li>
</ol>
<p>变量用等号连接，等号两边不能有空格</p>
<ol start="2">
<li>
<p>变量的值如果有空格，需要用单引号或双引号包括</p>
</li>
<li>
<p>变量的值中可以用\转义符</p>
</li>
<li>
<p>如果需要增加变量的值，那么可以进行变量值的叠加，不过变量需要用双引号包含“<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi mathvariant="normal">变</mi><mi mathvariant="normal">量</mi><mi mathvariant="normal">名</mi><mi mathvariant="normal">”</mi><mi mathvariant="normal">或</mi><mi mathvariant="normal">用</mi></mrow><annotation encoding="application/x-tex">变量名”或用</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.69444em;vertical-align:0em;"></span><span class="mord cjk_fallback">变</span><span class="mord cjk_fallback">量</span><span class="mord cjk_fallback">名</span><span class="mord">”</span><span class="mord cjk_fallback">或</span><span class="mord cjk_fallback">用</span></span></span></span>{变量名}包含</p>
</li>
<li>
<p>如果需要将命令的结果作为变量赋予变量，则需要使用反引号或$()包含命令</p>
</li>
<li>
<p>环境变量名建议大写，便于区分</p>
</li>
</ol>
<h3 id="变量的分类"><a class="markdownIt-Anchor" href="#变量的分类"></a> 变量的分类</h3>
<ol>
<li>用户自定义变量</li>
<li>环境变量：这种变量中主要保存的是和系统操作环境相关的数据。</li>
<li>位置参数变量：这种变量主要是用来向脚本当中传递参数或数据的，变量名不能自定义，变量作用是固定的。</li>
<li>预定义变量：是Bash中已经定义好的变量，变量名不能自定义，变量作用也是固定的。</li>
</ol>
<h2 id="环境变量"><a class="markdownIt-Anchor" href="#环境变量"></a> 环境变量</h2>
<p>用户自定义变量只在当前的Shell中生效，而环境变量会在当前Shell和这个Shell的所有子Shell当中生效。如果把环境变量写入相应的配置文件，那么这个环境变量就会在所有的Shell中生效</p>
<h3 id="设置环境变量"><a class="markdownIt-Anchor" href="#设置环境变量"></a> 设置环境变量</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> name=value <span class="comment">#设置环境变量</span></span><br><span class="line">env <span class="comment">#查看环境变量</span></span><br><span class="line"><span class="built_in">unset</span> 变量名 <span class="comment">#删除变量</span></span><br></pre></td></tr></table></figure>
<p>使用set查看变量（不止环境变量），env只能看环境变量</p>
<p>使用pstree查看当前所在的shell</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name=ali <span class="comment">#父级shell的本地变量</span></span><br><span class="line"><span class="built_in">export</span> sex=male <span class="comment">#所有shell的环境变量（全局变量）</span></span><br><span class="line">bash <span class="comment">#创建子shell</span></span><br><span class="line">age=18 <span class="comment">#子shell的本地变量</span></span><br></pre></td></tr></table></figure>
<p>在父级shell中只能看到name和sex，在子shell中只能看到age和sex</p>
<h3 id="path"><a class="markdownIt-Anchor" href="#path"></a> PATH</h3>
<p>PATH是系统命令查找的路径</p>
<p>PATH叠加</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=<span class="string">&quot;<span class="variable">$PATH</span>&quot;</span>:变量路径</span><br></pre></td></tr></table></figure>
<h3 id="ps1"><a class="markdownIt-Anchor" href="#ps1"></a> PS1</h3>
<table>
<thead>
<tr>
<th>提示符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>\d</code></td>
<td>显示日期，格式为&quot;星期 月 日&quot;</td>
</tr>
<tr>
<td><code>\h</code></td>
<td>显示简写主机名</td>
</tr>
<tr>
<td><code>\T</code></td>
<td>显示12小时制时间，格式为&quot;HH:MM:SS&quot;</td>
</tr>
<tr>
<td><code>\t</code></td>
<td>显示24小时制时间，格式为&quot;HH:MM:SS&quot;</td>
</tr>
<tr>
<td><code>\A</code></td>
<td>显示24小时制时间，格式为&quot;HH:MM&quot;</td>
</tr>
<tr>
<td><code>\u</code></td>
<td>显示当前用户名</td>
</tr>
<tr>
<td><code>\w</code></td>
<td>显示当前所在路径的完整名称</td>
</tr>
<tr>
<td><code>\W</code></td>
<td>显示当前所在目录的最后一个目录</td>
</tr>
<tr>
<td><code>\#</code></td>
<td>执行的第几个命令</td>
</tr>
<tr>
<td><code>\$</code></td>
<td>提示符，root用户为&quot;#&quot;，普通用户为&quot;$&quot;</td>
</tr>
<tr>
<td><code>\!</code></td>
<td>显示该命令的历史记录编号。</td>
</tr>
</tbody>
</table>
<p>PS1是系统提示符的变变量</p>
<p>例如</p>
<p><code>PS1=[\u@\h \W]\$</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[root@izbp1clp96b0qjo5whikc9z ~]<span class="comment">#</span></span><br></pre></td></tr></table></figure>
<h2 id="位置参数变量"><a class="markdownIt-Anchor" href="#位置参数变量"></a> 位置参数变量</h2>
<table>
<thead>
<tr>
<th style="text-align:left">变量</th>
<th style="text-align:left">含义</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left"><code>$0</code></td>
<td style="text-align:left">当前脚本的文件名。</td>
</tr>
<tr>
<td style="text-align:left"><code>$n</code>（n≥1）</td>
<td style="text-align:left">传递给脚本或函数的参数。n 是一个数字，表示第几个参数。例如，第一个参数是 $1，第二个参数是 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>2</mn><mi mathvariant="normal">。</mi><mn>10</mn><mi mathvariant="normal">以</mi><mi mathvariant="normal">上</mi><mi mathvariant="normal">的</mi><mi mathvariant="normal">参</mi><mi mathvariant="normal">数</mi><mi mathvariant="normal">用</mi><mi mathvariant="normal">大</mi><mi mathvariant="normal">括</mi><mi mathvariant="normal">号</mi></mrow><annotation encoding="application/x-tex">2。10以上的参数用大括号</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">2</span><span class="mord cjk_fallback">。</span><span class="mord">1</span><span class="mord">0</span><span class="mord cjk_fallback">以</span><span class="mord cjk_fallback">上</span><span class="mord cjk_fallback">的</span><span class="mord cjk_fallback">参</span><span class="mord cjk_fallback">数</span><span class="mord cjk_fallback">用</span><span class="mord cjk_fallback">大</span><span class="mord cjk_fallback">括</span><span class="mord cjk_fallback">号</span></span></span></span>{11}</td>
</tr>
<tr>
<td style="text-align:left"><code>$#</code></td>
<td style="text-align:left">传递给脚本或函数的参数个数。</td>
</tr>
<tr>
<td style="text-align:left"><code>$*</code></td>
<td style="text-align:left">传递给脚本或函数的所有参数。把所有参数看作一个整体</td>
</tr>
<tr>
<td style="text-align:left"><code>$@</code></td>
<td style="text-align:left">传递给脚本或函数的所有参数。不过<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi mathvariant="normal">@</mi><mi mathvariant="normal">把</mi><mi mathvariant="normal">每</mi><mi mathvariant="normal">个</mi><mi mathvariant="normal">参</mi><mi mathvariant="normal">数</mi><mi mathvariant="normal">区</mi><mi mathvariant="normal">分</mi><mi mathvariant="normal">对</mi><mi mathvariant="normal">待</mi><mi mathvariant="normal">，</mi><mi mathvariant="normal">当</mi><mi mathvariant="normal">被</mi><mi mathvariant="normal">双</mi><mi mathvariant="normal">引</mi><mi mathvariant="normal">号</mi><mi mathvariant="normal">‘</mi><mi mathvariant="normal">&quot;</mi><mi mathvariant="normal">&quot;</mi><mi mathvariant="normal">‘</mi><mi mathvariant="normal">包</mi><mi mathvariant="normal">含</mi><mi mathvariant="normal">时</mi><mi mathvariant="normal">，</mi></mrow><annotation encoding="application/x-tex">@把每个参数区分对待，当被双引号`&quot; &quot;`包含时，</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.69444em;vertical-align:0em;"></span><span class="mord">@</span><span class="mord cjk_fallback">把</span><span class="mord cjk_fallback">每</span><span class="mord cjk_fallback">个</span><span class="mord cjk_fallback">参</span><span class="mord cjk_fallback">数</span><span class="mord cjk_fallback">区</span><span class="mord cjk_fallback">分</span><span class="mord cjk_fallback">对</span><span class="mord cjk_fallback">待</span><span class="mord cjk_fallback">，</span><span class="mord cjk_fallback">当</span><span class="mord cjk_fallback">被</span><span class="mord cjk_fallback">双</span><span class="mord cjk_fallback">引</span><span class="mord cjk_fallback">号</span><span class="mord">‘</span><span class="mord">&quot;</span><span class="mord">&quot;</span><span class="mord">‘</span><span class="mord cjk_fallback">包</span><span class="mord cjk_fallback">含</span><span class="mord cjk_fallback">时</span><span class="mord cjk_fallback">，</span></span></span></span>@ 与 $* 稍有不同</td>
</tr>
<tr>
<td style="text-align:left"><code>$?</code></td>
<td style="text-align:left">上个命令的退出状态，或函数的返回值</td>
</tr>
<tr>
<td style="text-align:left"><code>$$</code></td>
<td style="text-align:left">当前 Shell 进程 ID。对于Shell 脚本，就是这些脚本所在的进程 ID。</td>
</tr>
</tbody>
</table>
<p><a href="http://num.sh">num.sh</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line">num1=<span class="variable">$1</span></span><br><span class="line">num2=<span class="variable">$2</span></span><br><span class="line">result=$((<span class="variable">$num1</span>+<span class="variable">$num2</span>))</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$result</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[root@izbp1clp96b0qjo5whikc9z ~]<span class="comment"># chmod 755 num.sh</span></span><br><span class="line">[root@izbp1clp96b0qjo5whikc9z ~]<span class="comment"># ./num.sh 1 2</span></span><br><span class="line">3</span><br></pre></td></tr></table></figure>
<h2 id="预定义变量"><a class="markdownIt-Anchor" href="#预定义变量"></a> 预定义变量</h2>
<table>
<thead>
<tr>
<th>预定义变量</th>
<th>作 用</th>
</tr>
</thead>
<tbody>
<tr>
<td>$?</td>
<td>最后一次执行的命令的返回状态。如果这个变量的值为 0，则证明上一条命令正确执行；如果这 个变量的值为非 0 (具体是哪个数由命令自己来决定)，则证明上一条命令执行错误</td>
</tr>
<tr>
<td>$$</td>
<td>当前进程的进程号（PID)</td>
</tr>
<tr>
<td>$!</td>
<td>后台运行的最后一个进程的进程号（PID)</td>
</tr>
</tbody>
</table>
<h3 id="键盘输入"><a class="markdownIt-Anchor" href="#键盘输入"></a> 键盘输入</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">read</span> [选项] [变量名]</span><br><span class="line">	-p <span class="string">&quot;提示信息&quot;</span>：在等待<span class="built_in">read</span>输入时，输出提示信息</span><br><span class="line">	-t 时间（秒数）：<span class="built_in">read</span>命令会一直等待用户输入，使用此命令可以设置等待时间</span><br><span class="line">	-n 字符数：接受指定字符数</span><br><span class="line">	-s ：隐藏输入的数据，适用于密码输入</span><br></pre></td></tr></table></figure>
<h2 id="变量声明"><a class="markdownIt-Anchor" href="#变量声明"></a> 变量声明</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">declare</span> [+/-] [选项] 变量名</span><br><span class="line">		-:给变量设置类型属性</span><br><span class="line">		+：取消变量类型属性</span><br><span class="line">		-i：将变量声明为整数型</span><br><span class="line">		-x：将变量声明为环境变量</span><br><span class="line">		-p：显示指定变量的被声明的类型</span><br></pre></td></tr></table></figure>
<span id="more"></span>---
<pre><code>title: 你好世界
date: 2021-05-10 11:46:40
tags: C++
---
&lt;!-- more --&gt;---
title: 你好世界
date: 2021-05-10 11:46:40
tags: C++
---
&lt;!-- more --&gt;---
title: 你好世界
date: 2021-05-10 11:46:40
tags: C++
---
---
title: �������
date: 2021-05-10 11:46:40
tags: C++
---
</code></pre>
]]></content>
  </entry>
  <entry>
    <title>C++ 浮点数精度</title>
    <url>/2021/05/12/C++%20%E6%B5%AE%E7%82%B9%E6%95%B0%E7%B2%BE%E5%BA%A6/</url>
    <content><![CDATA[<h1 id="c-浮点数精度"><a class="markdownIt-Anchor" href="#c-浮点数精度"></a> C++ 浮点数精度</h1>
<p><code>ios::fixed</code>非科学计数</p>
<p><code>ios::scientific</code>科学计数，带指针域</p>
<p><code>setprecision()</code>来控制精度</p>
<p>注意include<code>iomanip</code>头文件</p>
<p><code>cout&lt;&lt;setiosflags(ios::fixed)&lt;&lt;setprecision(1)</code></p>
]]></content>
  </entry>
  <entry>
    <title>C++常用大小写转换</title>
    <url>/2021/05/12/C++%E5%B8%B8%E7%94%A8%E5%A4%A7%E5%B0%8F%E5%86%99%E8%BD%AC%E6%8D%A2/</url>
    <content><![CDATA[<h1 id="c常用大小写转换"><a class="markdownIt-Anchor" href="#c常用大小写转换"></a> C++常用大小写转换</h1>
<h2 id="string类"><a class="markdownIt-Anchor" href="#string类"></a> string类</h2>
<ol>
<li>如果使用string类，可以使用<code>#include &lt;algorithm&gt;</code>里的如下方法进行大小写转换；<code>transform(str.begin(),str.end(),str.begin(),::tolower)</code>，记得<code>::tolower</code>前面有<code>::</code>,　而且是<code>::tolower</code>，不是<code>::tolower()</code></li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line">string s;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;请输入一个含大写的字符串：&quot;</span>;</span><br><span class="line">    string str;</span><br><span class="line">    cin&gt;&gt;str;</span><br><span class="line">    <span class="comment">///转小写</span></span><br><span class="line">    <span class="built_in">transform</span>(str.<span class="built_in">begin</span>(),str.<span class="built_in">end</span>(),str.<span class="built_in">begin</span>(),::tolower);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为小写后为：&quot;</span>&lt;&lt;str&lt;&lt;endl;</span><br><span class="line">    <span class="built_in">transform</span>(str.<span class="built_in">begin</span>(),str.<span class="built_in">end</span>(),str.<span class="built_in">begin</span>(),::toupper);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为大写后为：&quot;</span>&lt;&lt;str&lt;&lt;endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol start="2">
<li>string类也可以自己手写两个转化为大写和小写<code>transform()</code>方法，如下所示：</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mytolower</span><span class="params">(string&amp; s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=s.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]&gt;=<span class="string">&#x27;A&#x27;</span>&amp;&amp;s[i]&lt;=<span class="string">&#x27;Z&#x27;</span>)&#123;</span><br><span class="line">            s[i]+=<span class="number">32</span>;<span class="comment">//+32转换为小写</span></span><br><span class="line">            <span class="comment">//s[i]=s[i]-&#x27;A&#x27;+&#x27;a&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mytoupper</span><span class="params">(string&amp; s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=s.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]&gt;=<span class="string">&#x27;a&#x27;</span>&amp;&amp;s[i]&lt;=<span class="string">&#x27;z&#x27;</span>)&#123;</span><br><span class="line">            s[i]-=<span class="number">32</span>;<span class="comment">//+32转换为小写</span></span><br><span class="line">            <span class="comment">//s[i]=s[i]-&#x27;a&#x27;+&#x27;A&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;请输入一个含大写的字符串：&quot;</span>;</span><br><span class="line">    string str;</span><br><span class="line">    cin&gt;&gt;str;</span><br><span class="line">    <span class="comment">///转小写</span></span><br><span class="line">    <span class="built_in">mytolower</span>(str);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为小写后为：&quot;</span>&lt;&lt;str&lt;&lt;endl;</span><br><span class="line">    <span class="built_in">mytoupper</span>(str);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为大写后为：&quot;</span>&lt;&lt;str&lt;&lt;endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="char类"><a class="markdownIt-Anchor" href="#char类"></a> char类</h2>
<ol>
<li>如果用char数组，也可以自己手写两个转化为大写和小写方法，此种方法用到了<code>tolower(char c)</code>和<code>toupper(char c)</code>两个方法</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mytolower</span><span class="params">(<span class="keyword">char</span> *s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=<span class="built_in">strlen</span>(s);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]&gt;=<span class="string">&#x27;A&#x27;</span>&amp;&amp;s[i]&lt;=<span class="string">&#x27;Z&#x27;</span>)&#123;</span><br><span class="line">            s[i]=<span class="built_in">tolower</span>(s[i]);</span><br><span class="line">            <span class="comment">//s[i]+=32;//+32转换为小写</span></span><br><span class="line">            <span class="comment">//s[i]=s[i]-&#x27;A&#x27;+&#x27;a&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mytoupper</span><span class="params">(<span class="keyword">char</span> *s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=<span class="built_in">strlen</span>(s);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]&gt;=<span class="string">&#x27;a&#x27;</span>&amp;&amp;s[i]&lt;=<span class="string">&#x27;z&#x27;</span>)&#123;</span><br><span class="line">            s[i]=<span class="built_in">toupper</span>(s[i]);</span><br><span class="line">            <span class="comment">//s[i]-=32;//+32转换为小写</span></span><br><span class="line">            <span class="comment">//s[i]=s[i]-&#x27;a&#x27;+&#x27;A&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;请输入一个含大写的字符串：&quot;</span>;</span><br><span class="line">    <span class="keyword">char</span> s[<span class="number">201</span>];</span><br><span class="line">    <span class="built_in">gets</span>(s);</span><br><span class="line">    <span class="comment">///转小写</span></span><br><span class="line"> </span><br><span class="line">    <span class="built_in">mytolower</span>(s);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为小写后为：&quot;</span>&lt;&lt;s&lt;&lt;endl;</span><br><span class="line">    <span class="built_in">mytoupper</span>(s);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为大写后为：&quot;</span>&lt;&lt;s&lt;&lt;endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol start="4">
<li>如果用char数组，也可以使用s[i]+=32或者s[i]=s[i]-‘A’+'a’的形式，实现两个转化为大写和小写方法</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mytolower</span><span class="params">(<span class="keyword">char</span> *s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=<span class="built_in">strlen</span>(s);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]&gt;=<span class="string">&#x27;A&#x27;</span>&amp;&amp;s[i]&lt;=<span class="string">&#x27;Z&#x27;</span>)&#123;</span><br><span class="line">            s[i]+=<span class="number">32</span>;<span class="comment">//+32转换为小写</span></span><br><span class="line">            <span class="comment">//s[i]=s[i]-&#x27;A&#x27;+&#x27;a&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mytoupper</span><span class="params">(<span class="keyword">char</span> *s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=<span class="built_in">strlen</span>(s);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(s[i]&gt;=<span class="string">&#x27;a&#x27;</span>&amp;&amp;s[i]&lt;=<span class="string">&#x27;z&#x27;</span>)&#123;</span><br><span class="line">            s[i]-=<span class="number">32</span>;<span class="comment">//+32转换为小写</span></span><br><span class="line">            <span class="comment">//s[i]=s[i]-&#x27;a&#x27;+&#x27;A&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;请输入一个含大写的字符串：&quot;</span>;</span><br><span class="line">    <span class="keyword">char</span> s[<span class="number">201</span>];</span><br><span class="line">    <span class="built_in">gets</span>(s);</span><br><span class="line">    <span class="comment">///转小写</span></span><br><span class="line">    <span class="built_in">mytolower</span>(s);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为小写后为：&quot;</span>&lt;&lt;s&lt;&lt;endl;</span><br><span class="line">    <span class="built_in">mytoupper</span>(s);</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;转化为大写后为：&quot;</span>&lt;&lt;s&lt;&lt;endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>C++11</title>
    <url>/2021/05/12/C++11/</url>
    <content><![CDATA[<h1 id="c11"><a class="markdownIt-Anchor" href="#c11"></a> C++11</h1>
]]></content>
  </entry>
  <entry>
    <title>C++数据类型</title>
    <url>/2021/05/12/C++%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="c数据类型"><a class="markdownIt-Anchor" href="#c数据类型"></a> C++数据类型</h1>
<p>1bit可以代表0和1，8位的内存可以有</p>
<p class="katex-block"><span class="katex-display"><span class="katex"><span class="katex-mathml"><math><semantics><mrow><msup><mn>2</mn><mn>8</mn></msup><mo>=</mo><msubsup><mi>C</mi><mn>8</mn><mn>1</mn></msubsup><mo>+</mo><msubsup><mi>C</mi><mn>8</mn><mn>2</mn></msubsup><mo>+</mo><msubsup><mi>C</mi><mn>8</mn><mn>3</mn></msubsup><mi mathvariant="normal">＋</mi><mo separator="true">⋅</mo><mo separator="true">⋅</mo><mo separator="true">⋅</mo><mo separator="true">⋅</mo><mi mathvariant="normal">＋</mi><msubsup><mi mathvariant="normal">Ｃ</mi><mn>8</mn><mn>8</mn></msubsup><mo>=</mo><mn>256</mn></mrow><annotation encoding="application/x-tex">2^8 = C_8^1+C_8^2+C_8^3＋····＋Ｃ_8^8=256
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8641079999999999em;vertical-align:0em;"></span><span class="mord"><span class="mord">2</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:1.1111079999999998em;vertical-align:-0.247em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.07153em;">C</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-2.4530000000000003em;margin-left:-0.07153em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">1</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.247em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">+</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:1.1111079999999998em;vertical-align:-0.247em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.07153em;">C</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-2.4530000000000003em;margin-left:-0.07153em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">2</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.247em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">+</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:1.1111079999999998em;vertical-align:-0.247em;"></span><span class="mord"><span class="mord mathdefault" style="margin-right:0.07153em;">C</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-2.4530000000000003em;margin-left:-0.07153em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">3</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.247em;"><span></span></span></span></span></span></span><span class="mord cjk_fallback">＋</span><span class="mpunct">⋅</span><span class="mspace" style="margin-right:0.16666666666666666em;"></span><span class="mpunct">⋅</span><span class="mspace" style="margin-right:0.16666666666666666em;"></span><span class="mpunct">⋅</span><span class="mspace" style="margin-right:0.16666666666666666em;"></span><span class="mpunct">⋅</span><span class="mspace" style="margin-right:0.16666666666666666em;"></span><span class="mord cjk_fallback">＋</span><span class="mord"><span class="mord cjk_fallback">Ｃ</span><span class="msupsub"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-2.4530000000000003em;margin-left:0em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.247em;"><span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">2</span><span class="mord">5</span><span class="mord">6</span></span></span></span></span></p>
<p>种组合，即0到255或者-128到127</p>
<p class="katex-block"><span class="katex-display"><span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>0</mn><mi mathvariant="normal">～</mi><msup><mn>2</mn><mn>8</mn></msup><mo>−</mo><mn>1</mn><mi mathvariant="normal">或</mi><mi mathvariant="normal">者</mi><mo>−</mo><msup><mn>2</mn><mn>7</mn></msup><mi mathvariant="normal">～</mi><msup><mn>2</mn><mn>7</mn></msup><mo>−</mo><mn>1</mn></mrow><annotation encoding="application/x-tex">0～2^8-1或者-2^7～2^7-1
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.9474379999999999em;vertical-align:-0.08333em;"></span><span class="mord">0</span><span class="mord cjk_fallback">～</span><span class="mord"><span class="mord">2</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">8</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.72777em;vertical-align:-0.08333em;"></span><span class="mord">1</span><span class="mord cjk_fallback">或</span><span class="mord cjk_fallback">者</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.9474379999999999em;vertical-align:-0.08333em;"></span><span class="mord"><span class="mord">2</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">7</span></span></span></span></span></span></span></span><span class="mord cjk_fallback">～</span><span class="mord"><span class="mord">2</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641079999999999em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">7</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">−</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span></span></span></span></span></p>
<h2 id="整型"><a class="markdownIt-Anchor" href="#整型"></a> 整型</h2>
<table>
<thead>
<tr>
<th>整型</th>
<th>大小(1 byte = 8 bit)</th>
</tr>
</thead>
<tbody>
<tr>
<td>short</td>
<td>至少16位</td>
</tr>
<tr>
<td>int</td>
<td>32位，至少与short一样长</td>
</tr>
<tr>
<td>long</td>
<td>64至少32位，至少与int一样长</td>
</tr>
<tr>
<td>long long</td>
<td>至少64位，且至少与long一样长</td>
</tr>
<tr>
<td>unsigned short</td>
<td>无符号short</td>
</tr>
<tr>
<td>unsigned</td>
<td>无符号int</td>
</tr>
<tr>
<td>unsigned long</td>
<td>无符号long</td>
</tr>
<tr>
<td>unsigned long long</td>
<td>无符号long long</td>
</tr>
<tr>
<td>singned</td>
<td></td>
</tr>
</tbody>
</table>
<p>unsignde是unsigned int的缩写</p>
<h2 id="浮点型"><a class="markdownIt-Anchor" href="#浮点型"></a> 浮点型</h2>
<table>
<thead>
<tr>
<th>浮点型</th>
<th>大小</th>
</tr>
</thead>
<tbody>
<tr>
<td>float</td>
<td>至少32位</td>
</tr>
<tr>
<td>double</td>
<td>至少48位，且不少于float</td>
</tr>
<tr>
<td>long double</td>
<td>至少和double一样多</td>
</tr>
<tr>
<td>unsigned short</td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
</tr>
<tr>
<td></td>
<td></td>
</tr>
</tbody>
</table>
<h2 id="字符型"><a class="markdownIt-Anchor" href="#字符型"></a> 字符型</h2>
<table>
<thead>
<tr>
<th>字符型</th>
<th>大小</th>
</tr>
</thead>
<tbody>
<tr>
<td>char</td>
<td>8位</td>
</tr>
</tbody>
</table>
]]></content>
  </entry>
  <entry>
    <title>IO</title>
    <url>/2021/05/12/IO/</url>
    <content><![CDATA[<h1 id="io"><a class="markdownIt-Anchor" href="#io"></a> I/O</h1>
<h2 id="输出"><a class="markdownIt-Anchor" href="#输出"></a> 输出</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;fstream&gt;</span></span></span><br><span class="line">ofstream fout;</span><br><span class="line">fout.<span class="built_in">open</span>(<span class="string">&quot;???.txt&quot;</span>);</span><br><span class="line"><span class="comment">//ofstream fout(&quot;???.txt&quot;)</span></span><br></pre></td></tr></table></figure>
<h2 id="输入"><a class="markdownIt-Anchor" href="#输入"></a> 输入</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;fstream&gt;</span></span></span><br><span class="line">ifstream fin;</span><br><span class="line">fin.<span class="built_in">open</span>(<span class="string">&quot;???.txt&quot;</span>);</span><br><span class="line"><span class="comment">//ifstream fin(&quot;???.txt&quot;)</span></span><br></pre></td></tr></table></figure>
<p>输入与输出都是缓冲的</p>
<p>fin有一个管理的输入缓冲区，fout有输出缓冲区</p>
<h2 id="关闭"><a class="markdownIt-Anchor" href="#关闭"></a> 关闭</h2>
<p>当输入和输出流对象过期（例如程序终止）时，与文件的链接自动关闭。也可以用close关闭</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">fout.<span class="built_in">close</span>();</span><br><span class="line">fin.<span class="built_in">close</span>();</span><br></pre></td></tr></table></figure>
<p>关闭并不会删除流，而是切断流与文件的连接</p>
<h2 id="流状态"><a class="markdownIt-Anchor" href="#流状态"></a> 流状态</h2>
<p>流类都是从ios_base继承了一个流状态成员</p>
<ul>
<li>打开不存在的文件进行输入时，设置failbit位</li>
</ul>
<h2 id="检查文件是否打开和isopen"><a class="markdownIt-Anchor" href="#检查文件是否打开和isopen"></a> 检查文件是否打开和<code>isopen()</code></h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">fin.<span class="built_in">open</span>(<span class="string">&quot;???.txt&quot;</span>);</span><br><span class="line"><span class="keyword">if</span>(fin.<span class="built_in">fail</span>())</span><br><span class="line">&#123;</span><br><span class="line">    ......;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">fin.<span class="built_in">open</span>(<span class="string">&quot;???.txt&quot;</span>);</span><br><span class="line"><span class="keyword">if</span>(!fin)</span><br><span class="line">&#123;</span><br><span class="line">    ......;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">fin.<span class="built_in">open</span>(<span class="string">&quot;???.txt&quot;</span>);</span><br><span class="line"><span class="keyword">if</span>(!fin.<span class="built_in">is_open</span>())</span><br><span class="line">&#123;</span><br><span class="line">    ......;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="命令行处理"><a class="markdownIt-Anchor" href="#命令行处理"></a> 命令行处理</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span></span></span><br></pre></td></tr></table></figure>
<p>argc是命令行中的参数个数包括命令行本身。argv指向一个指向char的指针，argv[0]是一个指针指向存储的第一个命令行字符。</p>
<h2 id="文件模式"><a class="markdownIt-Anchor" href="#文件模式"></a> 文件模式</h2>
<p><img src="/media/zero/%E5%AD%A6%E4%B9%A0/markdown/%E5%9B%BE%E5%BA%8A/%E6%96%87%E4%BB%B6%E6%A8%A1%E5%BC%8F.png" alt="文件模式"></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">fout.<span class="built_in">open</span>(<span class="string">&quot;???.txt&quot;</span>, ios_base::out|ios_base::app); <span class="comment">//在文件尾追加</span></span><br></pre></td></tr></table></figure>
<h2 id="内核格式化"><a class="markdownIt-Anchor" href="#内核格式化"></a> 内核格式化</h2>
<p>sstream头文件</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line">    ostringstream outstr;</span><br><span class="line">    string naem;</span><br><span class="line">    cin &gt;&gt; name;</span><br><span class="line">    outstr &lt;&lt; naem;<span class="comment">//不会打印出来</span></span><br><span class="line">    cout &lt;&lt; outstr.<span class="built_in">str</span>() <span class="comment">//显示打印出来</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>str()返回一个被初始化为缓冲区内容的字符串对象，str可以“冻结”对象，这样便不能写入信息</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">istringstream <span class="title">instr</span><span class="params">(lit)</span></span>;</span><br></pre></td></tr></table></figure>
<p>istringstream 类允许使用istream方法族读取 istringstream 对象中的数据，istringstream对象可以使用string对象进行初始化。</p>
<p>假设facts是一个string对象，则要创建与该字符串相关联的istringstream对象，可以这样编写:<br>
istringstream instr(facts);  // use facts to initialize stream</p>
<p>这样便可以使用istream方法读取instr中的数据。<br>
例如，如果instr包含大量字符格式的整数，则可以这样读取它们:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> n;</span><br><span class="line"><span class="keyword">int</span> sum = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span> (instr &gt;&gt; n)</span><br><span class="line">	sum += n;</span><br></pre></td></tr></table></figure>
<p>简而言之，istringstream 和 ostringstream 类使得能够使用 istream 和 ostream 类的方法来管理存储在字符串中的字符数据。</p>
]]></content>
  </entry>
  <entry>
    <title>Linux服务管理</title>
    <url>/2021/05/12/Linux%E6%9C%8D%E5%8A%A1%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="linux服务和进程管理"><a class="markdownIt-Anchor" href="#linux服务和进程管理"></a> Linux服务和进程管理</h1>
<h2 id="服务分类"><a class="markdownIt-Anchor" href="#服务分类"></a> 服务分类</h2>
<p><img src="/2021/05/12/Linux%E6%9C%8D%E5%8A%A1%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5CLinux%E6%9C%8D%E5%8A%A1%E5%88%86%E7%B1%BB.png" alt></p>
<ul>
<li>
<p>整个linux系统是由rpm安装的，所以rpm包安装的跟Linux本身默认值一样</p>
</li>
<li>
<p>独立的服务是直接运行在内存中，响应速度快，但耗费资源</p>
</li>
<li>
<p>xinetd服务本身是独立的，但是它可以管理其他的服务。它管理的服务不占用内存，但是它本身占用内存。不耗费资源，但响应速度慢</p>
</li>
</ul>
<h2 id="查看已安装服务"><a class="markdownIt-Anchor" href="#查看已安装服务"></a> 查看已安装服务</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#RPM包安装的服务,查看服务的自启动状态</span></span><br><span class="line">chkconfig --list</span><br><span class="line"><span class="comment">#源码包安装的服务</span></span><br><span class="line">一般是/usr/<span class="built_in">local</span>/</span><br></pre></td></tr></table></figure>
<p>rpm包安装的位置是默认位置，源码包是自定义目录。service启动命令搜索的是/etc/rc/d/init.d/中的服务，源码包安装的服务是不会写入这个目录里</p>
<h2 id="rpm包安装的服务管理"><a class="markdownIt-Anchor" href="#rpm包安装的服务管理"></a> RPM包安装的服务管理</h2>
<h2 id="独立服务的管理"><a class="markdownIt-Anchor" href="#独立服务的管理"></a> 独立服务的管理</h2>
<h3 id="rpm安装的默认位置"><a class="markdownIt-Anchor" href="#rpm安装的默认位置"></a> rpm安装的默认位置</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/init.d/ 	 <span class="comment">#启动独立服务脚本的位置</span></span><br><span class="line">/etc/sysconfig/  <span class="comment">#初始化环境配置文件位置</span></span><br><span class="line">/etc/ 			<span class="comment">#配置文件位置</span></span><br><span class="line">/etc/xinetd.conf <span class="comment">#xinetd服务的启动脚本</span></span><br><span class="line">/etc/xinetd.d/   <span class="comment">#基于xinetd服务的启动脚本</span></span><br><span class="line">/var/lib/ 		<span class="comment">#服务产生的数据放在这里</span></span><br><span class="line">/var/<span class="built_in">log</span>/ 		<span class="comment">#日志</span></span><br></pre></td></tr></table></figure>
<h3 id="独立服务的启动"><a class="markdownIt-Anchor" href="#独立服务的启动"></a> 独立服务的启动</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/etc/init.d/独立服务的名字 start|stop|status|restart| <span class="comment">#method1</span></span><br><span class="line">service 独立服务的名字 start|stop||status <span class="comment">#method2,service是rad hat系列专有，service实际上是到/etc/init.d/目录下搜索服务</span></span><br><span class="line">systemctl [OPTIONS...] &#123;COMMAND&#125; <span class="comment">#method3</span></span><br></pre></td></tr></table></figure>
<h3 id="独立服务的自启动"><a class="markdownIt-Anchor" href="#独立服务的自启动"></a> 独立服务的自启动</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chkconfig [--level 运行级别] [独立服务名] [on|off] <span class="comment">#method 1</span></span><br><span class="line">/etc/rc.d/rc.local <span class="comment">#method 2修改这个文件,将启动命令写入此文件/etc/init.d/httpd start</span></span><br><span class="line">ntsysv <span class="comment">#method 3 red hat专有命令</span></span><br></pre></td></tr></table></figure>
<h3 id="基于xinetd服务不常用了"><a class="markdownIt-Anchor" href="#基于xinetd服务不常用了"></a> 基于xinetd服务（不常用了）</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install xinetd <span class="comment">#安装xinetd服务</span></span><br></pre></td></tr></table></figure>
<h2 id="查看系统中的所有进程"><a class="markdownIt-Anchor" href="#查看系统中的所有进程"></a> 查看系统中的所有进程</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps -aux <span class="comment">#查看系统中所有进程，使用BSD操作系统格式</span></span><br><span class="line">ps -le <span class="comment">#查看系统中所有进程，使用LINUX标准命令格式</span></span><br></pre></td></tr></table></figure>
<h2 id="查看系统健康状态"><a class="markdownIt-Anchor" href="#查看系统健康状态"></a> 查看系统健康状态</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">top [option]</span><br><span class="line">	-d 秒数：指定top命令几秒更新，默认3秒</span><br><span class="line">在top命令交互模式中可以执行的命令</span><br><span class="line">? or h：显示交互模式的帮助</span><br><span class="line">P：以CPU使用率排序，默认</span><br><span class="line">M：以内存使用率排序</span><br><span class="line">N：以PID排序</span><br><span class="line">q：退出</span><br></pre></td></tr></table></figure>
<h2 id="查看进程树"><a class="markdownIt-Anchor" href="#查看进程树"></a> 查看进程树</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pstree [option]</span><br><span class="line">	   -p：显示进程PID</span><br><span class="line">	   -u：显示进程所属用户</span><br></pre></td></tr></table></figure>
<h2 id="终止进程"><a class="markdownIt-Anchor" href="#终止进程"></a> 终止进程</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">kill</span> -l <span class="comment">#查看可用进程信号</span></span><br><span class="line"><span class="built_in">kill</span> [option] PID <span class="comment">#以信号代号的功能来操作进程</span></span><br></pre></td></tr></table></figure>
<p><img src="/2021/05/12/Linux%E6%9C%8D%E5%8A%A1%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Ckill%E8%BF%9B%E7%A8%8B%E4%BF%A1%E5%8F%B7.png" alt></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">killall [option][sign] 进程名字</span><br><span class="line">		-i：交互式，询问是否杀死某个进程</span><br><span class="line">		-I：忽略进程名大小写</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pkill  [option][sign] 进程名字</span><br><span class="line">		-t 终端号：按终端号将用户踢出</span><br></pre></td></tr></table></figure>
<h2 id="将进程放入后台和恢复"><a class="markdownIt-Anchor" href="#将进程放入后台和恢复"></a> 将进程放入后台和恢复</h2>
<ol>
<li>在进程执行后加&amp;，<strong>此方法放入后台是还会运行在后台</strong></li>
<li>在执行进程时，按ctrl+z，<strong>此方法在后台是暂停的</strong></li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">jobs</span> <span class="comment">#查看后台</span></span><br><span class="line"><span class="built_in">jobs</span> -l <span class="comment">#显示后台PID</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">fg</span> %工作号 <span class="comment">#恢复到前台</span></span><br><span class="line"><span class="built_in">bg</span> %工作好 <span class="comment">#重新放入后台</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>RTTI</title>
    <url>/2021/05/12/RTTI/</url>
    <content><![CDATA[<h1 id="rtti"><a class="markdownIt-Anchor" href="#rtti"></a> RTTI</h1>
<h2 id="dynamic_cast"><a class="markdownIt-Anchor" href="#dynamic_cast"></a> dynamic_cast</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Grand</span>&#123;</span>...;&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Superb</span> :</span> <span class="keyword">public</span> Grand&#123;...;&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Magnificent</span> ;</span> <span class="keyword">public</span> Superb&#123;...;&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">//pointer</span></span><br><span class="line">Super * pm = <span class="keyword">dynamic_cast</span>&lt;Super *&gt;(pg);</span><br></pre></td></tr></table></figure>
<p>使用派生类指针指向基类的指针是不安全的。</p>
<p><strong>然而</strong></p>
<p>使用<code>dynamic_cast&lt;typename *&gt;(对象)</code></p>
<p>相当于提出了这样一个问题：括号里的类型是否可以被安全的转化为<code>typename *</code>如果可以则返回对象的地址，若不行则返回一个空指针。</p>
<h2 id="typeod和type_info"><a class="markdownIt-Anchor" href="#typeod和type_info"></a> typeod和type_info</h2>
<p>typeid可以确定两个对象是否是一个类型。typeid可以返回一个type_info对象的引用。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in"><span class="keyword">typeid</span></span>(Magnificent); <span class="comment">//返回一个typeinfo对象的引用</span></span><br><span class="line"><span class="built_in"><span class="keyword">typeid</span></span>(Magnificent) == <span class="built_in"><span class="keyword">typeid</span></span>(*pg); <span class="comment">//可以利用typeid比较两个对象是否是同一个</span></span><br></pre></td></tr></table></figure>
<p><code>typeid(*pg).name()</code>typeinfo有一个name函数，通常返回内容是类名的字符串。</p>
<h1 id="类型转换符"><a class="markdownIt-Anchor" href="#类型转换符"></a> 类型转换符</h1>
<p>C语言中类型转换过于松散，所以采用更加严格的方式来进行类型转换。</p>
<ul>
<li>dynamic_cast</li>
<li>const_cast</li>
<li>static_cast</li>
<li>reinterpret_cast</li>
</ul>
<h2 id="const_cast"><a class="markdownIt-Anchor" href="#const_cast"></a> const_cast</h2>
<p><code>const_cast&lt;type-name&gt; (expression)</code></p>
<p>这个运算符可以删掉const标签。这个运算符原因是需要这样一个值，它在大多数情况下是常量但有时也是可以修改的</p>
<h2 id="static_cast"><a class="markdownIt-Anchor" href="#static_cast"></a> static_cast</h2>
<p><code>static_cast&lt;type-name&gt;(expression)</code></p>
<p>仅当type-name可以被隐式转换为expression的类型，或者expression可被隐式转换为type-name所属的类型时，上述转换才被允许。</p>
<h2 id="reinterpret_cast"><a class="markdownIt-Anchor" href="#reinterpret_cast"></a> reinterpret_cast</h2>
<p><code>reinterpret_cast&lt;type-name&gt;(expression)</code></p>
]]></content>
  </entry>
  <entry>
    <title>STL</title>
    <url>/2021/05/12/STL/</url>
    <content><![CDATA[<h1 id="stl"><a class="markdownIt-Anchor" href="#stl"></a> STL</h1>
<h2 id="map"><a class="markdownIt-Anchor" href="#map"></a> map</h2>
]]></content>
  </entry>
  <entry>
    <title>SLT</title>
    <url>/2021/05/12/SLT/</url>
    <content><![CDATA[<h1 id="stl"><a class="markdownIt-Anchor" href="#stl"></a> STL</h1>
<p>标准模板类，SLT提供一组表示容器，迭代器，函数对象和算法的模板</p>
<h2 id="操作"><a class="markdownIt-Anchor" href="#操作"></a> 操作</h2>
<ul>
<li>size() 返回容器中的元素数目</li>
<li>swap() 交换两个容器的内容</li>
<li>begin() 返回一个指向容器中第一个元素的迭代器</li>
<li>end() 返回一个超过容器尾的迭代器</li>
<li>push_back(对象) 将元素添加到矢量末尾</li>
<li><code>erase(scores.begin(), score.begin()+2)</code>删除矢量中给定区间的元素</li>
<li>insert(位置，开始，结尾)</li>
</ul>
<p><code>old.insert(old.begin(), new.begin()+1, new.end());</code></p>
<h2 id="vector"><a class="markdownIt-Anchor" href="#vector"></a> vector</h2>
<p><code>vector&lt;typename&gt; a(size)</code></p>
<h2 id="迭代器"><a class="markdownIt-Anchor" href="#迭代器"></a> 迭代器</h2>
<p>广义指针</p>
<p>该迭代器的类型是一个名为iterator的typedef，其作用域为整个类。</p>
<p>例如，要为vector的double类型规范声明一个迭代器，可以这样做：</p>
<p><code>vector&lt;double&gt;：：iterator pd；//pd an iterator</code></p>
<p>假设scores是一个vector<double>对象：</double></p>
<p><code>vector&lt;double&gt; scores；</code></p>
<p>则可以使用迭代器pd执行这样的操作：</p>
<p><code>pd=scores.begin()；//have pd point to the first element</code></p>
<p><code>*pd=22.3；//dereference pd and assign value to first element</code></p>
<p><code>++pd；//make pd point to the next element</code></p>
<p>正如您看到的，迭代器的行为就像指针。顺便说一句，还有一个C++11自动类型推断很有用的地方。</p>
<h2 id="其他方法"><a class="markdownIt-Anchor" href="#其他方法"></a> 其他方法</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">for_each()</span><br></pre></td></tr></table></figure>
<p>三个参数，前两个是定义容器中间区域的迭代器，最后一个是指向函数的指针。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;Review&gt;::iterator pr;</span><br><span class="line"><span class="keyword">for</span> (prb= books.<span class="built_in">begin</span>(); pr != books.<span class="built_in">end</span>(); pr++)</span><br><span class="line">    <span class="built_in">ShowReview</span>(*pr);</span><br><span class="line">可被替换为：</span><br><span class="line">for_each(books.<span class="built_in">begin</span>(), books.<span class="built_in">end</span>(), ShowReview);</span><br></pre></td></tr></table></figure>
<p><code>Random_shuffle()</code></p>
<p><strong>注意：改函数要求容器类允许随机访问，vector类可以做到这一点</strong></p>
<p>接受两个指定区间的迭代器参数，并随机排列该区域中的元素</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">random_shuffle</span>(books.<span class="built_in">begin</span>(), books.<span class="built_in">end</span>());</span><br><span class="line">或者：</span><br><span class="line">vector&lt;<span class="keyword">int</span>&gt; coolstuff;</span><br><span class="line"><span class="built_in">sort</span>(coolstuff.<span class="built_in">begin</span>(), coolstuff.<span class="built_in">end</span>());</span><br></pre></td></tr></table></figure>
<p>sort（）函数也要求容器支持随机访问。该函数有两个版本，第一个版本接受两个定义区间的迭代器参数，并使用为存储在容器中的类型</p>
<p>元素定义的&lt;运算符，对区间中的元素进行操作。例如，下面的语句按升序对coolstuff的内容进行排序，排序时使用内置的&lt;运算符对值进行比较</p>
<p>如果绕那个其元素是用户定义的对象，则要用sort()，必须定义能够处理该类型对象的operator&lt;函数</p>
<p>例如，如果为Review提供了成员或非成员函数operator&lt;（），则可以对包含Review对象的矢量进行排序。</p>
<p>由于Review是一个结构，因此其成员是公有的，这样的非成员函数将为：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> <span class="keyword">operator</span>&lt;(<span class="keyword">const</span> Review &amp; r1, <span class="keyword">const</span> Review &amp; r2)</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span>(rl. title&lt; r2. title)</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span>; </span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (rl. title==r2. title &amp;&amp; rl. rating&lt; r2. rating)</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span>; </span><br><span class="line"><span class="keyword">else</span> </span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>有了这样的函数后，就可以对包含Review对象（如books）的矢量进行排序了：<br>
<code>sort(books.begin()，books.end());</code></p>
<h2 id="基于范围的for循环"><a class="markdownIt-Anchor" href="#基于范围的for循环"></a> 基于范围的for循环</h2>
<p>基于范围的for循环是为用于STL而设计的。为复习该循环：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> prices[<span class="number">5</span>]=&#123;<span class="number">4.99</span>，<span class="number">10.99</span>，<span class="number">6.87</span>，<span class="number">7.99</span>，<span class="number">8.49</span>&#125;:<span class="keyword">for</span>(doublex:prices)</span><br><span class="line">cout&lt;&lt;x&lt;&lt;std:：end1；</span><br></pre></td></tr></table></figure>
<p>在这种for循环中，括号内的代码声明一个类型与容器存储的内容相同的变量，然后指出了容器的名称。接下来，循环体使用指定的变量依次访问容器的每个元素。</p>
<p>例如，<br>
<code>for_each(books.begin()，books.end()，ShowReview)；</code></p>
<p>可将其替换为下述基于范围的for循环：<br>
<code>for(autox:books)ShowReview(x)；</code></p>
<p>根据book的类型（<code>vector&lt;Review&gt;</code>），编译器将推断出x的类型为Review，而循环将依次将books中的每个Review对象传递给ShowReview（）。</p>
<p>不同于for_each()，基于范围的for循环可修改容器的内容，诀窍是指定一个引用参数。例如，假设有如下函数：</p>
<p><code>void InflateReview(Review&amp;r)&#123;r.rating++;&#125;</code></p>
<p>可使用如下循环对books的每个元素执行该函数：</p>
<p><code>for(auto&amp;x:books)InflateReview(x)；</code></p>
]]></content>
  </entry>
  <entry>
    <title>Shell数值运算和运算符</title>
    <url>/2021/05/12/Shell%E6%95%B0%E5%80%BC%E8%BF%90%E7%AE%97%E5%92%8C%E8%BF%90%E7%AE%97%E7%AC%A6/</url>
    <content><![CDATA[<h1 id="shell数值运算和运算符"><a class="markdownIt-Anchor" href="#shell数值运算和运算符"></a> Shell数值运算和运算符</h1>
<table>
<thead>
<tr>
<th>算术运算符</th>
<th>说明/含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>+、-</td>
<td>加法（或正号）、减法（或负号）</td>
</tr>
<tr>
<td>*、/、%</td>
<td>乘法、除法、取余（取模）</td>
</tr>
<tr>
<td>**</td>
<td>幂运算</td>
</tr>
<tr>
<td>++、–</td>
<td>自增和自减，可以放在变量的前面也可以放在变量的后面</td>
</tr>
<tr>
<td>!、&amp;&amp;、||</td>
<td>逻辑非（取反）、逻辑与（and）、逻辑或（or）</td>
</tr>
<tr>
<td>&lt;、&lt;=、&gt;、&gt;=</td>
<td>比较符号（小于、小于等于、大于、大于等于）</td>
</tr>
<tr>
<td>==、!=、=</td>
<td>比较符号（相等、不相等；对于字符串，= 也可以表示相当于）</td>
</tr>
<tr>
<td>&lt;&lt;、&gt;&gt;</td>
<td>向左移位、向右移位</td>
</tr>
<tr>
<td>~、|、 &amp;、^</td>
<td>按位取反、按位或、按位与、按位异或</td>
</tr>
<tr>
<td>=、+=、-=、*=、/=、%=</td>
<td>赋值运算符，例如 a+=1 相当于 a=a+1，a-=1 相当于 a=a-1</td>
</tr>
</tbody>
</table>
<h2 id="数值运算"><a class="markdownIt-Anchor" href="#数值运算"></a> 数值运算</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$((运算式)) 将字符串类型转换为数字</span><br></pre></td></tr></table></figure>
<h2 id="变量测试与内容替换"><a class="markdownIt-Anchor" href="#变量测试与内容替换"></a> 变量测试与内容替换</h2>
<p><img src="/2021/05/12/Shell%E6%95%B0%E5%80%BC%E8%BF%90%E7%AE%97%E5%92%8C%E8%BF%90%E7%AE%97%E7%AC%A6/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%8F%98%E9%87%8F%E7%BD%AE%E6%8D%A2.png" alt></p>
]]></content>
  </entry>
  <entry>
    <title>bindlst</title>
    <url>/2021/05/12/bindlst/</url>
    <content><![CDATA[<h1 id="bind1st和binder2nd"><a class="markdownIt-Anchor" href="#bind1st和binder2nd"></a> <code>bind1st</code>和<code>binder2nd</code></h1>
<p><code>bind1st</code>可以将自适应二元函数转换为一元函数</p>
<p>例如</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">f2</span>(); <span class="comment">//二元函数 void f2(int a, int b)</span></span><br><span class="line"><span class="built_in">bind1st</span>(f2, val) <span class="built_in">f1</span>(x); <span class="comment">//等价于f2(val, x),而f1是一个一元函数</span></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">bind1st</span>(mutiplies&lt;<span class="keyword">double</span>&gt;(), <span class="number">2.5</span>) <span class="built_in">f</span>(<span class="number">2</span>);</span><br></pre></td></tr></table></figure>
<p>bind2nd与它类似，只是将常数赋给第二个参数</p>
]]></content>
  </entry>
  <entry>
    <title>cin</title>
    <url>/2021/05/12/cin/</url>
    <content><![CDATA[<p>C++的输入是一种流，而cin就是这种流的对象。</p>
<span id="more"></span>
<h2 id="cin"><a class="markdownIt-Anchor" href="#cin"></a> cin &gt;&gt;</h2>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line">string str1, str2;</span><br><span class="line">cin &gt;&gt; str1;<span class="comment">//&quot;abc bcd&quot;</span></span><br><span class="line">cin &gt;&gt; str2;</span><br></pre></td></tr></table></figure>
<p>&gt;&gt;是一种运算符用于cin表示将字符串插入到流中。其中cin&gt;&gt;在读入字符时遇到<strong>空格</strong>或<strong>回车</strong>时会终止读入字符并且不会把空格或回车符从缓冲区中去除，再次调用cin时会遇见上次缓冲区残留的空格<strong>但是cin会将它忽略并且不将他储存在变量中</strong>。例如str1读入的&quot;abc&quot;,而str2读入的是&quot;bcd&quot;而不是&quot;空格bcd&quot;。</p>
<h2 id="cinget"><a class="markdownIt-Anchor" href="#cinget"></a> cin.get</h2>
<p>（１)ch = cin.get()读取一个字符</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char a;</span><br><span class="line">a = cin.get();</span><br></pre></td></tr></table></figure>
<p>但是它不会忽略空格，即使遇见空格也会将它读入。</p>
<p>（2）cin.get(char)读取一个字符</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char b;</span><br><span class="line">cin.get(b);</span><br></pre></td></tr></table></figure>
<p>（3）cin.get()读取空格和换行（在输入流中去除空格和换行）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char a[20];//键盘输入apple pandas</span><br><span class="line">(cin &gt;&gt; a).get()//输入流中pandas前面的空格被get()吃掉了，不会影响后续的读入</span><br></pre></td></tr></table></figure>
<p>（3）cin.get(array, Asize)读取一行</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char a[20];</span><br><span class="line">cin.get(a, 20);</span><br></pre></td></tr></table></figure>
<p>其中有两个参数数组ａ和读取的最大字符个数20。从缓冲区中读入最多20格字符储存在数组ａ中。</p>
<p>同样它会读入空格和换行符。</p>
<h2 id="cingetline"><a class="markdownIt-Anchor" href="#cingetline"></a> cin.getline</h2>
<p>cin.getline读取一行。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char a[20];</span><br><span class="line">cin.getline(a, 20)</span><br></pre></td></tr></table></figure>
<p>其中两个参数数组ａ和读取最大字符个数20。但是与cin.get有一点不同，**当cin.getline遇到空格或换行符时并不会将它读入而且还会把它从缓冲区中删除。**这一点很重要不要记混了。</p>
<h2 id="string的getline函数"><a class="markdownIt-Anchor" href="#string的getline函数"></a> string的getline函数</h2>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;string&gt;//要使用getline（cin，string）必须包括头文件string</span><br><span class="line">string str1;</span><br><span class="line">getline(cin, str1);</span><br></pre></td></tr></table></figure>
<p>string是c++风格的字符串，getline(cin, string)是在string文件中的特有函数只对string对象操作。<strong>它与cin.getline有一点相同它不会从缓冲区读入换行符，而且会将它们从缓冲区删除。</strong></p>
<h2 id="cinclear"><a class="markdownIt-Anchor" href="#cinclear"></a> cin.clear()</h2>
<p>它不是读取字符的函数，而是清除错误标记的函数。当我们用cin读取错误时，会在缓冲区留下一个错误标记，阻止后面的cin读取。所以为了让后面的cin正常读取就需要删除错误标记。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a;</span><br><span class="line">char b;</span><br><span class="line">cin &gt;&gt; a;//键盘敲入字符&#x27;a&#x27;，它与ａ的int定义不匹配，cin不读取&#x27;a&#x27;并且会在缓冲区留下错区标记</span><br><span class="line">cin &gt;&gt; b;//此处的cin并不会正常读入用户的下一次输入</span><br></pre></td></tr></table></figure>
<p><strong>使用cin.clear()</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> int a;</span><br><span class="line">char b;</span><br><span class="line">cin &gt;&gt; a;//键盘敲入字符&#x27;a&#x27;，它与ａ的int定义不匹配，cin不读取&#x27;a&#x27;并且会在缓冲区留下错区标记</span><br><span class="line">cin.clear();//清除错误标记</span><br><span class="line">cin &gt;&gt; b;//此处的cin会正常读入用户的下一次输入</span><br></pre></td></tr></table></figure>
<p><strong>补充:</strong></p>
<p><strong>１、cin可用于条件判断句中</strong></p>
<p>首先要明确三点</p>
<p>（1）流的正常状态，无论是输入流和输出流都会有三个标记eofbit、badbit、failbit。当它们都为0的时候表示流是正常状态。</p>
<p>（2）cin在条件句中是自测来实现True和False，若cin流正常则是True反之为False。</p>
<p>（3）&gt;&gt; 操作符返回左边对象也就是cin对象</p>
<p><strong>用于while中</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a;</span><br><span class="line">while(cin &gt;&gt; a)</span><br><span class="line">&#123;</span><br><span class="line"> cout &lt;&lt; &quot;hello&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>· 输入整数12a，cin&gt;&gt;从缓冲区也就是输入流中读取12不读取’a’，与int匹配，三个标记均为0，返回cin（非0值）为True执行while循环。a</p>
<p>· 输入a， cin&gt;&gt;从输入流中读取’a’与int不匹配,读取失败，failbit标记为1，'a’被留在输入流中，返回0为False不执行循环。</p>
<p>·输入Ctrl+z（windows的EOF）cin将eofbit和failbit设置为1，返回False。也可以用以下方法判断EOF</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int ch;</span><br><span class="line">ch = cin.get();</span><br><span class="line">if(ch != EOF)</span><br></pre></td></tr></table></figure>
<p><strong>用于if中</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a;</span><br><span class="line">cin &gt;&gt; a;</span><br><span class="line">if(cin)</span><br><span class="line">&#123;</span><br><span class="line">  cout &lt;&lt; &quot;hello&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>· 输入12a，cin读取12不读取a，返回的cin对象正常（非0值），所以到if句中检测通过执行。</p>
<p>· 输入a，cin读取’a’与int不匹配，返回的cin异常（0值），if句中检测未通过。</p>
<p>· 输入Ctrl+z(windows的EOF)，返回0。</p>
<h2 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h2>
<ol>
<li>
<p><strong>输入函数中get和getline字眼的函数读取空格和换行，而cin&gt;&gt;忽略。</strong></p>
</li>
<li>
<p><strong>getline字眼的函数会将空格和换行从输入流中删除，get字眼的函数不会。</strong></p>
</li>
<li>
<h2 id="cin可用于条件判断中若成功读入返回cin对象非0值若读入失败或者是eof则返回0-条件句中用cin多用于判断用户输入是否非法或者磁盘读写是否有误-more-title-你好世界date-2021-05-10-114640tags-c"><a class="markdownIt-Anchor" href="#cin可用于条件判断中若成功读入返回cin对象非0值若读入失败或者是eof则返回0-条件句中用cin多用于判断用户输入是否非法或者磁盘读写是否有误-more-title-你好世界date-2021-05-10-114640tags-c"></a> <strong>cin可用于条件判断中，若成功读入返回cin对象（非0值），若读入失败或者是EOF则返回0。条件句中用cin多用于判断用户输入是否非法或者磁盘读写是否有误。</strong><!-- more -->—<br>
title: 你好世界<br>
date: 2021-05-10 11:46:40<br>
tags: C++</h2>
<h2 id="-more-title-你好世界date-2021-05-10-114640tags-c"><a class="markdownIt-Anchor" href="#-more-title-你好世界date-2021-05-10-114640tags-c"></a> <!-- more -->—<br>
title: 你好世界<br>
date: 2021-05-10 11:46:40<br>
tags: C++</h2>
</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>cin的补充</title>
    <url>/2021/05/12/cin%E7%9A%84%E8%A1%A5%E5%85%85/</url>
    <content><![CDATA[<h1 id="cin的补充"><a class="markdownIt-Anchor" href="#cin的补充"></a> cin的补充</h1>
<h2 id><a class="markdownIt-Anchor" href="#"></a> &gt;&gt;</h2>
<p>连续读入有效的字符，直到遇见非有效部分（例如：空格，回车，或者不同于&gt;&gt;后类型的其他类型值），并把这些字符转换为需要的类型</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a;</span><br><span class="line">cin &gt;&gt; a;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-123 ;    //只读入-123</span><br><span class="line">-123</span><br><span class="line">;         //只读入-123</span><br><span class="line">-123Z;		//只读入-123,Z不是整型不算有效字符</span><br></pre></td></tr></table></figure>
<p><strong>以上字符连续抽取有效部分直到无效部分停止，并且把-123字符转换为-123整型（int）</strong></p>
<p><strong>Z将留在输入流，下一个cin将从Z开始读取，而-123将会被cin转换为整型被赋给a.</strong></p>
<p><strong>如果cin遇到非有效字符并不会读入，并且cin返回0（false）</strong></p>
<h2 id="错误输入"><a class="markdownIt-Anchor" href="#错误输入"></a> 错误输入</h2>
<p>cin和cout对象包含一个描述流状态的数据成员（从ios_base）继承过来的。流状态是iostate类型，iostate是bitmask类型。流状态由三个ios_base元素组成:eofit、badfit、failbit，每个元素都是一位，可以是1或0</p>
<ul>
<li><strong>cin未能读取预期的字符时，cin将状态位设置failbit</strong></li>
<li><strong>cin读取到文件尾，cin将状态位设置eofbit</strong></li>
<li><strong>cin试图读取不可读取的文件或磁盘I/O失败是，cin将状态位设置badbit</strong></li>
</ul>
<h2 id="流状态"><a class="markdownIt-Anchor" href="#流状态"></a> 流状态</h2>
<p><img src="/2021/05/12/cin%E7%9A%84%E8%A1%A5%E5%85%85/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Cimage-20200916170654580.png" alt="image-20200916170654580"></p>
<p><strong><code>claear(iostate s = 0)</code></strong></p>
<p>默认清除三个状态位。当有参数时，它清除三个位，并将参数的位设置进去</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">clear</span>(); <span class="comment">//清除三个位</span></span><br><span class="line"><span class="built_in">clear</span>(eofbit); <span class="comment">//设置eofit，清除其他位</span></span><br></pre></td></tr></table></figure>
<p><strong><code>setstate(iostate s)</code></strong></p>
<p>设置s位，其他位保持不变</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setstate</span>(eofbit); <span class="comment">//设置eofit,其余位不变</span></span><br></pre></td></tr></table></figure>
<h2 id="clear"><a class="markdownIt-Anchor" href="#clear"></a> <code>clear()</code></h2>
<p>clear虽然能重置状态位，但是并不能消除流中的错误字符。</p>
<p>消除的方法如下</p>
<ol>
<li><code>isspace()</code></li>
</ol>
<p>cctype函数。如果参数是空白字符时返回true</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(!<span class="built_in">isspace</span>(cin.<span class="built_in">get</span>()))</span><br><span class="line">    <span class="keyword">continue</span>;</span><br></pre></td></tr></table></figure>
<ol start="2">
<li><code>cin.get</code></li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(cin.<span class="built_in">get</span>())</span><br><span class="line">    <span class="keyword">continue</span>;</span><br></pre></td></tr></table></figure>
<h2 id="get"><a class="markdownIt-Anchor" href="#get"></a> <code>get()</code></h2>
<p>get函数常用重载</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">std::istream &amp;std::istream::<span class="built_in">get</span>(<span class="keyword">char</span> &amp;__c);</span><br><span class="line"><span class="keyword">int</span> std::istream::<span class="built_in">get</span>()</span><br></pre></td></tr></table></figure>
<p><img src="/media/zero/%E5%AD%A6%E4%B9%A0/markdown/%E5%9B%BE%E5%BA%8A/get%E7%9A%84%E9%87%8D%E8%BD%BD.png" alt="get的重载"></p>
<h2 id="getline-get-ignore"><a class="markdownIt-Anchor" href="#getline-get-ignore"></a> <code>getline()、get()、ignore()</code></h2>
<ul>
<li><img src="/2021/05/12/cin%E7%9A%84%E8%A1%A5%E5%85%85/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Cgetline%E5%92%8Cget%EF%BC%8Cigrnore.png" alt="getline和get，igrnore"></li>
</ul>
<p><code>istream &amp; ignore(int = 1, int = EOF)</code></p>
<p>ignore读取指定的字符或读取到文件尾</p>
]]></content>
  </entry>
  <entry>
    <title>cout</title>
    <url>/2021/05/10/cout/</url>
    <content><![CDATA[<h1 id="cout"><a class="markdownIt-Anchor" href="#cout"></a> cout</h1>
<p><code>cout.precision()</code>其实是输出流cout的一个格式控制函数，也就是在<code>iostream</code>中的一个成员函数。<code>precision()</code>返回当前的浮点数的精度值，而<code>cout.precision(val)</code>其实就是在输出的时候设定输出值以新的浮点数精度值显示，即小数点后保留val位。</p>
]]></content>
      <categories>
        <category>编程语言</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>decltype关键字的坑</title>
    <url>/2021/05/12/decltype%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E5%9D%91/</url>
    <content><![CDATA[<h1 id="decltype关键字的坑"><a class="markdownIt-Anchor" href="#decltype关键字的坑"></a> decltype关键字的坑</h1>
<h2 id="a为变量"><a class="markdownIt-Anchor" href="#a为变量"></a> a为变量</h2>
<p>返回a的变量类型</p>
<h2 id="a为左值表达式注意"><a class="markdownIt-Anchor" href="#a为左值表达式注意"></a> a为左值表达式（注意）</h2>
<p>返回a的变量类型的引用</p>
<h2 id="a为右值表达式"><a class="markdownIt-Anchor" href="#a为右值表达式"></a> a为右值表达式</h2>
<p>返回a的变量类型</p>
<h2 id="代码示例"><a class="markdownIt-Anchor" href="#代码示例"></a> 代码示例</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i=<span class="number">42</span>,*p=&amp;i,&amp;r=i;</span><br><span class="line"><span class="keyword">decltype</span>(r+<span class="number">0</span>) b<span class="comment">//正确，r+0为右值表达式，b是int</span></span><br><span class="line"><span class="keyword">decltype</span>(r) b<span class="comment">//错误，r是变量，b是int&amp;，未初始化错误</span></span><br><span class="line"><span class="keyword">decltype</span>(*p) b<span class="comment">//错误，*p是左值表达式，b是int&amp;</span></span><br></pre></td></tr></table></figure>
<p><code>decltype(*p)</code>返回的是一个引用，<strong>严格来说<code>*p</code>返回的就是引用</strong>。</p>
<h2 id="错误示例"><a class="markdownIt-Anchor" href="#错误示例"></a> 错误示例</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">max5</span><span class="params">(T * a)</span> -&gt; <span class="title">decltype</span><span class="params">(a[<span class="number">0</span>])</span></span>;<span class="comment">//a[0]是一个左值，所以decltype(a[0])和decltype(*a)返回的是一个int&amp;</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x[<span class="number">5</span>] &#123;<span class="number">5</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">8</span>, <span class="number">9</span>&#125;;</span><br><span class="line">    <span class="keyword">double</span> y[<span class="number">5</span>] &#123;<span class="number">2.0</span>, <span class="number">3.6</span>, <span class="number">5.9</span>, <span class="number">1.5</span>, <span class="number">7.9</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="built_in">max5</span>(x);</span><br><span class="line">    cout &lt;&lt; a;</span><br><span class="line">    <span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">max5</span><span class="params">(T * a)</span> -&gt; <span class="title">decltype</span><span class="params">(a[<span class="number">0</span>])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    T max = a[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (max &lt; a[i])</span><br><span class="line">        &#123;</span><br><span class="line">            max = a[i];</span><br><span class="line">        &#125;       </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="改正后"><a class="markdownIt-Anchor" href="#改正后"></a> 改正后</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">max5</span><span class="params">(T * a)</span> -&gt; <span class="title">decltype</span><span class="params">(a[<span class="number">0</span>]+a[<span class="number">2</span>])</span></span>;<span class="comment">//a[0]+a[2]是一个右值表达式，decltype返回其类型</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x[<span class="number">5</span>] &#123;<span class="number">5</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">8</span>, <span class="number">9</span>&#125;;</span><br><span class="line">    <span class="keyword">double</span> y[<span class="number">5</span>] &#123;<span class="number">2.0</span>, <span class="number">3.6</span>, <span class="number">5.9</span>, <span class="number">1.5</span>, <span class="number">7.9</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="built_in">max5</span>(x);</span><br><span class="line">    cout &lt;&lt; a;</span><br><span class="line">    <span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">max5</span><span class="params">(T * a)</span> -&gt; <span class="title">decltype</span><span class="params">(a[<span class="number">0</span>]+a[<span class="number">2</span>])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    T max = a[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (max &lt; a[i])</span><br><span class="line">        &#123;</span><br><span class="line">            max = a[i];</span><br><span class="line">        &#125;       </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>图的最短路径dijksra算法</title>
    <url>/2021/05/16/dijksra/</url>
    <content><![CDATA[<p>dijksra也是贪婪算法的一种</p>
<span id="more"></span>
<h2 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h2>
<p>维护数组<code>path</code>、<code>dist</code>和集和<code>S</code>，其中<code>path</code>记录最短路径上每个顶点的前驱顶点（若有记录顶点编号，若无记录-1），<code>dist</code>记录到每个节点的最短路径长度（若有记录距离，若无记录∞），集和<code>S</code>记录已经确定最短路径的顶点。</p>
<p>每次都选取从当前顶点出发距离最近的顶点，并更新<code>dist</code>，<code>path</code></p>
<h2 id="tips"><a class="markdownIt-Anchor" href="#tips"></a> Tips</h2>
<p>dijksra适用于有向图，无向图和带权图，但是不适用于带<strong>负权值</strong>的带权图</p>
<h2 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dijsktra</span><span class="params">(Graph G, <span class="keyword">int</span> origin, <span class="keyword">int</span> target)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    set&lt;<span class="keyword">int</span>&gt; S &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="comment">//初始化dist, path</span></span><br><span class="line">    <span class="function">vector&lt;<span class="keyword">int</span>&gt; <span class="title">dist</span><span class="params">(SIZE, INT_MAX)</span>, <span class="title">path</span><span class="params">(SIZE, <span class="number">-1</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; SIZE; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(G[origin][i] != INT_MAX)</span><br><span class="line">        &#123;</span><br><span class="line">            dist[i] = G[origin][i];</span><br><span class="line">            path[i] = origin;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//记录父顶点</span></span><br><span class="line">    <span class="keyword">int</span> father = origin;</span><br><span class="line">    <span class="keyword">while</span>(S.<span class="built_in">size</span>() &lt; SIZE)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//找出从当前节点出发的最小路径长度，即dist的最小值</span></span><br><span class="line">        <span class="keyword">int</span> minPath = INT_MAX, minNode = <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; SIZE; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(S.<span class="built_in">find</span>(i) == S.<span class="built_in">end</span>() &amp;&amp; dist[i] &lt; minPath)</span><br><span class="line">            &#123;</span><br><span class="line">                minNode = i; </span><br><span class="line">                minPath = dist[i];</span><br><span class="line">            &#125;     </span><br><span class="line">        &#125;</span><br><span class="line">        father = minNode;</span><br><span class="line">        <span class="comment">//加入集和</span></span><br><span class="line">        S.<span class="built_in">insert</span>(minNode);</span><br><span class="line">        <span class="comment">//找到当前顶点的邻接顶点， 记录路径长度，并更新dist,path</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; SIZE; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(S.<span class="built_in">find</span>(i) == S.<span class="built_in">end</span>() &amp;&amp; G[minNode][i] != INT_MAX &amp;&amp; (dist[minNode] + G[minNode][i]) &lt; dist[i])</span><br><span class="line">            &#123;</span><br><span class="line">                dist[i] = dist[minNode] + G[minNode][i];</span><br><span class="line">                path[i] = minNode; </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;从&quot;</span> &lt;&lt; origin &lt;&lt; <span class="string">&quot;到&quot;</span> &lt;&lt; target &lt;&lt; <span class="string">&quot;的最短路径长度为: &quot;</span> &lt;&lt; dist[target] &lt;&lt; endl; </span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;最短带权路径为: &quot;</span>;</span><br><span class="line">    <span class="keyword">int</span> i = target;</span><br><span class="line">    <span class="keyword">while</span>(path[i] != <span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; i &lt;&lt; <span class="string">&quot;&lt;--&quot;</span>;</span><br><span class="line">        i = path[i];</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; origin;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>docker使用ipv6</title>
    <url>/2021/05/12/docker%E4%BD%BF%E7%94%A8ipv6/</url>
    <content><![CDATA[<h1 id="docker使用ipv6"><a class="markdownIt-Anchor" href="#docker使用ipv6"></a> docker使用ipv6</h1>
<p>开启ipv6首先按照官方文档修改/etc/docker/daemon.json</p>
<p>然后创建自己的docker网络</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker network create --driver bridge --ipam-driver default --subnet 172.80.80.0/24 --gateway 172.80.80.1 --ipv6 --subnet a:b:c:d::/64 --gateway a:b:c:d::1 mynet</span><br></pre></td></tr></table></figure>
<p>随后使用<code>--network network_name</code>参数使容器连接到这个网络</p>
<p><code>docker run -d -p 8080:8080 --network mynet --name STRING IMAGE</code></p>
]]></content>
  </entry>
  <entry>
    <title>图的最短路径floyd算法</title>
    <url>/2021/05/16/floyd/</url>
    <content><![CDATA[<p>floyd查找最短路径的核心就是中转点。</p>
<span id="more"></span>
<h2 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h2>
<ul>
<li>
<p>维护一个<code>A</code>矩阵记录，从<code>i</code>到<code>j</code>的最短路径。矩阵<code>path</code>记录，从<code>i</code>到<code>j</code>的中转点。</p>
</li>
<li>
<p>初始化<code>A</code>以V<sub>0</sub>为中转点，<code>path</code>值全部置为<code>-1</code></p>
</li>
<li>
<p>遍历所有顶点V<sub>i</sub>，如果其他点以V<sub>i</sub>为中转点的最短路径比原来的小，则更新最短路径。</p>
</li>
</ul>
<h2 id="tips"><a class="markdownIt-Anchor" href="#tips"></a> Tips</h2>
<p>floyd算法适用于带负权值的带权图，但是不是适用于<strong>带负权值的边组成回路</strong>的带权图</p>
<h2 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">showPath</span><span class="params">(Graph path, <span class="keyword">int</span> origin, <span class="keyword">int</span> target)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(path[origin][target] &gt; <span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">showPath</span>(path, origin, path[origin][target]);</span><br><span class="line">        <span class="built_in">showPath</span>(path, path[origin][target], target);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;--&gt;&quot;</span> &lt;&lt; target;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">floyd</span><span class="params">(Graph G, <span class="keyword">int</span> origin, <span class="keyword">int</span> target)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//A记录最短路径 path记录最短路径的中转点</span></span><br><span class="line">    Graph A = G, <span class="built_in">path</span>(SIZE, vector&lt;<span class="keyword">int</span>&gt;(SIZE, <span class="number">-1</span>));</span><br><span class="line">    <span class="comment">//递推产生矩阵</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; SIZE; k++) <span class="comment">//k中转点</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//检查矩阵中的所有值</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> row = <span class="number">0</span>; row &lt; SIZE; row++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> column = <span class="number">0</span>; column &lt; SIZE; column++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(A[row][k] == INT_MAX || A[k][column] == INT_MAX) </span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                <span class="keyword">if</span>(A[row][column] &gt; A[row][k] + A[k][column])</span><br><span class="line">                &#123;</span><br><span class="line">                    A[row][column] = A[row][k] + A[k][column];</span><br><span class="line">                    path[row][column] = k;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;最短路径长度为：&quot;</span> &lt;&lt; A[origin][target] &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;最短路径为: &quot;</span> &lt;&lt; origin;</span><br><span class="line">    <span class="built_in">showPath</span>(path, origin, target);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>iomanip头文件</title>
    <url>/2021/05/12/iomanip%E5%A4%B4%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="iomanip头文件"><a class="markdownIt-Anchor" href="#iomanip头文件"></a> <code>iomanip</code>头文件</h1>
<h2 id="setwint"><a class="markdownIt-Anchor" href="#setwint"></a> <code>setw(int)</code></h2>
<p>setw()接受一个整型控制一个指定字符宽度</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cout &lt;&lt; <span class="built_in">setw</span>(<span class="number">5</span>) &lt;&lt; <span class="number">1</span> &lt;&lt; endl;</span><br><span class="line">cout &lt;&lt; <span class="number">11111</span> &lt;&lt; endl;</span><br></pre></td></tr></table></figure>
<p><img src="/media/zero/%E5%AD%A6%E4%B9%A0/markdown/%E5%9B%BE%E5%BA%8A/%E7%BB%93%E6%9E%9C.png" alt="image-20200917003033478"></p>
<h2 id="setfillchar"><a class="markdownIt-Anchor" href="#setfillchar"></a> <code>setfill(char)</code></h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cout &lt;&lt; <span class="built_in">setw</span>(<span class="number">5</span>) &lt;&lt; <span class="built_in">setfill</span>(<span class="string">&#x27;#&#x27;</span>)&lt;&lt; <span class="number">1</span> &lt;&lt; endl;</span><br><span class="line">cout &lt;&lt; <span class="number">11111</span> &lt;&lt; endl;</span><br></pre></td></tr></table></figure>
<p>接受一个制定填充字符的char参数</p>
<p><img src="/media/zero/%E5%AD%A6%E4%B9%A0/markdown/%E5%9B%BE%E5%BA%8A/%E7%BB%93%E6%9E%9C2.png" alt="image-20200916163245906"></p>
<h2 id="setprecisionint"><a class="markdownIt-Anchor" href="#setprecisionint"></a> <code>setprecision(int)</code></h2>
<p>控制精度</p>
]]></content>
  </entry>
  <entry>
    <title>linux cut printf awk sed</title>
    <url>/2021/05/12/linux%20cut%20printf%20awk%20sed/</url>
    <content><![CDATA[<h1 id="字符串截取"><a class="markdownIt-Anchor" href="#字符串截取"></a> 字符串截取</h1>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cut [option] filename</span><br><span class="line">	-f 列号：提取第几列</span><br><span class="line">	-d <span class="string">&quot;分隔符&quot;</span>：按照指定分隔符分割列 <span class="string">&quot; &quot;</span>会识别空格</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span> <span class="string">&#x27;输出类型输出格式&#x27;</span> 输出内容</span><br><span class="line">		%ns：输出字符串，n代表输出几个字符</span><br><span class="line">		&amp;ni：输出整数，n是输出几个数字</span><br><span class="line">		&amp;m.nf：位数和小数点位数，输出m位数，n个小数，(m-n)个整数</span><br></pre></td></tr></table></figure>
<figure class="highlight awk"><table><tr><td class="code"><pre><span class="line">awk <span class="string">&#x27;条件1&#123;动作1&#125;条件2&#123;动作2&#125;······&#x27;</span> 文件名</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sed [option] <span class="string">&#x27;[action]&#x27;</span> 文件名</span><br><span class="line">	-n		 a\</span><br><span class="line">			c\</span><br><span class="line">			i\</span><br><span class="line">			d</span><br><span class="line">			p</span><br><span class="line">             s</span><br><span class="line">	-e</span><br><span class="line">	-i</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>linux环境变量</title>
    <url>/2021/05/12/linux%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<h1 id="linux环境变量"><a class="markdownIt-Anchor" href="#linux环境变量"></a> Linux环境变量</h1>
<h2 id="环境变量"><a class="markdownIt-Anchor" href="#环境变量"></a> 环境变量</h2>
<p>用户自定义变量只在当前的Shell中生效，而环境变量会在当前Shell和这个Shell的所有子Shell当中生效。如果把环境变量写入相应的配置文件，那么这个环境变量就会在所有的Shell中生效</p>
<h3 id="设置环境变量"><a class="markdownIt-Anchor" href="#设置环境变量"></a> 设置环境变量</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> name=value <span class="comment">#设置环境变量</span></span><br><span class="line">env <span class="comment">#查看环境变量</span></span><br><span class="line"><span class="built_in">unset</span> 变量名 <span class="comment">#删除变量</span></span><br></pre></td></tr></table></figure>
<p>使用<strong>set</strong>查看变量（不止环境变量），env只能看环境变量</p>
<p>使用<strong>pstree</strong>查看当前所在的shell</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">name=ali <span class="comment">#父级shell的本地变量</span></span><br><span class="line"><span class="built_in">export</span> sex=male <span class="comment">#所有shell的环境变量（全局变量）</span></span><br><span class="line">bash <span class="comment">#创建子shell</span></span><br><span class="line">age=18 <span class="comment">#子shell的本地变量</span></span><br></pre></td></tr></table></figure>
<p>在父级shell中只能看到name和sex，在子shell中只能看到age和sex</p>
<h3 id="path"><a class="markdownIt-Anchor" href="#path"></a> PATH</h3>
<p>PATH是系统命令查找的路径</p>
<p>PATH叠加</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=<span class="string">&quot;<span class="variable">$PATH</span>&quot;</span>:变量路径</span><br></pre></td></tr></table></figure>
<h2 id="环境变量配置文件"><a class="markdownIt-Anchor" href="#环境变量配置文件"></a> 环境变量配置文件</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#系统的环境变量配置文件</span></span><br><span class="line">/etc/profile</span><br><span class="line">/etc/profile.d/*.sh</span><br><span class="line">/etc/bashrc</span><br><span class="line"><span class="comment">#用户的环境变量配置文件</span></span><br><span class="line">~/.bash_profile  <span class="comment">#~代表home目录</span></span><br><span class="line">~/.bashrc</span><br></pre></td></tr></table></figure>
<p><strong>让配置文件生效</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> 配置文件 <span class="comment">#让配置文件生效</span></span><br><span class="line">或</span><br><span class="line">. 配置文件</span><br></pre></td></tr></table></figure>
<h2 id="环境变量文件调用顺序"><a class="markdownIt-Anchor" href="#环境变量文件调用顺序"></a> 环境变量文件调用顺序</h2>
<p><strong>登录用户的调用顺序</strong></p>
<p><img src="/2021/05/12/linux%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Clinux%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E8%B0%83%E7%94%A8%E9%A1%BA%E5%BA%8F.png" alt></p>
<p><code>/etc/bashrc</code>中的PATH设置是在no login shell 的情况下生效</p>
<h2 id="其他配置文件"><a class="markdownIt-Anchor" href="#其他配置文件"></a> 其他配置文件</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">~/.bash_logout <span class="comment">#注销时生效的环境变量配置文件 默认是空</span></span><br><span class="line">~bash_history  <span class="comment">#历史命令的保存文件，注销时才会将内存的信息写进去或者使用命令立刻让内存中的历史命令写入文件</span></span><br><span class="line">/etc/issue <span class="comment">#登录欢迎信息,本地生效</span></span><br><span class="line">/etc/issue.net <span class="comment">#远程终端登录欢迎信息，是否显示信息有/etc/ssh/sshd_config决定，加入Banner /etc/issue.net才能显示</span></span><br><span class="line">/etc/motd <span class="comment">#不管本地登录还是远程登录都可以显示此欢迎信息</span></span><br></pre></td></tr></table></figure>
<p>欢迎信息，只能在<strong>本地终端欢迎信息使用</strong></p>
<table>
<thead>
<tr>
<th>转义符</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>\d</code></td>
<td>显示当前系统日期</td>
</tr>
<tr>
<td><code>\s</code></td>
<td>显示操作系统名称</td>
</tr>
<tr>
<td><code>\l</code></td>
<td>显示登录的终端号</td>
</tr>
<tr>
<td><code>\m</code></td>
<td>显示硬件体系结构如i386、i686</td>
</tr>
<tr>
<td><code>\n</code></td>
<td>显示主机名</td>
</tr>
<tr>
<td><code>\o</code></td>
<td>显示域名</td>
</tr>
<tr>
<td><code>\r</code></td>
<td>显示内核版本</td>
</tr>
<tr>
<td><code>\t</code></td>
<td>显示当前系统时间</td>
</tr>
<tr>
<td><code>\u</code></td>
<td>显示当前登录用户的序列号</td>
</tr>
</tbody>
</table>
]]></content>
  </entry>
  <entry>
    <title>return</title>
    <url>/2021/05/12/return/</url>
    <content><![CDATA[<h1 id="return的打断"><a class="markdownIt-Anchor" href="#return的打断"></a> return的打断</h1>
<p>return代表着从当前函数返回到主函数main中，所以无论在函数中执行循环几次，只要遇见return就会停止循环返回主函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mismatch</span><span class="params">(<span class="keyword">const</span> T&amp; arr1, <span class="keyword">const</span> T&amp; arr2, <span class="keyword">int</span> total)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; total; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span>(arr1[i] != arr2[i])&#123;</span><br><span class="line">            <span class="keyword">return</span> i; <span class="comment">//只要循环中满足条件执行此语句，那么函数将会立即返回不会执行下面的语句</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>; <span class="comment">//如果循环中一直没有遇见return那么循环将执行完毕，执行循环后的下一句。</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>string</title>
    <url>/2021/05/12/string/</url>
    <content><![CDATA[<h1 id="string"><a class="markdownIt-Anchor" href="#string"></a> string</h1>
<table>
<thead>
<tr>
<th style="text-align:left">构造函数</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">string(const char *s)</td>
<td style="text-align:left">将string 对象初始化为s 指向的NBTS</td>
</tr>
<tr>
<td style="text-align:left">string (size_ type n , char c)</td>
<td style="text-align:left">创建一个包含n个元素的string对象，其中每个元素都别初始化为字符c</td>
</tr>
<tr>
<td style="text-align:left">string(const string &amp; str)</td>
<td style="text-align:left">将一个string对象初始化为string对象str</td>
</tr>
<tr>
<td style="text-align:left">string()</td>
<td style="text-align:left">默认构造函数</td>
</tr>
<tr>
<td style="text-align:left">string(const char* s,size_type n)</td>
<td style="text-align:left">将string对象初始化为s指向的NBTS的前n个字符，即使超过了NBTS结尾</td>
</tr>
<tr>
<td style="text-align:left">template<class iter> string(Iter begin, Iter end)</class></td>
<td style="text-align:left">将string 对象初始化为区间[begin, end)内的字符， 其中begin 和end 的行为就像指 针， 用千指定位置， 范围包括begin 在内， 但不包括end</td>
</tr>
<tr>
<td style="text-align:left">string(const string &amp; str, string size_type pos=0, size_type = npos)</td>
<td style="text-align:left">将一个string对象初始化为对象str中从位置pos开始到结尾的字符，或从位置pos开始的n个字符</td>
</tr>
<tr>
<td style="text-align:left">string(string &amp;&amp; str) noexcept</td>
<td style="text-align:left">这是C++11 新增的， 它将一个string 对象初始化为string 对象str, 并可能修改str（移动构造函数）</td>
</tr>
<tr>
<td style="text-align:left">string(initializer_list<char> il)</char></td>
<td style="text-align:left">这是C++11 新增的， 它将一个string 对象初始化为初始化列表il 中的字符</td>
</tr>
</tbody>
</table>
<h2 id="移动构造函数"><a class="markdownIt-Anchor" href="#移动构造函数"></a> 移动构造函数</h2>
<p><code>string(string &amp;&amp; str)</code></p>
<p>创建一个str的副本但与复制构造函数不同，他不保证str视为const</p>
<h2 id="stringinitializer_listchar-il"><a class="markdownIt-Anchor" href="#stringinitializer_listchar-il"></a> <code>string(initializer_list&lt;char&gt; il)</code></h2>
<p>它使得列表初始化语句用于string类</p>
<p><code>string a = &#123;'a', 'b', 'c'&#125;;</code></p>
<p><code>string a &#123;'a', 'b', 'c'&#125;;</code></p>
<p>合法</p>
<h2 id="string类输入"><a class="markdownIt-Anchor" href="#string类输入"></a> string类输入</h2>
<p>string 版本的 getline( ）函数从输入中读取字符， 并将其存储到目标 string 中，直到发生下列三种情况</p>
<ul>
<li>到达文件尾，在这种情况下，输入流的 eofbit 将被设置， 这意味着方法 fail( ）和 eot())都将返回true;</li>
<li>遇到分界字符（默认为\n)，在这种情况下， 将把分界字符从输入流中删除， 但不存储它；</li>
<li>读取的字符数达到最大允许值 (string::npos 和可供分配的内存字节数中较小的下， 将设置输入流的 failbit, 这意味着方法 fail() 将返回 true。</li>
</ul>
<h2 id="string类的方法"><a class="markdownIt-Anchor" href="#string类的方法"></a> string类的方法</h2>
<ul>
<li><code>size_type find(const string* str, size_type pos = 0)const</code></li>
</ul>
<p>从字符串的 pos 位置开始， 查找子字符串 str。如果找到， 则返回该子字符串首次出现时其首字符的索引； 否则， 返回 string : : npos</p>
<ul>
<li><code>size_type find(const char* str, size_type pos = 0)const</code></li>
</ul>
<p>从字符串的 pos 位置开始， 查找子字符串 s。如果找到， 则返回该子字符串首次出现时其首字符的索引；否则， 返回 string :: npos</p>
<ul>
<li><code>size_type find(const char* s, size_type pos = 0, size_type n)</code></li>
</ul>
<p>从字符串的 pos 位置开始， 查找 s 的前 n 个字符组成的子字符串。 如果找到， 则返回该子字符串首次出现时其首字符的索引； 否则， 返回 string : : npos</p>
<ul>
<li><code>size_type find(char ch, size_type pos = 0)const</code></li>
</ul>
<p>从字符串的 pos 位置开始， 查找字符 ch。如果找到， 则返回该字符首次出现的位置；否则， 返回 string : : npos</p>
<ul>
<li><code>refind()</code></li>
</ul>
<p>查找字符串或字符最后一次出现的位置</p>
<ul>
<li><code>find_first_of()</code></li>
</ul>
<p>查找参数中任何一个字符串首次出现的位置</p>
<ul>
<li><code>find_last_of()</code></li>
</ul>
<p>查找参数中任何一个字符串最后一次出现的位置</p>
<ul>
<li><code>find_first_not_of()</code></li>
</ul>
<p>查找第一个不包含在参数中的字符</p>
<ul>
<li><code>find_last_not_of()</code></li>
<li><code>capacity()</code></li>
</ul>
<p>返回当前分配给字符串的内存块的大小</p>
<ul>
<li><code>reserve()</code></li>
</ul>
<p>请求内存块的最小的长度</p>
<p>…</p>
<h2 id="智能指针模板类"><a class="markdownIt-Anchor" href="#智能指针模板类"></a> 智能指针模板类</h2>
<p><code>unique_ptr&lt;typename&gt; p(new typename)</code></p>
<p><code>shared_ptr&lt;typename&gt; p(new typename)</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">a</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string * b = <span class="keyword">new</span> <span class="built_in">string</span>(<span class="string">&quot;adw&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">bad</span>())</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">bad_name</span>();</span><br><span class="line">    <span class="keyword">delete</span> b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>常规指针遇到throw会停止，并不会执行throw后面的代码，执行站解退只会释放指针本身的内存而它指向的对象内存不会被释放，造成内存泄漏。</p>
<p>智能指针模板定义了类似指针的对象，他们是一个类模板，包含析构函数。所以当程序遇到throw的时候，执行栈解退的时候，自动调用析构函数，释放内存。</p>
]]></content>
  </entry>
  <entry>
    <title>string类的常用方法</title>
    <url>/2021/05/12/string%E7%B1%BB%E7%9A%84%E5%B8%B8%E7%94%A8%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<h1 id="string类的常用方法"><a class="markdownIt-Anchor" href="#string类的常用方法"></a> string类的常用方法</h1>
<h2 id="1-string转换为char"><a class="markdownIt-Anchor" href="#1-string转换为char"></a> 1 string转换为char*</h2>
<h3 id="11-使用c_str方法"><a class="markdownIt-Anchor" href="#11-使用c_str方法"></a> 1.1 使用<code>c_str()</code>方法</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string strOutput = <span class="string">&quot;Hello World&quot;</span>;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;[cout] strOutput is: &quot;</span> &lt;&lt; strOutput &lt;&lt; endl;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">// string 转换为 char*</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* pszOutput = strOutput.<span class="built_in">c_str</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;[printf] strOutput is: %s\n&quot;</span>, pszOutput);</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>
<p><code>cout</code>可直接输出<code>string</code>类的对象的内容；</p>
</li>
<li>
<p>使用<code>c_str()</code>方法转换<code>string</code>类型到<code>char*</code>类型时，需要为<code>char*</code>添加<code>const</code>关键字；</p>
</li>
<li>
<p><code>printf()</code>函数不能直接打印<code>string</code>类的对象的内容，可以通过将<code>string</code>转换为<code>char*</code>类型，再使用 <code>printf()</code>函数打印。</p>
</li>
</ul>
<h3 id="22-data方法与c_str方法"><a class="markdownIt-Anchor" href="#22-data方法与c_str方法"></a> 2.2 data()方法与c_str()方法</h3>
<p><code>data()</code>方法与<code>c_str()</code>方法相似，都返回<code>const char*</code>类型。两者区别和联系如下：</p>
<ul>
<li>在C++98版本中，c_str()返回 const char* 类型，返回的字符串会以空字符（null character）结尾；</li>
<li>在C++98版本中，data()返回 const char* 类型，返回的字符串不以空字符（null character）结尾；</li>
<li>在C++11版本中，c_str()与data()用法相同（Both string::data and string::c_str are synonyms and return the same value.）</li>
</ul>
<h2 id="2-计算string长度-string字符串比较"><a class="markdownIt-Anchor" href="#2-计算string长度-string字符串比较"></a> 2 计算string长度、string字符串比较</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> HELLOSTR <span class="meta-string">&quot;Hello World&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string strOutput = <span class="string">&quot;Hello World&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> nLen = strOutput.<span class="built_in">length</span>();</span><br><span class="line">     </span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;the length of strOutput is: &quot;</span> &lt;&lt; nLen &lt;&lt; endl;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">if</span> (<span class="number">0</span> == strOutput.<span class="built_in">compare</span>(HELLOSTR))</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;strOutput equal with macro HELLOSTR&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>
<p><code>string</code>类型可直接使用<code>length()</code>方法计算字符串长度，该方法计算结果为字符串的实际长度，如本例中<code>&quot;Hello World&quot;</code>字符串的长度为11；</p>
</li>
<li>
<p><code>string</code>类型可使用 <code>compare(const string&amp; str)</code>方法进行字符串比较。</p>
</li>
</ul>
<h2 id="3-string对象判空"><a class="markdownIt-Anchor" href="#3-string对象判空"></a> 3 string对象判空</h2>
<p>可使用<code>empty()</code>方法对string类型的对象进行判空，如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (str2.<span class="built_in">empty</span>())</span><br><span class="line">&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;str2 is empty.&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-char-char转换为string"><a class="markdownIt-Anchor" href="#4-char-char转换为string"></a> 4 char*、char[]转换为string</h2>
<p>将 <code>char*</code>、<code>char[]</code> 转换为 <code>string</code> 类型时，直接进行赋值操作，将 <code>char*</code>、<code>char[]</code>的变量赋值给<code>string</code>对象即可。</p>
<ul>
<li>ps：这里所说的“赋值”操作，实际上是将<code>char*</code>、<code>char[]</code> 定义的字符串的首地址赋值给 <code>string</code> 对象了。</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* pszName = <span class="string">&quot;liitdar&quot;</span>;</span><br><span class="line">    <span class="keyword">char</span> pszCamp[] = <span class="string">&quot;alliance&quot;</span>;</span><br><span class="line"></span><br><span class="line">    string strName;</span><br><span class="line">    string strCamp;</span><br><span class="line">     </span><br><span class="line">    strName = pszName;</span><br><span class="line">    strCamp = pszCamp;</span><br><span class="line">     </span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;strName is: &quot;</span> &lt;&lt; strName &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;strCamp is: &quot;</span> &lt;&lt; strCamp &lt;&lt; endl;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="5-string类的find方法"><a class="markdownIt-Anchor" href="#5-string类的find方法"></a> 5 string类的find方法</h2>
<p>使用string类的find方法，在字符串中检索自字符串是否存在。</p>
<h3 id="51-用法"><a class="markdownIt-Anchor" href="#51-用法"></a> 5.1 用法</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">size_t</span> <span class="title">find</span> <span class="params">(<span class="keyword">const</span> string&amp; str, <span class="keyword">size_t</span> pos = <span class="number">0</span>)</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">size_t</span> <span class="title">find</span> <span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* s, <span class="keyword">size_t</span> pos = <span class="number">0</span>)</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">size_t</span> <span class="title">find</span> <span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* s, <span class="keyword">size_t</span> pos, <span class="keyword">size_t</span> n)</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">size_t</span> <span class="title">find</span> <span class="params">(<span class="keyword">char</span> c, <span class="keyword">size_t</span> pos = <span class="number">0</span>)</span> <span class="keyword">const</span></span>;</span><br></pre></td></tr></table></figure>
<h3 id="52-返回值"><a class="markdownIt-Anchor" href="#52-返回值"></a> 5.2 返回值</h3>
<p>find函数返回值：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">The position of the first character of the first match. If no matches were found, the function returns string::npos.</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">size_t</span> is an <span class="keyword">unsigned</span> integral <span class="title">type</span> <span class="params">(the same as member type string::size_type)</span>.</span></span><br></pre></td></tr></table></figure>
<h3 id="53示例代码"><a class="markdownIt-Anchor" href="#53示例代码"></a> 5.3示例代码</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 待检索的字符串</span></span><br><span class="line">    string strOutput = <span class="string">&quot;|0|1|2|&quot;</span>;</span><br><span class="line">    <span class="comment">// 需要检索的子串</span></span><br><span class="line">    string strObj = <span class="string">&quot;|1|&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 子串位于字符串中的位置</span></span><br><span class="line">    <span class="keyword">size_t</span> nLoc = strOutput.<span class="built_in">find</span>(strObj);</span><br><span class="line">    <span class="comment">// 如果检索到子串在字符串中，则打印子串的位置</span></span><br><span class="line">    <span class="keyword">if</span> (nLoc != string::npos)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;nLoc is: &quot;</span> &lt;&lt; nLoc &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="6-string类的insert方法"><a class="markdownIt-Anchor" href="#6-string类的insert方法"></a> 6 string类的insert方法</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string strDemo = <span class="string">&quot;I am&quot;</span>;</span><br><span class="line"></span><br><span class="line">    strDemo.<span class="built_in">insert</span>(<span class="number">4</span>, <span class="string">&quot; good.&quot;</span>);</span><br><span class="line">     </span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;strDemo is: &quot;</span> &lt;&lt; strDemo &lt;&lt; endl; <span class="comment">// I am good</span></span><br><span class="line">     </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="7-int类型转为string类的方法"><a class="markdownIt-Anchor" href="#7-int类型转为string类的方法"></a> 7 int类型转为string类的方法</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 方法1</span></span><br><span class="line">    <span class="keyword">int</span> nNum1 = <span class="number">123</span>;</span><br><span class="line">    stringstream ss;</span><br><span class="line"></span><br><span class="line">    ss &lt;&lt; nNum1;</span><br><span class="line">    string strTest1 = ss.<span class="built_in">str</span>();</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;strTest1 is: &quot;</span> &lt;&lt; strTest1 &lt;&lt; endl;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    string strTest2;</span></span><br><span class="line"><span class="comment">    strTest2 &lt;&lt; ss;     // stringstream 未定义 &lt;&lt; 操作符，故此句报错</span></span><br><span class="line"><span class="comment">    cout &lt;&lt; &quot;strTest2 is: &quot; &lt;&lt; strTest2 &lt;&lt; endl;</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">     </span><br><span class="line">    string strTest3;</span><br><span class="line">    ss &gt;&gt; strTest3;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;strTest3 is: &quot;</span> &lt;&lt; strTest3 &lt;&lt; endl;</span><br><span class="line">     </span><br><span class="line">    <span class="comment">// 方法2</span></span><br><span class="line">    <span class="keyword">int</span> nNum2 = <span class="number">456</span>;</span><br><span class="line">    string strTest4;</span><br><span class="line">    strTest4 = <span class="built_in">to_string</span>(nNum2);    <span class="comment">// C++11 标准</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;strTest4 is: &quot;</span> &lt;&lt; strTest4 &lt;&lt; endl;</span><br><span class="line">     </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>this指针</title>
    <url>/2021/05/12/this%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<h1 id="this指针"><a class="markdownIt-Anchor" href="#this指针"></a> this指针</h1>
<p>在类中每一个成员函数都有一个this指针指向它调用对象的地址。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STUENT</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STUDENT</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	std::string name;</span><br><span class="line">    	<span class="keyword">double</span> length;</span><br><span class="line">    	<span class="keyword">double</span> weight;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="built_in">Student</span>(); <span class="comment">//构造函数</span></span><br><span class="line">    	<span class="built_in">Student</span>(std::string, <span class="keyword">double</span>, <span class="keyword">double</span>); <span class="comment">//构造函数</span></span><br><span class="line">    	~<span class="built_in">Student</span>(); <span class="comment">//析构函数</span></span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show_name</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show_length</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show_weight</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    	<span class="function"><span class="keyword">const</span> <span class="keyword">double</span> &amp; <span class="title">taller</span><span class="params">(<span class="keyword">const</span> Student &amp;)</span> <span class="keyword">const</span></span>; <span class="comment">//返回身高的引用</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//源代码</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line">Student::~<span class="built_in">Student</span>() <span class="comment">//析构函数需要定义</span></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Student::<span class="built_in">Student</span>(std::string n, <span class="keyword">double</span> l, <span class="keyword">double</span> w)</span><br><span class="line">&#123;</span><br><span class="line">    name = n;</span><br><span class="line">    length = l;</span><br><span class="line">    weight = w;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Student::show_name</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; name &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Student::show_length</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; length &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Student::show_weight</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; weight &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">const</span> <span class="keyword">double</span> &amp; <span class="title">Student::taller</span><span class="params">(<span class="keyword">const</span> Student &amp; s)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (s.length &gt; length)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;taller is &quot;</span> &lt;&lt; s.name &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; s.length &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> s.length;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (s.length &lt; length)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;taller is &quot;</span> &lt;&lt; name &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; length &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> (*<span class="keyword">this</span>).length; <span class="comment">//this指针指的是调用对象的地址</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (s.length == length)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;the same length\n&quot;</span>;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//main函数</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;test.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Student A:\n&quot;</span>;</span><br><span class="line">    Student A = &#123;<span class="string">&quot;小明&quot;</span>, <span class="number">1.75</span>, <span class="number">90</span>&#125;;</span><br><span class="line">    A.<span class="built_in">show_name</span>();</span><br><span class="line">    A.<span class="built_in">show_length</span>();</span><br><span class="line">    A.<span class="built_in">show_weight</span>();</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Student B:\n&quot;</span>;</span><br><span class="line">    Student B &#123;<span class="string">&quot;小王&quot;</span>, <span class="number">1.80</span>, <span class="number">80</span>&#125;;</span><br><span class="line">    B.<span class="built_in">show_name</span>();</span><br><span class="line">    B.<span class="built_in">show_length</span>();</span><br><span class="line">    B.<span class="built_in">show_weight</span>();</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;-----------------------------\n&quot;</span>;</span><br><span class="line">    B.<span class="built_in">taller</span>(A);<span class="comment">//在这里this指的是B对象的地址</span></span><br><span class="line">    A.<span class="built_in">taller</span>(B);<span class="comment">//在这里this指的是A对象的地址</span></span><br><span class="line">    <span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>typename和class的区别</title>
    <url>/2021/05/12/typename%E5%92%8Cclass%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h1 id="typename和class的区别"><a class="markdownIt-Anchor" href="#typename和class的区别"></a> typename和class的区别</h1>
<h2 id="作为定义模板时功能是一样的"><a class="markdownIt-Anchor" href="#作为定义模板时功能是一样的"></a> 作为定义模板时功能是一样的</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span>;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;;</span><br></pre></td></tr></table></figure>
<h2 id="但是如果一个类含有嵌套类时"><a class="markdownIt-Anchor" href="#但是如果一个类含有嵌套类时"></a> 但是如果一个类含有嵌套类时</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">        &#123;</span>....&#125;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">c</span><span class="params">(A::B a)</span></span>&#123;&#125; <span class="comment">//错误编译器无法识别A::B是A类的数据成员还是嵌套类的类型，可能B是在A类中定义的整形，或者字符类型或者其他。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">        &#123;</span>....&#125;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">c</span><span class="params">(<span class="keyword">typename</span> A::B a)</span></span>&#123;&#125; <span class="comment">//typename告诉编译器A::B是类型而不是数据成员（A类中嵌套类的类型）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>using声明和using编译指令</title>
    <url>/2021/05/12/using%E5%A3%B0%E6%98%8E%E5%92%8Cusing%E7%BC%96%E8%AF%91%E6%8C%87%E4%BB%A4/</url>
    <content><![CDATA[<h1 id="using声明和using编译指令"><a class="markdownIt-Anchor" href="#using声明和using编译指令"></a> using声明和using编译指令</h1>
<h2 id="using声明"><a class="markdownIt-Anchor" href="#using声明"></a> using声明</h2>
<p>在代码中使用using声明相当于定义比变量</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> n1</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> n1::x;<span class="comment">//这里相当于int x = 5;</span></span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;<span class="comment">//出错，双定义</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="using编译指令"><a class="markdownIt-Anchor" href="#using编译指令"></a> using编译指令</h2>
<p>在代码块中使用using编译指令，则它其中的变量是全局的，<strong>但作用域是根据using编译指令的位置确定。</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> n1</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="keyword">namespace</span> n1;</span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;<span class="comment">//正确，局部变量覆盖全局变量</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="注意using编译指令的作用域"><a class="markdownIt-Anchor" href="#注意using编译指令的作用域"></a> 注意using编译指令的作用域</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> n1</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> n1;<span class="comment">//作用域是整个文件</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;<span class="comment">//正确，局部变量覆盖全局变量</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> n1</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="keyword">namespace</span> n1;<span class="comment">//作用域是main()函数</span></span><br><span class="line">    <span class="keyword">int</span> x = <span class="number">5</span>;<span class="comment">//正确，局部变量覆盖全局变量</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>vscode快捷键</title>
    <url>/2021/05/12/vscode%E5%BF%AB%E6%8D%B7%E9%94%AE/</url>
    <content><![CDATA[<h1 id="vscode快捷键"><a class="markdownIt-Anchor" href="#vscode快捷键"></a> vscode快捷键</h1>
<p>所有快捷键设置：文件→首选项→键盘快捷方式</p>
<ol>
<li>在当前行中间换行到下一行：ctrl+enter</li>
<li>当光标点击到某一行时，默认选中全行，可以直接复制剪切</li>
<li>直接删除某一行：shift+delete或者ctrl+shift+k</li>
<li>多行光标选择：alt+鼠标左键</li>
<li>自动生成html结构和meta声明：首先输入“!”，然后按tab键</li>
<li>标签自动补全：tab键</li>
<li>折叠所有代码：ctrl+k、ctrl+0</li>
<li>拆分编辑器：ctrl+\</li>
<li>跳转行号：ctrl+G</li>
<li>添加函数注释：在函数上方输入“/**”，然后点击enter</li>
<li>格式化：alt+shift+f</li>
<li>注释：ctrl+/</li>
<li>全部保存：ctrl+k, 然后只按s一个键</li>
<li>向上移动一行：alt+↑</li>
<li>向下移动一行：alt+↓</li>
<li>向上复制一行：alt+shift+↑</li>
<li>向下复制一行：alt+shift+↓</li>
<li>查找：ctrl+F</li>
<li>替换：ctrl+H</li>
<li>文件中查找：ctrl+shift+f</li>
<li>文件中替换：ctrl+shift+h</li>
<li>转到定义：F12</li>
<li>转到实现：ctrl+F12</li>
<li>打开文件夹：ctrl+k, ctrl+o</li>
<li>关闭文件夹：ctrl+k, 然后单按一个f</li>
<li>选中一段代码，通过“ctrl+[”可以左移，“ctrl+]”可以右移</li>
</ol>
<h3 id="编辑器与窗口管理"><a class="markdownIt-Anchor" href="#编辑器与窗口管理"></a> 编辑器与窗口管理</h3>
<ol>
<li>打开一个新窗口： <code>Ctrl+Shift+N</code></li>
<li>关闭窗口： <code>Ctrl+Shift+W</code></li>
<li>同时打开多个编辑器（查看多个文件）</li>
<li>新建文件 <code>Ctrl+N</code></li>
<li>文件之间切换 <code>Ctrl+Tab</code></li>
<li>切出一个新的编辑器（最多 <code>3</code> 个） <code>Ctrl+\</code>，也可以按住 <code>Ctrl</code> 鼠标点击 <code>Explorer</code> 里的文件名</li>
<li>左中右 <code>3</code> 个编辑器的快捷键 <code>Ctrl+1</code> <code>Ctrl+2</code> <code>Ctrl+3</code></li>
<li><code>3</code> 个编辑器之间循环切换 <code>Ctrl+</code></li>
<li>编辑器换位置， <code>Ctrl+k</code>然后按 <code>Left</code>或 <code>Right</code></li>
</ol>
<h3 id="代码编辑"><a class="markdownIt-Anchor" href="#代码编辑"></a> 代码编辑</h3>
<h4 id="格式调整"><a class="markdownIt-Anchor" href="#格式调整"></a> 格式调整</h4>
<ol>
<li>代码行缩进 <code>Ctrl+[</code> 、 <code>Ctrl+]</code></li>
<li><code>Ctrl+C</code> 、 <code>Ctrl+V</code> 复制或剪切当前行/当前选中内容</li>
<li>代码格式化： <code>Shift+Alt+F</code>，或 <code>Ctrl+Shift+P</code> 后输入 <code>format code</code></li>
<li>上下移动一行： <code>Alt+Up</code> 或 <code>Alt+Down</code></li>
<li>向上向下复制一行： <code>Shift+Alt+Up</code> 或 <code>Shift+Alt+Down</code></li>
<li>在当前行下边插入一行 <code>Ctrl+Enter</code></li>
<li>在当前行上方插入一行 <code>Ctrl+Shift+Enter</code></li>
</ol>
<h4 id="光标相关"><a class="markdownIt-Anchor" href="#光标相关"></a> 光标相关</h4>
<ol>
<li>移动到行首： <code>Home</code></li>
<li>移动到行尾： <code>End</code></li>
<li>移动到文件结尾： <code>Ctrl+End</code></li>
<li>移动到文件开头： <code>Ctrl+Home</code></li>
<li>移动到定义处： <code>F12</code></li>
<li>定义处缩略图：只看一眼而不跳转过去 <code>Alt+F12</code></li>
<li>移动到后半个括号： <code>Ctrl+Shift+]</code></li>
<li>选择从光标到行尾： <code>Shift+End</code></li>
<li>选择从行首到光标处： <code>Shift+Home</code></li>
<li>删除光标右侧的所有字： <code>Ctrl+Delete</code></li>
<li>扩展/缩小选取范围： <code>Shift+Alt+Left</code> 和 <code>Shift+Alt+Right</code></li>
<li>多行编辑(列编辑)：<code>Alt+Shift+鼠标左键</code>，<code>Ctrl+Alt+Down/Up</code></li>
<li>同时选中所有匹配： <code>Ctrl+Shift+L</code></li>
<li><code>Ctrl+D</code> 下一个匹配的也被选中 (在 sublime 中是删除当前行，后面自定义快键键中，设置与 <code>Ctrl+Shift+K</code> 互换了)</li>
<li>回退上一个光标操作： <code>Ctrl+U</code></li>
</ol>
<h4 id="重构代码"><a class="markdownIt-Anchor" href="#重构代码"></a> 重构代码</h4>
<ol>
<li>找到所有的引用： <code>Shift+F12</code></li>
<li>同时修改本文件中所有匹配的： <code>Ctrl+F12</code></li>
<li>重命名：比如要修改一个方法名，可以选中后按 <code>F2</code>，输入新的名字，回车，会发现所有的文件都修改了</li>
<li>跳转到下一个 <code>Error</code> 或 <code>Warning</code>：当有多个错误时可以按 <code>F8</code> 逐个跳转</li>
<li>查看 <code>diff</code>： 在 <code>explorer</code> 里选择文件右键 <code>Set file to compare</code>，然后需要对比的文件上右键选择 <code>Compare with file_name_you_chose</code></li>
</ol>
<h4 id="查找替换"><a class="markdownIt-Anchor" href="#查找替换"></a> 查找替换</h4>
<ol>
<li>查找 <code>Ctrl+F</code></li>
<li>查找替换 <code>Ctrl+H</code></li>
<li>整个文件夹中查找 <code>Ctrl+Shift+F</code></li>
</ol>
<h3 id="显示相关"><a class="markdownIt-Anchor" href="#显示相关"></a> 显示相关</h3>
<ol>
<li>全屏：<code>F11</code></li>
<li>zoomIn/zoomOut：<code>Ctrl +/-</code></li>
<li>侧边栏显/隐：<code>Ctrl+B</code></li>
<li>显示资源管理器 <code>Ctrl+Shift+E</code></li>
<li>显示搜索 <code>Ctrl+Shift+F</code></li>
<li>显示 Git <code>Ctrl+Shift+G</code></li>
<li>显示 Debug <code>Ctrl+Shift+D</code></li>
<li>显示 Output <code>Ctrl+Shift+U</code></li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>一个骚操作</title>
    <url>/2021/05/12/%E4%B8%80%E4%B8%AA%E9%AA%9A%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h1 id="一个骚操作"><a class="markdownIt-Anchor" href="#一个骚操作"></a> 一个骚操作</h1>
<p>权限不够的时候，可以随便找一个安装程序，用<strong>管理员身份启动</strong>之后在选择安装地址的时候，对权限不够的盘或者文件进行删除。</p>
<p>要删除Ubuntu系统下的EFI分区，有各种软件和方法，这里推荐直接用windows下的diskpart来删除，省得安装第三方流氓软件。</p>
<p>首先用管理员权限打开cmd，输入【diskpart】，利用【list disk】查询磁盘信息。</p>
<p>这里我们Ubuntu装在磁盘1中，所以选择磁盘1【select disk 1】，然后查看磁盘1下所有分区信息【list partition】，可以看到我们Ubuntu的EFI分区为488MB，根据分区大小这里选择分区4【select partition 4】。然后删除它【delete partition override】。如下图所示：</p>
<p><img src="/2021/05/12/%E4%B8%80%E4%B8%AA%E9%AA%9A%E6%93%8D%E4%BD%9C/E:%5Cmarkdown%5Cimages%5Cimgs%5C20171126113123527.png" alt></p>
<p>然后电脑就会多出一个p盘，但是权限不够无法修改。这是就要用骚操作。</p>
<p>随便找一个安装程序，用<strong>管理员身份启动</strong>之后在选择安装地址的时候，对权限不够的盘或者文件进行删除。</p>
]]></content>
  </entry>
  <entry>
    <title>关于delete的执行过程和delete后应该做的事情</title>
    <url>/2021/05/12/%E5%85%B3%E4%BA%8Edelete%E7%9A%84%E6%89%A7%E8%A1%8C%E8%BF%87%E7%A8%8B%E5%92%8Cdelete%E5%90%8E%E5%BA%94%E8%AF%A5%E5%81%9A%E7%9A%84%E4%BA%8B%E6%83%85/</url>
    <content><![CDATA[<h1 id="关于new后delete的执行过程和delete后应该做的事情"><a class="markdownIt-Anchor" href="#关于new后delete的执行过程和delete后应该做的事情"></a> 关于new后delete的执行过程和delete后应该做的事情</h1>
<h2 id="delete的机理"><a class="markdownIt-Anchor" href="#delete的机理"></a> delete的机理</h2>
<p><code>delete object</code></p>
<p>会调用object的析构函数</p>
<h2 id="delete后应该做的事"><a class="markdownIt-Anchor" href="#delete后应该做的事"></a> delete后应该做的事</h2>
<p>创建指针本身就会占用内存</p>
<p>delete只是释放了指针指向对象的内存，而它本身还在。</p>
<p>这个指针也就成了悬空指针。</p>
<p><strong>所以</strong></p>
<p>delete后应该把这个指针本身设置为nulllptr、</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>　* p = <span class="keyword">new</span> <span class="built_in"><span class="keyword">int</span></span>(<span class="number">4</span>);</span><br><span class="line"><span class="keyword">delete</span> p;</span><br><span class="line">p = <span class="literal">nullptr</span>;</span><br></pre></td></tr></table></figure>
<h2 id="但是在类中的情况"><a class="markdownIt-Anchor" href="#但是在类中的情况"></a> 但是在类中的情况</h2>
<p>类中执行delete一般是在析构函数中执行而且是最后一段代码，执行析构函数就代表着退出了作用域，所以一般可以不设置nullptr</p>
]]></content>
  </entry>
  <entry>
    <title>关于new</title>
    <url>/2021/05/12/%E5%85%B3%E4%BA%8Enew/</url>
    <content><![CDATA[<h1 id="关于new"><a class="markdownIt-Anchor" href="#关于new"></a> 关于<code>new</code></h1>
<p><code>new</code>分两种一种是常规<code>new</code>，另一种是定位<code>new</code>。</p>
<h2 id="常规new"><a class="markdownIt-Anchor" href="#常规new"></a> 常规<code>new</code></h2>
<h3 id="常规new的创建"><a class="markdownIt-Anchor" href="#常规new的创建"></a> 常规<code>new</code>的创建</h3>
<p><code>new</code>返回的是地址。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> * p1 = <span class="keyword">new</span> <span class="keyword">int</span>;<span class="comment">//给int分配空间</span></span><br><span class="line"><span class="keyword">char</span> * p2 = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">20</span>];<span class="comment">//给char数组分配空间</span></span><br></pre></td></tr></table></figure>
<h3 id="常规new的释放"><a class="markdownIt-Anchor" href="#常规new的释放"></a> 常规new的释放</h3>
<p>常规new的释放是用<code>delete</code>，<code>delete</code>释放空间是有两种格式</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> * p1 = <span class="keyword">new</span> <span class="keyword">int</span>;<span class="comment">//给int分配空间</span></span><br><span class="line"><span class="keyword">delete</span> p1;</span><br><span class="line"><span class="keyword">char</span> * p2 = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">20</span>];<span class="comment">//给char数组分配空间</span></span><br><span class="line"><span class="keyword">delete</span> [] p2;</span><br></pre></td></tr></table></figure>
<h3 id="常规new的内存"><a class="markdownIt-Anchor" href="#常规new的内存"></a> 常规<code>new</code>的内存</h3>
<p>常规<code>new</code>是在堆(heap)中存放，用<code>delete</code>释放。</p>
<h2 id="定位new"><a class="markdownIt-Anchor" href="#定位new"></a> 定位<code>new</code></h2>
<h3 id="定位new的创建"><a class="markdownIt-Anchor" href="#定位new的创建"></a> 定位<code>new</code>的创建</h3>
<h3 id="定位new需要用到new标准库"><a class="markdownIt-Anchor" href="#定位new需要用到new标准库"></a> 定位new需要用到new标准库</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;new&gt;</span></span></span><br><span class="line"><span class="keyword">char</span> buffer[<span class="number">50</span>];</span><br><span class="line"><span class="keyword">double</span> * p1 = <span class="built_in"><span class="keyword">new</span></span>(buffer) <span class="keyword">double</span>;</span><br><span class="line"><span class="keyword">double</span> * p2 = <span class="built_in"><span class="keyword">new</span></span>(buffer) <span class="keyword">double</span>[<span class="number">10</span>];</span><br></pre></td></tr></table></figure>
<p>定位<code>new</code>的内存</p>
<p>定位<code>new</code>的内存实在指定的内存中存储，上例是储存在<code>buffer</code>的内存中。</p>
<h2 id="两者的不同"><a class="markdownIt-Anchor" href="#两者的不同"></a> 两者的不同</h2>
<ul>
<li>
<p><code>delete</code>只能用于<strong>指向常规<code>new</code>运算符分配的堆内存</strong>，定位<code>new</code>在<code>delete</code>的管辖范围外</p>
</li>
<li>
<p>常规<code>new</code>存放的内存是堆，定位<code>new</code>将内存放在指定的地方</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>关于返回值</title>
    <url>/2021/05/12/%E5%85%B3%E4%BA%8E%E8%BF%94%E5%9B%9E%E5%80%BC/</url>
    <content><![CDATA[<h1 id="关于返回值"><a class="markdownIt-Anchor" href="#关于返回值"></a> 关于返回值</h1>
<h1 id="1-返回类型对象"><a class="markdownIt-Anchor" href="#1-返回类型对象"></a> 1 返回类型对象</h1>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">a</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然b是一个局部变量，但仍可返回。<strong>应为是返回int类型，所以会调用复制，把b复制一个</strong></p>
<h1 id="2返回类型引用"><a class="markdownIt-Anchor" href="#2返回类型引用"></a> 2返回类型引用</h1>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span>&amp; <span class="title">a</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">return</span> b; <span class="comment">//错误</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>b是一个局部变量，返回引用还是返回b自己，但b会在函数执行完毕被释放。<strong>返回引用时不会复制</strong>。</p>
]]></content>
  </entry>
  <entry>
    <title>函数对象</title>
    <url>/2021/05/12/%E5%87%BD%E6%95%B0%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="functional的函数符"><a class="markdownIt-Anchor" href="#functional的函数符"></a> <code>functional</code>的函数符</h1>
<p><img src="/2021/05/12/%E5%87%BD%E6%95%B0%E5%AF%B9%E8%B1%A1/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E9%A2%84%E5%AE%9A%E7%9A%84%E5%87%BD%E6%95%B0%E7%AC%A6.png" alt="预定的函数符"></p>
]]></content>
  </entry>
  <entry>
    <title>函数</title>
    <url>/2021/05/12/%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<hr>
<h2 id="title-你好世界date-2021-05-10-114640tagsc"><a class="markdownIt-Anchor" href="#title-你好世界date-2021-05-10-114640tagsc"></a> title: 你好世界<br>
date: 2021-05-10 11:46:40<br>
tags:C++</h2>
<h1 id="1-函数"><a class="markdownIt-Anchor" href="#1-函数"></a> 1 函数</h1>
<h2 id="11函数声明"><a class="markdownIt-Anchor" href="#11函数声明"></a> 1.1函数声明</h2>
<h3 id="111-格式"><a class="markdownIt-Anchor" href="#111-格式"></a> 1.1.1 格式</h3>
<p>类型+函数名（*<em>指针</em>）+（参数）</p>
<h3 id="112-函数原型"><a class="markdownIt-Anchor" href="#112-函数原型"></a> 1.1.2 函数原型</h3>
<p><code>typeName function(parameterType);</code></p>
<p>函数原型需要放在<code>main()</code>函数前面，结构前面。</p>
<h3 id="113-函数定义"><a class="markdownIt-Anchor" href="#113-函数定义"></a> 1.1.3 函数定义</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">typeName <span class="title">function</span><span class="params">(parameterType)</span> <span class="comment">//参数可以传递指针、结构地址等</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="12-函数使用"><a class="markdownIt-Anchor" href="#12-函数使用"></a> 1.2               .函数使用</h2>
<h3 id="121-参数"><a class="markdownIt-Anchor" href="#121-参数"></a> 1.2.1 参数</h3>
<h4 id="按值传递"><a class="markdownIt-Anchor" href="#按值传递"></a> 按值传递</h4>
<p>传入数值。</p>
<h4 id="按地址传递"><a class="markdownIt-Anchor" href="#按地址传递"></a> 按地址传递</h4>
<p>传入地址。</p>
<h4 id="两种方法的利弊"><a class="markdownIt-Anchor" href="#两种方法的利弊"></a> 两种方法的利弊</h4>
<p><strong>按值传递自动保护原来的数据但是会需要多占用内存和时间，而按地址传递虽然快但是不能保护原数据。</strong></p>
<ul>
<li>按值传递：保护性好，占用更多资源，速度慢</li>
<li>按地址传递：保护性差，占用少，速度快</li>
</ul>
<h3 id="122-返回"><a class="markdownIt-Anchor" href="#122-返回"></a> 1.2.2 返回</h3>
<h4 id="返回类型"><a class="markdownIt-Anchor" href="#返回类型"></a> 返回类型</h4>
<p>可以返回<strong>指针</strong>也可以返回<strong>数值</strong>。</p>
<h3 id="123-调用"><a class="markdownIt-Anchor" href="#123-调用"></a> 1.2.3 调用</h3>
<h5 id="普通调用"><a class="markdownIt-Anchor" href="#普通调用"></a> 普通调用</h5>
<p>直接在<code>main()</code>函数中使用函数名+()的方式调用。</p>
<h5 id="递归"><a class="markdownIt-Anchor" href="#递归"></a> 递归</h5>
<p>在函数内部调用自身，需要设置终止条件，通常用<code>if else</code>来实现。</p>
<h2 id="13-函数指针"><a class="markdownIt-Anchor" href="#13-函数指针"></a> 1.3 函数指针</h2>
<h3 id="131-格式"><a class="markdownIt-Anchor" href="#131-格式"></a> 1.3.1 格式</h3>
<p><code>typeName (*pointName)(parameterType)</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">pam</span><span class="params">(<span class="keyword">int</span>)</span></span>; <span class="comment">//函数原型</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> *<span class="title">pf</span><span class="params">(<span class="keyword">int</span>)</span> <span class="comment">//返回doule类型指针的函数</span></span></span><br><span class="line"><span class="function"><span class="title">double</span> <span class="params">(*pf)</span><span class="params">(<span class="keyword">int</span>)</span> <span class="comment">//指向函数的指针</span></span></span><br><span class="line"><span class="function">pf </span>= pam <span class="comment">//函数名是地址</span></span><br><span class="line"><span class="built_in"><span class="keyword">double</span></span> (*p[<span class="number">3</span>])(<span class="keyword">int</span>); <span class="comment">//声明一个指针数组，注意（）。括号的优先级比*高</span></span><br></pre></td></tr></table></figure>
<h3 id="132-函数指针的调用"><a class="markdownIt-Anchor" href="#132-函数指针的调用"></a> 1.3.2 函数指针的调用</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">pam</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">es</span><span class="params">(<span class="keyword">int</span>, <span class="keyword">double</span> (*p)(<span class="keyword">int</span>))</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in"><span class="keyword">double</span></span> (*p)(<span class="keyword">int</span>);</span><br><span class="line">    <span class="built_in">es</span>(<span class="number">6</span>, pam);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">pam</span><span class="params">(<span class="keyword">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;num = &quot;</span> &lt;&lt; a &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">es</span><span class="params">(<span class="keyword">int</span> b, <span class="keyword">double</span> (*p)(<span class="keyword">int</span>))</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    (*p)(<span class="number">5</span>); <span class="comment">//这种调用方法很好理解</span></span><br><span class="line">    <span class="built_in">p</span>(<span class="number">5</span>); <span class="comment">//C++也允许直接用指针名字加（）调用函数，不同流派的矛盾而已</span></span><br><span class="line">    <span class="built_in">pam</span>(<span class="number">5</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="2-内联函数"><a class="markdownIt-Anchor" href="#2-内联函数"></a> 2 内联函数</h1>
<h2 id="21-声明"><a class="markdownIt-Anchor" href="#21-声明"></a> 2.1 声明</h2>
<ul>
<li>在函数声明前加<code>inline</code></li>
<li>在函数定义前加<code>inline</code></li>
<li>通常是把定义写在函数原型后面</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">double</span> <span class="title">square</span><span class="params">(<span class="keyword">double</span> x)</span> </span>&#123;<span class="keyword">return</span> x * x;&#125;</span><br></pre></td></tr></table></figure>
<h2 id="22-使用原则"><a class="markdownIt-Anchor" href="#22-使用原则"></a> 2.2 使用原则</h2>
<ul>
<li>代码执行时间比调用函数时间少</li>
<li>代码经常被使用</li>
</ul>
<h1 id="3-引用变量"><a class="markdownIt-Anchor" href="#3-引用变量"></a> 3 引用变量</h1>
<h2 id="31-创建引用变量"><a class="markdownIt-Anchor" href="#31-创建引用变量"></a> 3.1 创建引用变量</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> rats;</span><br><span class="line"><span class="keyword">int</span> &amp; radents = rats;<span class="comment">//实际是int * const pr = &amp;rats</span></span><br></pre></td></tr></table></figure>
<p>其中<code>&amp;</code>不是地址运算符，<code>int &amp;</code>是指向<code>int</code>的引用。<code>rats</code>和<code>radents</code>拥有相同的内存地址，对其中一个修改另一个也会变。</p>
<h2 id="32-使用引用变量左值引用"><a class="markdownIt-Anchor" href="#32-使用引用变量左值引用"></a> 3.2 使用引用变量(左值引用)</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">recube</span>(x);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">recube</span><span class="params">(<span class="keyword">double</span> &amp; ra)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ra;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="33-临时变量"><a class="markdownIt-Anchor" href="#33-临时变量"></a> 3.3 临时变量</h2>
<p>如果实参与引用参数不匹配，C++将形成临时变量(仅当参数为<code>const</code>时才允许)，以下情况将创建临时变量</p>
<ul>
<li>实参的类型正确，但不是左值</li>
<li>实参的类型不正确，但可以转化为正确的类型</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">refcube</span><span class="params">(<span class="keyword">const</span> <span class="keyword">double</span> &amp; ra)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ra * ra * ra;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">long</span> edge = <span class="number">5L</span>;</span><br><span class="line"><span class="keyword">double</span> side = <span class="number">3.0</span>;</span><br><span class="line"><span class="keyword">double</span> c1 = <span class="built_in">refcube</span>(edge);</span><br><span class="line"><span class="keyword">double</span> c2 = <span class="built_in">refcube</span>(side + <span class="number">7.0</span>);</span><br></pre></td></tr></table></figure>
<p><code>edge</code>的类型和引用参数不匹配，<code>side + 7.0</code>类型正确但没有名称，此时编译器将生成一个临时的匿名变量，并让<code>ra</code>指向它。而<strong>临时变量仅在函数调用时存在，此后编译器将随意对它删除</strong></p>
<p><strong>所以:应尽可能使用<code>const</code></strong></p>
<ul>
<li>使用<code>const</code>可以避免无意中修改数据的编程错误</li>
<li>使用<code>const</code>使函数能够处理<code>const</code>和非<code>const</code>实参，否则将只能接受非<code>const</code>数据</li>
<li>使用<code>const</code>引用使函数能够正确生成并使用临时变量</li>
</ul>
<h2 id="34-结构引用"><a class="markdownIt-Anchor" href="#34-结构引用"></a> 3.4 结构引用</h2>
<p>结构引用跟指针引用很相似</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="keyword">int</span> level;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    student Li &#123;<span class="string">&quot;li&quot;</span>, <span class="number">20</span>&#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">show_name</span><span class="params">(Li)</span></span></span><br><span class="line"><span class="function">&#125;</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">show_name</span> <span class="params">(stduent &amp; rt)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; rt.name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">student &amp; <span class="title">change</span><span class="params">(stdent &amp; rp)</span><span class="comment">// 返回引用</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    rp.level += <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">return</span> rp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="返回引用时需注意的问题"><a class="markdownIt-Anchor" href="#返回引用时需注意的问题"></a> 返回引用时需注意的问题</h3>
<p>返回的引用不能是临时引用，否则在调用函数后临时引用将会被删除，会出现引用不存在的情况。</p>
<p><strong>注意应避免以下情况</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">student &amp; <span class="title">clone</span><span class="params">(student &amp; ft)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    stduent newone;</span><br><span class="line">    newone = student;</span><br><span class="line">    <span class="keyword">return</span> newone;<span class="comment">//在调用函数后newone将不存在</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>解决方案：</p>
<ul>
<li>直接用形参  <code>return ft</code></li>
<li>创建一个指向形参的指针 <code>student * p; *p = ft;</code></li>
<li>用new分配空间</li>
</ul>
<h2 id="35-类对象引用"><a class="markdownIt-Anchor" href="#35-类对象引用"></a> 3.5 类对象引用</h2>
<p>与结构相似</p>
<h3 id="对象-继承的引用"><a class="markdownIt-Anchor" href="#对象-继承的引用"></a> 对象、继承的引用</h3>
<p>可以创建一个基类的引用，该引用可以接受基类对象也可以接受其派生类的对象。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//ofstream是ostream的派生类</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ofstream fout;</span><br><span class="line">    <span class="built_in">f</span>(fout); <span class="comment">//fout是oftream类的对象，os指向fout</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">f</span>(cout); <span class="comment">//cout是ostream类的对象，os指向cout</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(ostream &amp; os)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="36-什么时候用引用变量"><a class="markdownIt-Anchor" href="#36-什么时候用引用变量"></a> 3.6 什么时候用引用变量</h2>
<p>使用引用参数的主要原因有两个。</p>
<ul>
<li>程序员能够修改调用函数中的数据对象。</li>
<li>通过传递引用而不是整个数据对象， 可以提高程序的运行速度。</li>
</ul>
<p>当数据对象较大时（如结构和类对象）， 第二个原因最重要。这些也是使用指针参数的原因。这是有道理的， 因为引用参数实际上是基千指针的代码的另一个接口。</p>
<h3 id="一些建议"><a class="markdownIt-Anchor" href="#一些建议"></a> 一些建议</h3>
<p>对于使用传递的值而不作修改的函数。</p>
<ul>
<li>如果数据对象很小， 如内置数据类型或小型结构， 则按值传递。</li>
<li>如果数据对象是数组， 则使用指针， 因为这是唯一的选择， 并将指针声明为指向<code>const</code>的指针。</li>
<li>如果数据对象是较大的结构， 则使用<code>const</code>指针或<code>const</code>引用， 以提高程序的效率。这样可以节省复制结构所需的时间和空间。</li>
<li>如果数据对象是类对象， 则使用<code>const</code> 引用。类设计的语义常常要求使用引用， 这是C++新增这</li>
<li>项特性的主要原因。因此， 传递类对象参数的标准方式是按引用传递。</li>
</ul>
<p>对于修改调用函数中数据的函数。</p>
<ul>
<li>如果数据对象是内置数据类型，则使用指针。如果看到诸如<code>fix.it (&amp;x)</code>这样的代码（其中<code>x</code>是<code>int</code>)，则很明显， 该函数将修改<code>x</code>。</li>
<li>如果数据对象是数组， 则只能使用指针。</li>
<li>如果数据对象是结构， 则使用引用或指针。</li>
<li>如果数据对象是类对象， 则使用引用。</li>
</ul>
<h1 id="4-默认参数"><a class="markdownIt-Anchor" href="#4-默认参数"></a> 4 默认参数</h1>
<p>指的是函数原型中已经设置默认的参数，用户可以不用输入的参数。</p>
<p><strong>默认参数是写在函数原型中，而不是函数定义中</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b = <span class="number">5</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">test</span>(<span class="number">4</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a*b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>函数默认参数需在函数原型中定义</strong></li>
<li>用户可以传递值来覆盖默认参数</li>
<li>当一个参数设置为默认参数，则它后边的所有参数都要设置默认参数</li>
<li>赋值时不能跳过任何参数</li>
</ul>
<h1 id="5-函数重载"><a class="markdownIt-Anchor" href="#5-函数重载"></a> 5 函数重载</h1>
<p>函数多态 == 函数重载</p>
<p>C++允许有名字相同的函数，但是特征标需要不同。</p>
<ul>
<li>函数重载的关键是<strong>函数的参数列表(函数特征标)</strong></li>
<li>函数参数中类型引用和类型本身视为同一个特征标</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">cube</span><span class="params">(<span class="keyword">double</span> x)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">cube</span><span class="params">(<span class="keyword">double</span> &amp; x)</span></span>;</span><br><span class="line"><span class="comment">//这两个函数是一样的 但是特定情况下也是能够通过编译的。重载协议致力于匹配一个最优的版本。但如果两个都是最优就会进行报错</span></span><br></pre></td></tr></table></figure>
<ul>
<li><code>const</code>的特殊，这种特殊是由于C++中非<code>const</code>值可以赋值给<code>const</code></li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">a</span><span class="params">(<span class="keyword">char</span> * b)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">a</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> * b)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">c</span><span class="params">(<span class="keyword">char</span> *d)</span></span>;</span><br><span class="line"><span class="comment">//a函数参数的传入会根据传入类型是否是const而决定使用那个函数</span></span><br><span class="line"><span class="comment">//而c函数可以传入const和非const值</span></span><br></pre></td></tr></table></figure>
<ul>
<li>函数重载的关键时特征标而不是类型</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">a</span><span class="params">(<span class="keyword">int</span> n, <span class="keyword">int</span> m)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">a</span><span class="params">(<span class="keyword">int</span> y, <span class="keyword">int</span> x)</span></span>;</span><br><span class="line"><span class="comment">//这两个函数是互相排斥的，c++不允许这样重载</span></span><br></pre></td></tr></table></figure>
<h1 id="6-函数模板"><a class="markdownIt-Anchor" href="#6-函数模板"></a> 6 函数模板</h1>
<h2 id="61-格式"><a class="markdownIt-Anchor" href="#61-格式"></a> 6.1 格式</h2>
<p><code>template &lt;typename T&gt;//or class T</code>创建一个模板T</p>
<h2 id="62-使用模板"><a class="markdownIt-Anchor" href="#62-使用模板"></a> 6.2 使用模板</h2>
<p>模板是为了解决函数使用相同的<strong>算法</strong>。例如下面不管是<code>int</code>还是<code>double</code>类型他们需要交换值时都是用相同的一套算法。</p>
<p><strong>T被称作为泛型，可以被具体性替代(如<code>int</code>，<code>double</code>)</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt; <span class="comment">//此处没有;</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Swap</span><span class="params">(T &amp;a, T &amp;b)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">4</span>;</span><br><span class="line">    <span class="keyword">double</span> c = <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">double</span> d = <span class="number">6</span>;</span><br><span class="line">    <span class="built_in">Swap</span>(a, b); <span class="comment">//将T替换为int</span></span><br><span class="line">    <span class="built_in">Swap</span>(c, d); <span class="comment">//将T替换为double</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//一下一整块都是模板定义</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;  <span class="comment">//此处没有; </span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Swap</span><span class="params">(T &amp;a, T &amp;b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    T temp;</span><br><span class="line">    temp = a;</span><br><span class="line">    a = b;</span><br><span class="line">    b = temp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>函数接受参数传递并把模板中的T替换为参数的类型。</p>
<h2 id="63-重载模板"><a class="markdownIt-Anchor" href="#63-重载模板"></a> 6.3 重载模板</h2>
<p>类似函数的重载，对于相同的名字其功能类似。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 交换值</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Swap</span><span class="params">(T &amp;a, T &amp;b)</span></span>;</span><br><span class="line"><span class="comment">// 交换数组</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Swap</span><span class="params">(T * a, T * b, <span class="keyword">int</span> a)</span></span>; <span class="comment">//模板中也可以使用具体的类型</span></span><br></pre></td></tr></table></figure>
<h2 id="64-模板的局限性和处理方法显示具体化"><a class="markdownIt-Anchor" href="#64-模板的局限性和处理方法显示具体化"></a> 6.4 模板的局限性和处理方法（显示具体化）</h2>
<p>模板中定义比较大小，如果T是结构的话则语法错误。所以使用中需要谨慎，可以用显示具体化解决。</p>
<h3 id="641-显示具体化"><a class="markdownIt-Anchor" href="#641-显示具体化"></a> 6.4.1 显示具体化</h3>
<p>格式：例<code>template &lt;&gt; void Swap&lt;结构名&gt;(job &amp;, job &amp;)</code>其中<code>&lt;结构名&gt;</code>可以省略。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//交换job结构里的成员</span></span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="keyword">void</span> Swap&lt;job&gt;(job &amp;j1, job &amp;j2)  <span class="comment">// specialization</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">double</span> t1;</span><br><span class="line">    <span class="keyword">int</span> t2;</span><br><span class="line">    t1 = j1.salary;</span><br><span class="line">    j1.salary = j2.salary;</span><br><span class="line">    j2.salary = t1;</span><br><span class="line">    t2 = j1.floor;</span><br><span class="line">    j1.floor = j2.floor;</span><br><span class="line">    j2.floor = t2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="实例化和具体化"><a class="markdownIt-Anchor" href="#实例化和具体化"></a> 实例化和具体化</h4>
<p>当使用模板的函数时，编译器会生成这个模板的实例对象。最初编译器是<strong>隐式实例化</strong>现在可以<strong>显示实例化</strong>。</p>
<ul>
<li>
<p>隐式实例化只有传入参数时编译器根据参数判断会创建相应实例对象</p>
</li>
<li>
<p>显示实例化可以直接创建一个实例，需要在函数名子后面加<code>&lt;类型&gt;</code>，如<code>template void Swap&lt;int&gt;(int, int)</code>将会创建<code>int</code>型实例对象。</p>
</li>
</ul>
<p>前提是传入参数是类型要匹配。</p>
<h3 id="642-decltype关键字"><a class="markdownIt-Anchor" href="#642-decltype关键字"></a> 6.4.2 <code>decltype</code>关键字</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;class T1, class T2&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ft</span><span class="params">(T1 x, T2 y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ??? sum = x + y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在定义时<code>sum</code>的类型未知所以C++11引入<code>decltype</code>关键字</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">decltype</span>(x+y) sum = x + y;</span><br></pre></td></tr></table></figure>
<p><code>decltype</code>声明了了一个和<code>x+y</code>相同类型的<code>sum</code>。</p>
<h3 id="643-auto关键字后置返回类型"><a class="markdownIt-Anchor" href="#643-auto关键字后置返回类型"></a> 6.4.3 auto关键字(后置返回类型)</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T1</span>, <span class="keyword">class</span> <span class="title">T2</span>&gt;</span></span><br><span class="line">??? <span class="built_in">ft</span>(T1 x, T2 y)</span><br><span class="line">&#123;</span><br><span class="line">     <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种情况是不能使用decltype关键字的，<code>x</code>和<code>y</code>的类型实在<code>???</code>后声明的，编译器不会越过<code>???</code>去执行下一步。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;class T1, class T2&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">ft</span><span class="params">(T1 x, T2 y)</span> -&gt; <span class="keyword">double</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">     <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;class T1, class T2&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">ft</span><span class="params">(T1 x, T2 y)</span> -&gt; <span class="title">decltype</span><span class="params">(x + y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">     <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用<code>auto</code><strong>关键字来占位</strong>，用<code>-&gt; double和-&gt;decltype(x+y)</code>来表示后置返回类型。</p>
<h2 id="65-编译器的选择规则"><a class="markdownIt-Anchor" href="#65-编译器的选择规则"></a> 6.5 编译器的选择规则</h2>
<p>过程</p>
<ul>
<li>
<p>第1步： 创建候选函数列表。其中包含与被调用函数的名称相同的函数和模板函数。</p>
</li>
<li>
<p>第2步： 使用候选函数列表创建可行函数列表。这些都是参数数目正确的函数， 为此有一个隐式<br>
转换序列， 其中包括实参类型与相应的形参类型完全匹配的情况。例如， 使用<code>float</code>参数的函数调<br>
用可以将该参数转换为<code>double</code>, 从而与<code>double</code>形参匹配， 而模板可以为<code>float</code>生成一个实例。</p>
</li>
<li>
<p>第3步： 确定是否有最佳的可行函数。如果有， 则使用它， 否则该函数调用出错。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//候选函数列表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">may</span> <span class="params">(<span class="keyword">int</span>)</span></span>;<span class="comment">// #1</span></span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">may</span><span class="params">(<span class="keyword">float</span>, <span class="keyword">float</span> = <span class="number">3</span>)</span></span>;<span class="comment">//#2</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">may</span> <span class="params">(<span class="keyword">char</span>)</span></span>;<span class="comment">//#3</span></span><br><span class="line"><span class="function"><span class="keyword">char</span> * <span class="title">may</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *)</span></span>; <span class="comment">// #4</span></span><br><span class="line"><span class="function"><span class="keyword">char</span> <span class="title">may</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> &amp;)</span></span>; <span class="comment">// #5</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;class T&gt; <span class="keyword">void</span> <span class="title">may</span><span class="params">(<span class="keyword">const</span> T &amp;)</span> </span>; <span class="comment">// #6</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;class T&gt; <span class="keyword">void</span> <span class="title">may</span><span class="params">(T *)</span></span>; <span class="comment">// #7</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//函数调用</span></span><br><span class="line"><span class="built_in">may</span>(<span class="string">&#x27;a&#x27;</span>);<span class="comment">//这时函数将从候选列表选择最佳</span></span><br></pre></td></tr></table></figure>
<p>编译器将会匹配<strong>最佳</strong>的函数</p>
</li>
<li>
<p>优先级：</p>
<ol>
<li>完全匹配， 但常规函数优先千模板。</li>
<li>提升转换（例如，<code>char</code>和<code>shorts</code>自动转换为int, <code>float</code>自动转换为<code>double</code>)。</li>
<li>标准转换（例如，<code>int</code>转换为<code>char</code>,<code>long</code>转换为<code>double</code> )。</li>
<li>用户定义的转换， 如类声明中定义的转换。</li>
</ol>
</li>
</ul>
<p>例如， 函数＃1优于函数＃2 , 因为<code>char</code>到<code>int</code>的转换是提升转换， 而char到float的转换是标准转换函数＃3、函数＃5和函数＃6 都优千函数＃1和＃2， 因为它们都是完全匹配的。＃3和＃5优于＃6， 因为＃6 函数是模板。</p>
<hr>
<h2 id="title-date-2021-05-10-114640tags"><a class="markdownIt-Anchor" href="#title-date-2021-05-10-114640tags"></a> title: �������<br>
date: 2021-05-10 11:46:40<br>
tags:</h2>
]]></content>
  </entry>
  <entry>
    <title>友元</title>
    <url>/2021/05/12/%E5%8F%8B%E5%85%83/</url>
    <content><![CDATA[<h1 id="友元"><a class="markdownIt-Anchor" href="#友元"></a> 友元</h1>
<ul>
<li>友元函数</li>
<li>友元类</li>
<li>友元成员函数</li>
</ul>
<h2 id="友元函数"><a class="markdownIt-Anchor" href="#友元函数"></a> 友元函数</h2>
<h3 id="创建"><a class="markdownIt-Anchor" href="#创建"></a> 创建</h3>
<p>在类中声明，使用<code>friend</code>关键字</p>
<p><code>friend Time operator *(double m, Time % t)</code></p>
<p><strong>虽然友元函数在类中声明，但并不是成员函数，不能用成员运算符调用，但是权限却和成员函数相同，定义的时候不用::调用，并且不含friend关键字</strong></p>
<h2 id="友元类"><a class="markdownIt-Anchor" href="#友元类"></a> 友元类</h2>
<p>在原类中声明友元类，则友元类可以访问原类中的<strong>私有成员</strong>和<strong>公有成员</strong>。</p>
<h2 id="友元成员函数"><a class="markdownIt-Anchor" href="#友元成员函数"></a> 友元成员函数</h2>
<p>可以在原类中单独声明其他类中的一个成员函数作为友元函数，这个友元函数可以访问原类中的<strong>私有成员</strong>和<strong>公有成员</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tv</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Remote</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ...;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Remote::set_chan</span><span class="params">(Tv&amp;t, <span class="keyword">int</span> c)</span></span>;</span><br><span class="line">    ....;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tv</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    .....;</span><br><span class="line">    <span class="function"><span class="keyword">friend</span> <span class="keyword">void</span> <span class="title">Remote::set_chan</span><span class="params">(Tv&amp;t, <span class="keyword">int</span> c)</span></span>;<span class="comment">//友元成员函数</span></span><br><span class="line">    ......;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Tv中的友元函数含有Remote的方法，所以Tv前面必须要有Remote的set_chan()的声明，而Remote类中又含有Tv所以要在最前面声明一个<code>class Tv;</code></p>
<h2 id="共同的友元"><a class="markdownIt-Anchor" href="#共同的友元"></a> 共同的友元</h2>
<p>当函数需要访问两个类的私有数据，这是可以把这个函数可以声明为两个类的友元。</p>
]]></content>
  </entry>
  <entry>
    <title>启动管理</title>
    <url>/2021/05/12/%E5%90%AF%E5%8A%A8%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="启动管理"><a class="markdownIt-Anchor" href="#启动管理"></a> 启动管理</h1>
<h2 id="启动管理-2"><a class="markdownIt-Anchor" href="#启动管理-2"></a> 启动管理</h2>
<p>运行级别</p>
<table>
<thead>
<tr>
<th>运行级别</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>关机</td>
</tr>
<tr>
<td>1</td>
<td>单用户模式，类似于windows安全模式，主要是用于系统恢复</td>
</tr>
<tr>
<td>2</td>
<td>不完全的命令行模式，不含NFS</td>
</tr>
<tr>
<td>3</td>
<td>完全的命令行模式</td>
</tr>
<tr>
<td>4</td>
<td>系统保留</td>
</tr>
<tr>
<td>5</td>
<td>图形界面</td>
</tr>
<tr>
<td>6</td>
<td>重启动</td>
</tr>
</tbody>
</table>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">runlevel <span class="comment">#查看运行级别</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">init 运行级别  <span class="comment">#改变系统运行级别</span></span><br></pre></td></tr></table></figure>
<h2 id="启动过程"><a class="markdownIt-Anchor" href="#启动过程"></a> 启动过程</h2>
<p>​	<img src="/2021/05/12/%E5%90%AF%E5%8A%A8%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B.png" alt="启动流程"></p>
<h2 id="grub"><a class="markdownIt-Anchor" href="#grub"></a> GRUB</h2>
<h3 id="grub配置文件"><a class="markdownIt-Anchor" href="#grub配置文件"></a> grub配置文件</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">default = 数字	<span class="comment">#启动的默认系统</span></span><br><span class="line">timeout = 数字	<span class="comment">#启动等待时间</span></span><br><span class="line">splashimage = 地址 <span class="comment">#启动背景图片</span></span><br><span class="line">hiddenmenu <span class="comment">#隐藏主菜单</span></span><br><span class="line">title 菜单上系统的名字</span><br><span class="line">		root(hd0,0) <span class="comment">#启动程序的保存分区</span></span><br><span class="line">		kernel 内核  <span class="comment">#启动内核</span></span><br><span class="line">		initrd 文件.img  <span class="comment">#模拟的根目录</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>土壤改良剂</title>
    <url>/2021/05/12/%E5%9C%9F%E5%A3%A4%E6%94%B9%E8%89%AF%E5%89%82/</url>
    <content><![CDATA[<h1 id="土壤改良剂"><a class="markdownIt-Anchor" href="#土壤改良剂"></a> 土壤改良剂</h1>
<ul>
<li>
<p>首先原料来源于固体废弃物，将固废资源化利用，减少了固体废弃物堆放的问题。具有改善土壤质量，改良土壤物化性质，生物性质，提高土壤肥率，促进耕作土壤的可持续发展。</p>
</li>
<li>
<p>该土壤改良剂具有保水、节肥、增加土壤的肥力、改善土壤通透性、提供植物生长必需的微量元素、吸附和固化土壤中的重金属元素等特征。同时，本发明土壤改良剂及其制备方法还具有原料极易获取、制作流程简单、环保安全和土壤改良效果佳等优点。同时具有大多数土壤改良剂不具备的吸附土壤中重金属的能力。一定程度上吸附土壤中重金属污染的作用。</p>
</li>
<li>
<h3 id="322产品优势"><a class="markdownIt-Anchor" href="#322产品优势"></a> 3.2.2产品优势</h3>
<p><strong>1</strong>**）固废资源化利用。**本产品的原料来源于采煤过程和洗煤过程中排放的固体废物煤矸石，以及农业固体废物为主要材料。在制备土壤改良剂的同时，也将堆放的固体废弃物使用了，解决了固废的污染问题和堆放问题。</p>
<p><strong>2</strong>**）具有很强的污染吸附能力。**通过市场调查发现，在现有的土壤改良剂中，没有任何一种土壤改良剂具有对土壤重金属污染物的吸附能力，而本公司的产品出现正好打破了这一点。既能够解决土壤重金属污染问题，又可以对土壤进行改良，具有多重优势。</p>
<p><strong>3</strong>**）成分合理，副作用极小。**根据市场调查，对现在市面上的土壤改良剂成分分析，研究发现现有土壤改良剂主要以生物炭或农业秸秆为基质，易造成土壤板结，恶化土壤质量。但是本公司的煤矸石土壤改良剂成分合理，制备工艺成熟，有效避免了对土壤改良后的副作用。</p>
<p><strong>4</strong>**）能够有效改善土壤理化性质。**根据市场调查，研究分析，现有土壤改良剂的有效成分较为单一，不能有效的改善土壤的理化性质。</p>
<p><strong>5</strong>**）产品适用广泛性强。**根据市场调查，现在市面上的土壤改良剂主要针对某种特定作物的土壤进行改良，使用的广泛性不足。本公司的煤矸石土壤改良剂对土壤改良有较强的广泛性，改良后的土壤对多种作物的生长具有极大的促进作用。</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>备份</title>
    <url>/2021/05/12/%E5%A4%87%E4%BB%BD/</url>
    <content><![CDATA[<h1 id="备份"><a class="markdownIt-Anchor" href="#备份"></a> 备份</h1>
<p>需要备份的数据</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/root/</span><br><span class="line">/home/</span><br><span class="line">/var/spool/mail/</span><br><span class="line">/etc/</span><br><span class="line">其他</span><br></pre></td></tr></table></figure>
<h2 id="备份策略"><a class="markdownIt-Anchor" href="#备份策略"></a> 备份策略</h2>
<ol>
<li>完全备份</li>
<li>增量备份</li>
<li>差异备份</li>
</ol>
<h2 id="备份-2"><a class="markdownIt-Anchor" href="#备份-2"></a> 备份</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dump [option] 备份之后的文件名 原文件或目录</span><br><span class="line">	 -n：n为0-9十个备份级别，-0，-1，-2.....</span><br><span class="line">	 -f 文件名：指定备份之后的文件名</span><br><span class="line">	 -u：备份成功后，把备份时间放在/etc/dumpdates文件</span><br><span class="line">	 -v：显示备份过程中更多的输出信息</span><br><span class="line">	 -j：调用bzlib库压缩备份文件，就是把备份文件压缩为.bz2格式</span><br><span class="line">	 -W：显示允许被dump的分区的备份等级及备份时间</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">restore [模式选择] [option]</span><br><span class="line">		-C：比较备份数据和实际数据的变化</span><br><span class="line">		-i：进入交互模式，手工选择恢复的文件</span><br><span class="line">		-t：查看模式，用于查看备份文件中拥有的那些数据</span><br><span class="line">		-r：还原模式</span><br><span class="line">				 -f：指定备份文件的文件名</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>多重继承</title>
    <url>/2021/05/12/%E5%A4%9A%E9%87%8D%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<h1 id="多重继承"><a class="markdownIt-Anchor" href="#多重继承"></a> 多重继承</h1>
<p><img src="/2021/05/12/%E5%A4%9A%E9%87%8D%E7%BB%A7%E6%89%BF/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%A4%9A%E9%87%8D%E7%BB%A7%E6%89%BF.png" alt="未命名文件"></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">SingerWaiter ed;</span><br><span class="line">Worker *p = &amp;ed; <span class="comment">//此时具有二义性</span></span><br></pre></td></tr></table></figure>
<p><code>singerwaiter</code>分别继承了<code>waiter</code>和<code>singer</code>中的<code>worker</code>基类，所以p并不知道到底是那个地址的<code>worker</code></p>
<p>所以要使用强制类型转换</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Worker *p = (Waiter *) &amp;ed;</span><br><span class="line">Worker *p = (Singer *) &amp;ed;</span><br></pre></td></tr></table></figure>
<h2 id="虚基类"><a class="markdownIt-Anchor" href="#虚基类"></a> 虚基类</h2>
<p>当使用虚基类的时候就不会出现上述情况</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Waiter</span> :</span> <span class="keyword">virtual</span> <span class="keyword">public</span> Worker;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singer</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Worker;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SingerWaiter</span> :</span> <span class="keyword">public</span> Singer, <span class="keyword">public</span> Waiter;</span><br></pre></td></tr></table></figure>
<p>使用关键字<code>virtual</code>使得<code>waiter</code>和<code>singer</code>继承的基类是同一个地址基类，这样<code>singerwaiter</code>就不会出现二义性</p>
<h2 id="混合使用虚基类和非虚基类"><a class="markdownIt-Anchor" href="#混合使用虚基类和非虚基类"></a> 混合使用虚基类和非虚基类</h2>
<p><img src="/2021/05/12/%E5%A4%9A%E9%87%8D%E7%BB%A7%E6%89%BF/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E8%99%9A%E5%9F%BA%E7%B1%BB%E6%B7%B7%E5%90%88%E7%BB%A7%E6%89%BF.png" alt="虚基类混合继承"></p>
]]></content>
  </entry>
  <entry>
    <title>头文件经常包含的内容</title>
    <url>/2021/05/12/%E5%A4%B4%E6%96%87%E4%BB%B6%E7%BB%8F%E5%B8%B8%E5%8C%85%E5%90%AB%E7%9A%84%E5%86%85%E5%AE%B9/</url>
    <content><![CDATA[<h1 id="1-头文件经常包含的内容"><a class="markdownIt-Anchor" href="#1-头文件经常包含的内容"></a> 1 头文件经常包含的内容</h1>
<ul>
<li>函数原型</li>
<li>使用<code>#define</code>或<code>const</code>定义的符号常量</li>
<li>结构声明</li>
<li>类声明</li>
<li>模板声明</li>
<li>内联函数</li>
</ul>
<h2 id="11-使用头文件"><a class="markdownIt-Anchor" href="#11-使用头文件"></a> 1.1 使用头文件</h2>
<p>要用<code>&quot;coordin.h&quot;</code>而不是<code>&lt;coordin.h&gt;</code>尖括号是存储在标准头文件中的，双引号的时自己定义的。</p>
<h2 id="12-头文件管理ifndef和endif"><a class="markdownIt-Anchor" href="#12-头文件管理ifndef和endif"></a> 1.2 头文件管理(ifndef和endif)</h2>
<p>同一个头文件只能include一次，使用<code>#ifndef</code>和<code>#endif</code>来解决。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//coordin.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> COORDIN_H_</span></span><br><span class="line">.......</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p>每次读取头文件时会查询COORDIN_H_是否被定义，如果被定义则跳过。</p>
]]></content>
  </entry>
  <entry>
    <title>存储的连续性、作用域和链接性</title>
    <url>/2021/05/12/%E5%AD%98%E5%82%A8%E7%9A%84%E8%BF%9E%E7%BB%AD%E6%80%A7%E3%80%81%E4%BD%9C%E7%94%A8%E5%9F%9F%E5%92%8C%E9%93%BE%E6%8E%A5%E6%80%A7/</url>
    <content><![CDATA[<h1 id="1-存储的连续性-作用域和链接性"><a class="markdownIt-Anchor" href="#1-存储的连续性-作用域和链接性"></a> 1 存储的连续性、作用域和链接性</h1>
<ul>
<li>自动存储持续性：在函数定义中声明的变量（包括函数参数）的存储持续性为自动的。它们在程序开始执行其所属的函数或代码块时被创建，在执行完函数或代码块时，它们使用的内存被释放。 C++有两种存储持续性为自动的变量。</li>
<li>静态存储持续性：在函数定义外定义的变量和使用关键字static定义的变量的存储持续性都为静态。它们在程序整个运行过程中都存在。C++有3种存储持续性为静态的变量。</li>
<li>线程存储持续性(C++11)：当前，多核处理器很常见，这些CPU可同时处理多个执行任务。这让程序能够将计算放在可并行处理的不同线程中。如果变量是使用关键字<code>thread_local</code>声明的，则其生命周期与所属的线程一样长。</li>
<li>动态存储持续性：用<code>new</code>运算符分配的内存将一直存在，直到使用<code>delete</code>运算符将其释放或程序 结束为止。这种内存的存储持续性为动态，有时被称为自由存储(freestore)或堆(heap)。</li>
</ul>
<h2 id="11-作用域"><a class="markdownIt-Anchor" href="#11-作用域"></a> 1.1 作用域</h2>
<h2 id="12-自动存储持续性"><a class="markdownIt-Anchor" href="#12-自动存储持续性"></a> 1.2 自动存储持续性</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">int</span> one;<span class="comment">//在所有代码块外，全局变量</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> one; <span class="comment">//代码块内又创建同名变量，原变量被隐藏，当此变量所在代码块执行完毕，原变量被显现</span></span><br><span class="line">    <span class="keyword">int</span> two; <span class="comment">//只在该代码块内有效，局部变量</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> three;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="121-自动变量和栈"><a class="markdownIt-Anchor" href="#121-自动变量和栈"></a> 1.2.1 自动变量和栈</h3>
<p>栈是由一块内存和指针构成。一个指针指向栈顶一个指向栈底，每次向栈内添加变量栈顶的指针就会上移，不需要时就会回复到原位。</p>
]]></content>
  </entry>
  <entry>
    <title>字符串处理</title>
    <url>/2021/05/12/%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="字符串处理"><a class="markdownIt-Anchor" href="#字符串处理"></a> 字符串处理</h1>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sort [option] filename</span><br><span class="line">	-f	忽略大小写</span><br><span class="line">	-n	以数值型排序</span><br><span class="line">	-r	反向排序</span><br><span class="line">	-t	指定分隔符</span><br><span class="line">	-k n[,m]	按照指定字段范围排序</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wc [option] 文件名</span><br><span class="line">	-l：只统计行数</span><br><span class="line">	-w：只统计单词数</span><br><span class="line">	-m：只统计字符串</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>实参是数组时的注意事项</title>
    <url>/2021/05/12/%E5%AE%9E%E5%8F%82%E6%98%AF%E6%95%B0%E7%BB%84%E6%97%B6%E7%9A%84%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/</url>
    <content><![CDATA[<h1 id="将数组作为实参传入函数时注意事项"><a class="markdownIt-Anchor" href="#将数组作为实参传入函数时注意事项"></a> 将数组作为实参传入函数时注意事项</h1>
<p>数组的名字是一个指针，指向数组第一个元素。</p>
<h2 id="普通的传入"><a class="markdownIt-Anchor" href="#普通的传入"></a> 普通的传入</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">count</span><span class="params">(<span class="keyword">int</span> * a)</span></span>;  <span class="comment">//1</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">count</span><span class="params">(<span class="keyword">int</span> a[])</span></span>;  <span class="comment">//2</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">count</span><span class="params">(<span class="keyword">int</span> a[<span class="number">5</span>])</span></span>; <span class="comment">//3 与2相同</span></span><br></pre></td></tr></table></figure>
<p><strong>使用数组名字作为指针传入一个函数要注意这个函数的形参</strong></p>
<h2 id="注意int-c-5的意义"><a class="markdownIt-Anchor" href="#注意int-c-5的意义"></a> <strong>注意<code>int (&amp;c) [5]的意义</code></strong></h2>
<p><strong>c是a的别名</strong></p>
<p>所以传入函数时</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">count</span><span class="params">(<span class="keyword">int</span> (&amp;c) [<span class="number">5</span>])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...........;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="int-c-5和int-c-5的区别"><a class="markdownIt-Anchor" href="#int-c-5和int-c-5的区别"></a> <code>int (*c) [5]和int (&amp;c) [5]的区别</code></h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in"><span class="keyword">int</span></span> (*c) [<span class="number">5</span>];<span class="comment">// 相当于一个二维数组</span></span><br><span class="line"><span class="comment">//c指向行的第一个元素</span></span><br><span class="line"><span class="built_in"><span class="keyword">int</span></span> (*c) [<span class="number">5</span>] &#123;&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;, &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">4</span>&#125;,&#123;<span class="number">1</span>,<span class="number">6</span>,<span class="number">2</span>,<span class="number">4</span>,<span class="number">5</span>&#125;&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="普通传入和int-c-5"><a class="markdownIt-Anchor" href="#普通传入和int-c-5"></a> 普通传入和int (&amp;c) [5]</h2>
<p>普通传入是没有数组的长度的，sizeof©的结果只是这个c指针的类型长度，sizeof(*c)只是c</p>
<p>指向的元素对象的类型长度。</p>
<p>而int (&amp;c) [5]是数组的引用，原封不动的将数组传入，<strong>没有经过复制构造函数的复制过程</strong>，所以sizeof©是c数组的长度,sizeof(*c)是c指向的第一个元素对象的类型大小.</p>
]]></content>
  </entry>
  <entry>
    <title>容器的类型</title>
    <url>/2021/05/12/%E5%AE%B9%E5%99%A8%E7%9A%84%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="容器的类型"><a class="markdownIt-Anchor" href="#容器的类型"></a> 容器的类型</h1>
<h2 id="一般容器的特征"><a class="markdownIt-Anchor" href="#一般容器的特征"></a> 一般容器的特征</h2>
<p>X 表示容器类型，如vector<int>; T 表示存储在容器中的对象类型； a 和b 表示类型为X 的值； r 表示类型为X＆的值； u 表示类型为X的标识符</int></p>
<p>（即如果X 表示vector<int>， 则u 是一个vector<int>对象）。</int></int></p>
<table>
<thead>
<tr>
<th>表达式</th>
<th>返回类型</th>
<th>说明</th>
<th>复杂度</th>
</tr>
</thead>
<tbody>
<tr>
<td>X::iterator</td>
<td>指向T的选代器类型</td>
<td>满足正向选代器要求的任何达代器</td>
<td>编译时间</td>
</tr>
<tr>
<td>X::value_type</td>
<td>T</td>
<td>T的类型</td>
<td>编译时间</td>
</tr>
<tr>
<td>X u;</td>
<td></td>
<td>创建一个名为u的空容器</td>
<td>固定</td>
</tr>
<tr>
<td>X();</td>
<td></td>
<td>创建一个匿名的空容器</td>
<td>固定</td>
</tr>
<tr>
<td>X u(a);</td>
<td></td>
<td>调用复制构造函数后u == a</td>
<td>线性</td>
</tr>
<tr>
<td>X u=a;</td>
<td></td>
<td>作用同X u(a)</td>
<td>线性</td>
</tr>
<tr>
<td>r=a;</td>
<td>X&amp;</td>
<td>调用赋值运算符后r == a</td>
<td>线性</td>
</tr>
<tr>
<td>(&amp;a) -&gt; ~X()</td>
<td>void</td>
<td>对容器中每个元素应用析构函数</td>
<td>线性</td>
</tr>
<tr>
<td>a.begin()</td>
<td>选代器</td>
<td>返回指向容器第一个元素的选代器</td>
<td>固定</td>
</tr>
<tr>
<td>a.end()</td>
<td>选代器</td>
<td>返回超尾值达代器</td>
<td>固定</td>
</tr>
<tr>
<td>a.size()</td>
<td>无符号整型</td>
<td>返回元素个数,等价于a.end()-a.begin()</td>
<td>固定</td>
</tr>
<tr>
<td>a.swap(b)</td>
<td>void</td>
<td>交换a和b的内容</td>
<td>固定</td>
</tr>
<tr>
<td>a==b;</td>
<td>可转换为bool</td>
<td>如果a和b的长度相同且a中每个元素都等于(==为真)b中相应的元素,则为真</td>
<td>线性</td>
</tr>
<tr>
<td>a!=b;</td>
<td>可转换为bool</td>
<td>返回!(a==b)</td>
<td>线性</td>
</tr>
<tr>
<td>X u(rv);</td>
<td></td>
<td>调用移动构造函数后，u与rv的原始值相同</td>
<td>线性</td>
</tr>
<tr>
<td>X u = rv;</td>
<td></td>
<td>作用同X u(rv)</td>
<td></td>
</tr>
<tr>
<td>a = rv;</td>
<td>X&amp;</td>
<td>调用移动赋值运算符后， u的值与rv的原始值相同</td>
<td>线性</td>
</tr>
<tr>
<td>a.cbegin()</td>
<td>const_iterator</td>
<td>返回指向容器第一个元素的const迭代器</td>
<td>固定</td>
</tr>
<tr>
<td>a.cend()</td>
<td>const_iterator</td>
<td>返回超尾值const迭代器</td>
<td>固定</td>
</tr>
</tbody>
</table>
<h1 id="序列"><a class="markdownIt-Anchor" href="#序列"></a> 序列</h1>
<p>序列的要求</p>
<p><img src="/2021/05/12/%E5%AE%B9%E5%99%A8%E7%9A%84%E7%B1%BB%E5%9E%8B/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C2020-09-10_210444.png" alt="2020-09-10_210444"></p>
<p>序列的可选要求</p>
<p><img src="/2021/05/12/%E5%AE%B9%E5%99%A8%E7%9A%84%E7%B1%BB%E5%9E%8B/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C2020-09-10_210454.png" alt="2020-09-10_210454"></p>
<h1 id="容器"><a class="markdownIt-Anchor" href="#容器"></a> 容器</h1>
<h2 id="vector"><a class="markdownIt-Anchor" href="#vector"></a> vector</h2>
<p>前面介绍了多个使用vector 模板的例子，该模板是在vector头文件中声明的。简单地说，vector 是数组的一种类表示， 它提供了自动内</p>
<p>存管理功能，可以动态地改变vector 对象的长度，并随着元素的添加和删除而增大和缩小。它提供了对元素的随机访问。在尾部添加和删</p>
<p>除元素的时间是固定的， 但在头部或中间插入和删除元素的复杂度为线性时间。除序列外，vector 还是可反转容器(reversiblecontainer)</p>
<p>概念的模型。这增加了两个类方法： rbegin( ）和rend( ）， 前者返回一个指向反转序列的第一个元素的迭代器， 后者返回反转序列的超</p>
<p>尾迭代器。因此，如果dice 是一个vector<int>容器，而Show(int)是显示一个整数的函数， 则下面的代码将首先正向显示dice 的内容然</int></p>
<p>后反向显示</p>
<h2 id="deque"><a class="markdownIt-Anchor" href="#deque"></a> deque</h2>
<p>deque 模板类（在deque 头文件中声明）表示双端队列(double-ended queue)， 通常被简称为deque。在STL 中，其实现类似千vector</p>
<p>容器，支持随机访问。主要区别在于，从deque 对象的开始位置插入和删除元素的时间是固定的， 而不像vector 中那样是线性时间的。</p>
<p>所以，如果多数操作发生在序列的起始和结尾处， 则应考虑使用deque 数据结构。为实现在deque 两端执行插入和删除操作的时间为固</p>
<p>定的这一目的， deque 对象的设计比vector 对象更为复杂。因此， 尽管二者都提供对元素的随机访问和在序列中部执行线性时间的插入</p>
<p>和删除操作， 但vector 容器执行这些操作时速度要快些。</p>
<h2 id="list"><a class="markdownIt-Anchor" href="#list"></a> list</h2>
<p>list 模板类（在list 头文件中声明）表示双向链表。除了第一个和最后一个元素外，每个元素都与前后的元素相链接， 这意味着可以双向</p>
<p>遍历链表。list 和vector 之间关键的区别在千，list 在链表中任一位置进行插入和删除的时间都是固定的(vector 模板提供了除结尾处外的</p>
<p>线性时间的插入和删除，在结尾处，它提供了固定时间的插入和删除）。因此， vector 强调的是通过随机访问进行快速访问， 而list 强</p>
<p>调的是元素的快速插入和删除。与vector 相似，list 也是可反转容器。与vector 不同的是，list 不支持数组表示法和随机访问。与矢量迭</p>
<p>代器不同，从容器中插入或删除元素之后，链表迭代器指向元素将不变。我们来解释一下这句话。例如，假设有一个指向vector 容器第5</p>
<p>个元素的迭代器，并在容器的起始处插入一个元素。此时， 必须移动其他所有元素， 以便腾出位置， 因此插入后， 第5 个元素包含的值</p>
<p>将是以前第4 个元素的值。因此，迭代器指向的位置不变， 但数据不同。然后， 在链表中插入新元素并不会移动已有的元素， 而只是修</p>
<p>改链接信息。指向某个元素的迭代器仍然指向该元素， 但它链接的元素可能与以前不同。</p>
<p><img src="/2021/05/12/%E5%AE%B9%E5%99%A8%E7%9A%84%E7%B1%BB%E5%9E%8B/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C2020-09-10_212535.png" alt="2020-09-10_212535"></p>
<h2 id="list工具箱"><a class="markdownIt-Anchor" href="#list工具箱"></a> list工具箱</h2>
<p>list方法组成了一个方便的工具箱。例如， 假设有两个邮件列表要整理，则可以对每个列表进行排序，合并它们， 然后使用unique(）来</p>
<p>删除重复的元素。sort( ）、merge( ）和unique( ） 方法还各自拥有接受另一个参数的版本， 该参数用千指定用来比较元素的函数。同</p>
<p>样， remove(）方法也有一个接受另一个参数的版本， 该参数用于指定用来确定是否删除元素的函数。这些参数都是谓词函数，将稍后</p>
<p>介绍。</p>
<h2 id="forward_-list-c11"><a class="markdownIt-Anchor" href="#forward_-list-c11"></a> forward_ list (C++11)</h2>
<p>C++ll新增了容器类forward_list,它实现了单链表。在这种链表中，每个节点都只链接到下一个节点，而没有链接到前一个节点。因此</p>
<p>forward—list 只需要正向迭代器， 而不需要双向迭代器。因此，不同千vector和list, forward <em>list是不可反转的容器。相比千list, forward</em></p>
<p>list 更简单、更紧凑， 但功能也更少。</p>
<h2 id="queue"><a class="markdownIt-Anchor" href="#queue"></a> queue</h2>
<p>queue模板类（在头文件queue（以前为queue.h) 中声明） 是一个适配器类。由前所述，ostream _iterator模板就是一个适配器，让输</p>
<p>出流能够使用迭代器接口。同样， queue模板让底层类（默认为deque)展示典型的队列接口。queue 模板的限制比deque 更多。它不仅</p>
<p>不允许随机访问队列元素， 甚至不允许遍历队列。它把使用限制在定义队列的基本操作上，可以将元素添加到队尾、从队首删除元素、</p>
<p>查看队首和队尾的值、检查元素数目和测试队列是否为空。表16.10列出了这些操作。注意， pop( ）是一个删除数据的方法， 而不是检</p>
<p>索数据的方法。如果要使用队列中的值， 应首先使用front(）来检索这个值， 然后使用pop(）将它从队列中删除。</p>
<p><img src="/2021/05/12/%E5%AE%B9%E5%99%A8%E7%9A%84%E7%B1%BB%E5%9E%8B/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Cqueue%E6%93%8D%E4%BD%9C.png" alt="queue操作"></p>
<h2 id="priority_-queue"><a class="markdownIt-Anchor" href="#priority_-queue"></a> priority_ queue</h2>
<p>priority_ queue模板类（在queue 头文件中声明） 是另一个适配器类，它支持的操作与queue相同。两者之间的主要区别在于，在priority_queue中，最大的元素被移到队首（生活不总是公平的， 队列也一样）。内部区别在于，默认的底层类是vector。可以修改用于确定哪个元素放到队首的比较方式，方法是提供一个可选的构造函数参数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">priority_queue&lt;<span class="keyword">int</span>&gt; pql; <span class="comment">// default version</span></span><br><span class="line"><span class="function">priority_queue&lt;<span class="keyword">int</span>&gt; <span class="title">pq2</span><span class="params">(greater&lt;<span class="keyword">int</span>&gt;&#125;; <span class="comment">// use greater&lt;in巨to order</span></span></span></span><br></pre></td></tr></table></figure>
<p>greater&lt;＞(）函数是一个预定义的函数对象， 本章稍后将讨论它。</p>
<h2 id="stack"><a class="markdownIt-Anchor" href="#stack"></a> stack</h2>
<p>与queue相似， stack（在头文件stack 以前为stack.h——中声明）也是一个适配器类，它给底层类（默认情况下为vector)提供了典型的</p>
<p>栈接口。stack 模板的限制比vector更多。它不仅不允许随机访问栈元素，甚至不允许遍历栈。它把使用限制在定义栈的基本操作上， 即</p>
<p>可以将压入推到栈顶、从栈顶弹出元素、查看栈顶的值、检查元素数目和测试栈是否为空。表16.11列出了这些操作。</p>
<p><img src="/2021/05/12/%E5%AE%B9%E5%99%A8%E7%9A%84%E7%B1%BB%E5%9E%8B/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Cstack%E7%9A%84%E6%93%8D%E4%BD%9C.png" alt="stack的操作"></p>
<p>与queue相似， 如果要使用栈中的值， 必须首先使用top(）来检索这个值， 然后使用pop(）将它从栈中删除。</p>
<h2 id="array"><a class="markdownIt-Anchor" href="#array"></a> array</h2>
<p>第4章介绍过，模板类array是否头文件array中定义的，它并非STL容器， 因为其长度是固定的。因此，array没有定义调整容器大小的操</p>
<p>作，如push_back(）和insert(），但定义了对它来说有意义的成员函数，如operator D(）和at(）。可将很多标准STL算法用千array对</p>
<p>象， 如copy(）和for_each(）。</p>
<h1 id="关联容器"><a class="markdownIt-Anchor" href="#关联容器"></a> 关联容器</h1>
<p>关联容器是将值和键关联</p>
<p><code>set multiset map multimap</code></p>
<p>前两种在set头文件，后两种在map</p>
]]></content>
  </entry>
  <entry>
    <title>嵌套类</title>
    <url>/2021/05/12/%E5%B5%8C%E5%A5%97%E7%B1%BB/</url>
    <content><![CDATA[<h1 id="嵌套类"><a class="markdownIt-Anchor" href="#嵌套类"></a> 嵌套类</h1>
<table>
<thead>
<tr>
<th>声明位置</th>
<th>包含它的类是否可以使用它</th>
<th>从包含它的类派生而来的类是否可以使用它</th>
<th>在外部是否可以使用</th>
</tr>
</thead>
<tbody>
<tr>
<td>私有部分</td>
<td>是</td>
<td>否</td>
<td>否</td>
</tr>
<tr>
<td>保护部分</td>
<td>是</td>
<td>是</td>
<td>否</td>
</tr>
<tr>
<td>公有部分</td>
<td>是</td>
<td>是</td>
<td>是，通过<code>::</code>使用</td>
</tr>
</tbody>
</table>
]]></content>
  </entry>
  <entry>
    <title>必须使用成员初始化列表的几种情况</title>
    <url>/2021/05/12/%E5%BF%85%E9%A1%BB%E4%BD%BF%E7%94%A8%E6%88%90%E5%91%98%E5%88%9D%E5%A7%8B%E5%8C%96%E5%88%97%E8%A1%A8%E7%9A%84%E5%87%A0%E7%A7%8D%E6%83%85%E5%86%B5/</url>
    <content><![CDATA[<p><strong>1、类成员为const类型</strong></p>
<p><strong>2、类成员为引用类型</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">A</span>(<span class="keyword">int</span> &amp;v) : <span class="built_in">i</span>(v), <span class="built_in">p</span>(v), <span class="built_in">j</span>(v) &#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">print_val</span><span class="params">()</span> </span>&#123; cout &lt;&lt; <span class="string">&quot;hello:&quot;</span> &lt;&lt; i &lt;&lt; <span class="string">&quot;  &quot;</span> &lt;&lt; j &lt;&lt; endl; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> i;<span class="comment">//类成员为const类型</span></span><br><span class="line">    <span class="keyword">int</span> p;</span><br><span class="line">    <span class="keyword">int</span> &amp;j;<span class="comment">//类成员为引用类型</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pp = <span class="number">45</span>;</span><br><span class="line">    <span class="function">A <span class="title">b</span><span class="params">(pp)</span></span>;</span><br><span class="line">    b.<span class="built_in">print_val</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>究其因</strong></p>
<p>const对象或引用只能初始化但是不能赋值。构造函数的函数体内只能赋值而不是初始化，因此初始化const对象或引用的唯一机会是构造函数函数体之前的初始化列表中。</p>
<p><strong>从无到有叫初始化，初始化（调用拷贝构造函数）创建了新对象；赋值（调用赋值操作符）没有创建新对象，而是对已有的对象赋值</strong>。</p>
<p><strong>3、类成员为没有默认构造函数的类类型</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"> <span class="number">1</span> <span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> <span class="number">2</span> <span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"> <span class="number">3</span> </span><br><span class="line"> <span class="number">4</span> <span class="class"><span class="keyword">class</span> <span class="title">Base</span></span></span><br><span class="line"><span class="class"> 5 &#123;</span></span><br><span class="line"> <span class="number">6</span> <span class="keyword">public</span>:</span><br><span class="line"> <span class="number">7</span>     <span class="built_in">Base</span>(<span class="keyword">int</span> a) : <span class="built_in">val</span>(a) &#123;&#125;</span><br><span class="line"> <span class="number">8</span> <span class="keyword">private</span>:</span><br><span class="line"> <span class="number">9</span>     <span class="keyword">int</span> val;</span><br><span class="line"><span class="number">10</span> &#125;;</span><br><span class="line"><span class="number">11</span> </span><br><span class="line"><span class="number">12</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">13 &#123;</span></span><br><span class="line"><span class="number">14</span> <span class="keyword">public</span>:</span><br><span class="line"><span class="number">15</span>     <span class="built_in">A</span>(<span class="keyword">int</span> v) : <span class="built_in">p</span>(v), <span class="built_in">b</span>(v) &#123;&#125;</span><br><span class="line"><span class="number">16</span>     <span class="function"><span class="keyword">void</span> <span class="title">print_val</span><span class="params">()</span> </span>&#123; cout &lt;&lt; <span class="string">&quot;hello:&quot;</span> &lt;&lt; p &lt;&lt; endl; &#125;</span><br><span class="line"><span class="number">17</span> <span class="keyword">private</span>:</span><br><span class="line"><span class="number">18</span>     <span class="keyword">int</span> p;</span><br><span class="line"><span class="number">19</span>     Base b;<span class="comment">//类成员为没有默认构造函数的类</span></span><br><span class="line"><span class="number">20</span> &#125;;</span><br><span class="line"><span class="number">21</span> </span><br><span class="line"><span class="number">22</span> <span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function">23 </span>&#123;</span><br><span class="line"><span class="number">24</span>     <span class="keyword">int</span> pp = <span class="number">45</span>;</span><br><span class="line"><span class="number">25</span>     <span class="function">A <span class="title">b</span><span class="params">(pp)</span></span>;</span><br><span class="line"><span class="number">26</span>     b.<span class="built_in">print_val</span>();</span><br><span class="line"><span class="number">27</span> &#125;</span><br></pre></td></tr></table></figure>
<p>原因同样是创建对象时，要初始类成员的每一个成员</p>
<p><strong>4、如果类存在继承关系，派生类必须在其初始化列表中调用基类的构造函数</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"> <span class="number">1</span> <span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"> <span class="number">2</span> <span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"> <span class="number">3</span> </span><br><span class="line"> <span class="number">4</span> <span class="class"><span class="keyword">class</span> <span class="title">Base</span></span></span><br><span class="line"><span class="class"> 5 &#123;</span></span><br><span class="line"> <span class="number">6</span> <span class="keyword">public</span>:</span><br><span class="line"> <span class="number">7</span>     <span class="built_in">Base</span>(<span class="keyword">int</span> a) : <span class="built_in">val</span>(a) &#123;&#125;</span><br><span class="line"> <span class="number">8</span> <span class="keyword">private</span>:</span><br><span class="line"> <span class="number">9</span>     <span class="keyword">int</span> val;</span><br><span class="line"><span class="number">10</span> &#125;;</span><br><span class="line"><span class="number">11</span> </span><br><span class="line"><span class="number">12</span> <span class="class"><span class="keyword">class</span> <span class="title">A</span> :</span> <span class="keyword">public</span> Base</span><br><span class="line"><span class="number">13</span> &#123;</span><br><span class="line"><span class="number">14</span> <span class="keyword">public</span>:</span><br><span class="line"><span class="number">15</span>     <span class="built_in">A</span>(<span class="keyword">int</span> v) : <span class="built_in">p</span>(v), <span class="built_in">Base</span>(v) &#123;&#125;<span class="comment">//类A继承自类Base，需在其初始化列表中调用Base的构造函数</span></span><br><span class="line"><span class="number">16</span>     <span class="function"><span class="keyword">void</span> <span class="title">print_val</span><span class="params">()</span> </span>&#123; cout &lt;&lt; <span class="string">&quot;hello:&quot;</span> &lt;&lt; p &lt;&lt; endl; &#125;</span><br><span class="line"><span class="number">17</span> <span class="keyword">private</span>:</span><br><span class="line"><span class="number">18</span>     <span class="keyword">int</span> p;</span><br><span class="line"><span class="number">19</span> &#125;;</span><br><span class="line"><span class="number">20</span> </span><br><span class="line"><span class="number">21</span> <span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function">22 </span>&#123;</span><br><span class="line"><span class="number">23</span>     <span class="keyword">int</span> pp = <span class="number">45</span>;</span><br><span class="line"><span class="number">24</span>     <span class="function">A <span class="title">b</span><span class="params">(pp)</span></span>;</span><br><span class="line"><span class="number">25</span>     b.<span class="built_in">print_val</span>();</span><br><span class="line"><span class="number">26</span> &#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>异常</title>
    <url>/2021/05/12/%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<h1 id="异常"><a class="markdownIt-Anchor" href="#异常"></a> 异常</h1>
<p>​</p>
<h2 id="try和catch"><a class="markdownIt-Anchor" href="#try和catch"></a> try和catch</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span></span><br><span class="line">&#123;</span><br><span class="line">    ····;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in"><span class="keyword">catch</span></span>(typename1 &amp; tyn1)</span><br><span class="line">&#123;</span><br><span class="line">    ····;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in"><span class="keyword">catch</span></span>(typename2 &amp; tyn2)</span><br><span class="line">&#123;</span><br><span class="line">    ····;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in"><span class="keyword">catch</span></span>(typename3 &amp; tyn3)</span><br><span class="line">&#123;</span><br><span class="line">    ····;</span><br><span class="line">&#125;</span><br><span class="line">....;</span><br><span class="line"><span class="built_in"><span class="keyword">catch</span></span>(...) <span class="comment">//省略号代表即使不知道的异常类型也能捕捉</span></span><br><span class="line">&#123;</span><br><span class="line">    ····;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="异常类型"><a class="markdownIt-Anchor" href="#异常类型"></a> 异常类型</h2>
<h3 id="定义异常类型"><a class="markdownIt-Anchor" href="#定义异常类型"></a> 定义异常类型</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">bad1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    ····;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    ····;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="抛出异常throw"><a class="markdownIt-Anchor" href="#抛出异常throw"></a> 抛出异常throw</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">A</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> 	<span class="keyword">if</span>(a!=b)</span><br><span class="line">        <span class="function">bad1 <span class="title">b</span><span class="params">()</span></span>;</span><br><span class="line">        <span class="keyword">throw</span> b; <span class="comment">//或者throw bad1();throw的东西必须是个对象，bad1()隐式的的创建了一个对象</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1.0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>throw会立刻终止执行下面的程序，并执行栈解退，将控制权向上传递，并且上一个函数如果有catch则会捕获这一异常。特别的是栈解退的如果是类对象将会为该对象调用析构函数。</strong></p>
<h3 id="捕获异常catch"><a class="markdownIt-Anchor" href="#捕获异常catch"></a> 捕获异常catch</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in"><span class="keyword">catch</span></span>(bad1 &amp; b1)</span><br><span class="line">&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;····异常&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>注意</strong>b1指向的是的是b的<strong>拷贝副本</strong>并不是指向b本身。</p>
<h2 id="基类和异常类的捕捉顺序"><a class="markdownIt-Anchor" href="#基类和异常类的捕捉顺序"></a> 基类和异常类的捕捉顺序</h2>
<p>当使用catch来捕获基类的异常类型，这时如果有抛出派生类型的错误，catch也将会捕获。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//bad.h</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">bad1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    .....;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">bad2</span> :</span> <span class="keyword">public</span> bad1</span><br><span class="line">&#123;</span><br><span class="line">    .....;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">bad3</span> :</span> <span class="keyword">public</span> bad2</span><br><span class="line">&#123;</span><br><span class="line">    ....;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;bad.h&quot;</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bad</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a, b;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="comment">//捕捉异常</span></span><br><span class="line">    <span class="keyword">try</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">bad</span>(a, b);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in"><span class="keyword">catch</span></span>(bad3) <span class="comment">//如果第一个捕捉的异常是基类那他将捕捉由他派生的所有类，所以异常的捕获顺序是倒序。</span></span><br><span class="line">    &#123;</span><br><span class="line">        ...;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in"><span class="keyword">catch</span></span>(bad2)</span><br><span class="line">    &#123;</span><br><span class="line">        ....;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in"><span class="keyword">catch</span></span>(bad1)</span><br><span class="line">    &#123;</span><br><span class="line">        ....;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    return0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bad</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(a = b)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">bad1</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(a &lt; b)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">thorw <span class="title">bad2</span><span class="params">()</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(a &gt; b)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">bad3</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="exception类"><a class="markdownIt-Anchor" href="#exception类"></a> exception类</h2>
<p>异常的基类</p>
<p><code>#include&lt;exception&gt;</code></p>
<h2 id="stdexcept类"><a class="markdownIt-Anchor" href="#stdexcept类"></a> stdexcept类</h2>
<p>该类含有<code>logic_error</code>和<code>runtime_error</code></p>
<p>logic_error表明存在可以通过编程修复的问题</p>
<p>runtime_error表明存在无法避免的问题</p>
<p><strong>stdexcept是由exception类派生的，捕捉异常时主义注意派生和基类的顺序</strong></p>
<h2 id="bad_alloc和new"><a class="markdownIt-Anchor" href="#bad_alloc和new"></a> bad_alloc和new</h2>
<p>使用new导致的内存分配问题，C++的最新处理方式是让new引发bad_alloc异常。头文件new包含bad_alloc类的声明，他也是从exception类公有派生的。</p>
<h2 id="空指针和new"><a class="markdownIt-Anchor" href="#空指针和new"></a> 空指针和new</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> * pi = <span class="built_in"><span class="keyword">new</span></span>(std::nothrow) <span class="keyword">int</span>;</span><br><span class="line"><span class="keyword">int</span> * pa = <span class="built_in"><span class="keyword">new</span></span>(std::nowthrow) <span class="keyword">int</span>[<span class="number">500</span>];</span><br></pre></td></tr></table></figure>
<p><code>std::nothrow</code>和<code>std::nowthrow</code>是一个标记（开关），可以在new失败时返回一个空指针。</p>
<h2 id="注意事项"><a class="markdownIt-Anchor" href="#注意事项"></a> 注意事项</h2>
<p>异常和动态分配并不是能很好的配合</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test1</span><span class="params">(<span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">string <span class="title">mesg</span><span class="params">(<span class="string">&quot;awdawdaw&quot;</span>)</span></span>;</span><br><span class="line">    ....;</span><br><span class="line">    <span class="keyword">if</span>(oh_no)</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">exception</span>();</span><br><span class="line">    ....;</span><br><span class="line">    retun;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当程序执行到thorw时程序会终止，然而程序还是会调用string的析构函数，应为程序执行了栈解退。</p>
<p><strong>但是</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void test1(int n)</span><br><span class="line">&#123;</span><br><span class="line">    double * ar = new double[10];</span><br><span class="line">    ....;</span><br><span class="line">    if(oh_no)</span><br><span class="line">        throw exception();</span><br><span class="line">    ....;</span><br><span class="line">    delete [] ar;</span><br><span class="line">    retun;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当程序执行到throw时程序终止，delete语句不被执行这时指针还将会在内存中造成内存泄露。</p>
<p><strong>这种情况可以避免</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test1</span><span class="params">(<span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">double</span> * ar = <span class="keyword">new</span> <span class="keyword">double</span>[<span class="number">10</span>];</span><br><span class="line">    ....;</span><br><span class="line">    <span class="keyword">try</span></span><br><span class="line">    &#123;<span class="keyword">if</span>(oh_no)</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">exception</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in"><span class="keyword">catch</span></span>(expetion &amp;ex)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">delete</span> [] ar;</span><br><span class="line">        <span class="keyword">throw</span>; <span class="comment">//将该次错误再次抛出到外面的函数中</span></span><br><span class="line">    &#125;</span><br><span class="line">    retun;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>抽象基类(ABC编程)</title>
    <url>/2021/05/12/%E6%8A%BD%E8%B1%A1%E5%9F%BA%E7%B1%BB(ABC%E7%BC%96%E7%A8%8B)/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>指针</title>
    <url>/2021/05/12/%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<blockquote>
<p>更新中···········</p>
</blockquote>
<h1 id="指针"><a class="markdownIt-Anchor" href="#指针"></a> 指针</h1>
<h2 id="指针的声明"><a class="markdownIt-Anchor" href="#指针的声明"></a> 指针的声明</h2>
<p><strong>格式：<code>typename * pointerName</code></strong></p>
<p>指针可以理解为一个复合类型，<code>pointName</code>是一个指向<code>typeName</code>类型的指针。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>* p;<span class="comment">//指向int类型的指针</span></span><br><span class="line"><span class="keyword">double</span>* p;<span class="comment">//指向double类型的指针</span></span><br><span class="line"><span class="keyword">char</span>* p[<span class="number">20</span>];<span class="comment">//指向char类型的指针数组</span></span><br></pre></td></tr></table></figure>
<h3 id="指针的用法"><a class="markdownIt-Anchor" href="#指针的用法"></a> 指针的用法</h3>
<h4 id="1给指针赋值"><a class="markdownIt-Anchor" href="#1给指针赋值"></a> 1.给指针赋值</h4>
<p>指针也是有值的，值是一个十六进制的内存地址。给指针赋值等号的右边必须也是一个返回地址的对象。</p>
<h5 id="用取内存"><a class="markdownIt-Anchor" href="#用取内存"></a> 用&amp;取内存</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a;</span><br><span class="line"><span class="keyword">int</span>* p;</span><br><span class="line">p = &amp;a;</span><br></pre></td></tr></table></figure>
<h5 id="把指针赋给指针"><a class="markdownIt-Anchor" href="#把指针赋给指针"></a> 把指针赋给指针</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a;</span><br><span class="line"><span class="keyword">int</span>* p1;</span><br><span class="line"><span class="keyword">int</span>* p2;</span><br><span class="line">p1 = &amp;a;</span><br><span class="line">p2 = p1;</span><br></pre></td></tr></table></figure>
<h5 id="把数组名字赋给指针"><a class="markdownIt-Anchor" href="#把数组名字赋给指针"></a> 把数组名字赋给指针</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a[<span class="number">20</span>];</span><br><span class="line"><span class="keyword">int</span>* p</span><br><span class="line">p = a;<span class="comment">//seizeof)(*p) = 4（一个int的大小） sizeof(a) = 80(int的字节x20)。ps：不同位数的操作系统字节数不一样</span></span><br></pre></td></tr></table></figure>
<p>其实数组名在做右值时是一个常量指针，它指向数组第一个元素。但它与普通指针有所不同，<strong>实际是</strong><code>int* const a;</code>因此，<strong>数组名在做左值时是不可以被修改的</strong>。还有一点特殊的是用<code>sizeof(a)</code>来判断长度时返回的不是第一个元素的字节数，而是<strong>整个</strong>数组的字节数，但是<code>sizeog(*p)</code>返回的却只是这个指针本身的大小 ，<code>sizeof(p)</code>是一个字节的大小(32位是4，64位是8）。</p>
<p>    1. 以下是可行的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a[<span class="number">20</span>] &#123;<span class="number">2</span>,<span class="number">3</span>,<span class="number">8</span>,<span class="number">9</span>&#125;;</span><br><span class="line">a + <span class="number">1</span>;<span class="comment">//表达式，右值可以选择赋给一个变量例如：b = a + 1</span></span><br><span class="line"><span class="comment">//其中a + 1：数组名+1表示内存+4（一个int的长度），它指向数组的第二个元素，因此对它解引用(a+1)[0] = 2</span></span><br></pre></td></tr></table></figure>
<p>    2. 以下是不可行的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">a++;  </span><br><span class="line">a = a + <span class="number">3</span>;<span class="comment">//不可行，应为a是const不可被修改</span></span><br></pre></td></tr></table></figure>
<h5 id="用new给指针分配地址n"><a class="markdownIt-Anchor" href="#用new给指针分配地址n"></a> 用new给指针分配地址N</h5>
<p><strong>格式：<code>pointName = new typeName</code></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>* p = <span class="keyword">new</span> <span class="keyword">int</span>;<span class="comment">//给int类型对象分配内存并返回地址</span></span><br><span class="line"><span class="keyword">delete</span> p;</span><br><span class="line"><span class="keyword">int</span>* pa = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">20</span>];<span class="comment">//给int数组类型对象分配内存并返回地址</span></span><br><span class="line"><span class="keyword">delete</span> [] pa;</span><br></pre></td></tr></table></figure>
<p>其中注意用完new创建的对象后，<strong>要用delete释放内存，其中数组加[]。</strong></p>
<h4 id="2指针的解引用"><a class="markdownIt-Anchor" href="#2指针的解引用"></a> 2.指针的解引用</h4>
<h5 id="用操作符"><a class="markdownIt-Anchor" href="#用操作符"></a> 用*操作符</h5>
<p>解引用的指针是指针指向的值。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">int</span>* p = &amp;a;</span><br><span class="line"><span class="comment">//*p的值是5</span></span><br></pre></td></tr></table></figure>
<h5 id="用数组表示法"><a class="markdownIt-Anchor" href="#用数组表示法"></a> 用数组表示法</h5>
<p>*p等价于p[0]</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">int</span>* p = &amp;a;</span><br><span class="line"><span class="comment">//p[0]的值是5</span></span><br></pre></td></tr></table></figure>
<h4 id="3指针算术"><a class="markdownIt-Anchor" href="#3指针算术"></a> 3.指针算术</h4>
<p>c++允许指针与整数相加减，指针的值加1表示地址值加上指针类型的字节数。也可以将两个指针相减得出两个元素的间隔。</p>
<h2 id="-code9-"><a class="markdownIt-Anchor" href="#-code9-"></a> <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> tacos[<span class="number">10</span>] &#123;<span class="number">5</span>,<span class="number">2</span>,<span class="number">8</span>,<span class="number">4</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">4</span>&#125;;</span><br><span class="line"><span class="keyword">int</span>* p = tacos;</span><br><span class="line">p = p + <span class="number">1</span>;<span class="comment">//此时p指向的是2</span></span><br><span class="line"><span class="keyword">int</span>* pa = &amp;tacos[<span class="number">9</span>];</span><br><span class="line">pa - p;<span class="comment">//结果是8，tatcos[8] - tatcos[1]</span></span><br></pre></td></tr></table></figure></h2>
<h1 id="指针-2"><a class="markdownIt-Anchor" href="#指针-2"></a> 指针++</h1>
<h4 id="1数组表示法和指针表示法的关系"><a class="markdownIt-Anchor" href="#1数组表示法和指针表示法的关系"></a> 1.数组表示法和指针表示法的关系</h4>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> a[<span class="number">10</span>] &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;; </span><br><span class="line"><span class="keyword">int</span>* p = a;</span><br><span class="line">a[<span class="number">0</span>];<span class="comment">//值为1</span></span><br><span class="line">p[<span class="number">0</span>];<span class="comment">//值为1</span></span><br><span class="line">p[<span class="number">2</span>];<span class="comment">//值为3</span></span><br></pre></td></tr></table></figure>
<p>指向数组的指针可以直接加方括号索引等效于数组名加索引，p[n]等效于*(a + n)。</p>
<h4 id="2指针和字符串"><a class="markdownIt-Anchor" href="#2指针和字符串"></a> 2.指针和字符串</h4>
<p>字符和字符串是两个不同的类型，字符是<code>char</code>类型，字符串是<code>const char*</code>类型，所以字符串要赋值的话要用<code>char*</code>类型或者<code>const char*</code>类型。(注意：<code>const char*</code>类型实际不可以直接赋值给<code>char*</code>，而字符串可以这样是为了兼容C)C++中建议尽量不要直接将字符串赋给<code>char*</code>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;apple&quot;</span><span class="comment">//这是一个字符串，字面值是apple而实际是一个const char*类型的指针。</span></span><br><span class="line"><span class="keyword">char</span>* p;</span><br><span class="line">p = <span class="string">&quot;apple&quot;</span>;<span class="comment">//可行，兼容C.但不建议</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* pa;</span><br><span class="line">pa = <span class="string">&quot;pandas&quot;</span>; <span class="comment">// *p: p</span></span><br><span class="line"><span class="comment">//p实际上是可以被修改</span></span><br><span class="line">*<span class="string">&quot;pandas&quot;</span> <span class="comment">// p C++将字符串解释为其第一个元素的地址</span></span><br></pre></td></tr></table></figure>
<p>C++中字符串尽量用<code>string</code>类来表示。</p>
<h4 id="3函数指针"><a class="markdownIt-Anchor" href="#3函数指针"></a> 3.函数指针</h4>
<h5 id="格式"><a class="markdownIt-Anchor" href="#格式"></a> 格式</h5>
<p><code>typeName (*pointName)(parameterType)</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">pam</span><span class="params">(<span class="keyword">int</span>)</span></span>; <span class="comment">//函数原型</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> *<span class="title">pf</span><span class="params">(<span class="keyword">int</span>)</span> <span class="comment">//返回doule类型指针的函数</span></span></span><br><span class="line"><span class="function"><span class="title">double</span> <span class="params">(*pf)</span><span class="params">(<span class="keyword">int</span>)</span> <span class="comment">//指向函数的指针</span></span></span><br><span class="line"><span class="function">pf </span>= pam <span class="comment">//函数名是地址</span></span><br><span class="line"><span class="built_in"><span class="keyword">double</span></span> (*p[<span class="number">3</span>])(<span class="keyword">int</span>); <span class="comment">//声明一个指针数组，注意（）。括号的优先级比*高</span></span><br></pre></td></tr></table></figure>
<h5 id="函数指针的调用"><a class="markdownIt-Anchor" href="#函数指针的调用"></a> 函数指针的调用</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">pam</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">es</span><span class="params">(<span class="keyword">int</span>, <span class="keyword">double</span> (*p)(<span class="keyword">int</span>))</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in"><span class="keyword">double</span></span> (*p)(<span class="keyword">int</span>);</span><br><span class="line">    <span class="built_in">es</span>(<span class="number">6</span>, pam);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">pam</span><span class="params">(<span class="keyword">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;num = &quot;</span> &lt;&lt; a &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">es</span><span class="params">(<span class="keyword">int</span> b, <span class="keyword">double</span> (*p)(<span class="keyword">int</span>))</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    (*p)(<span class="number">5</span>); <span class="comment">//这种调用方法很好理解</span></span><br><span class="line">    <span class="built_in">p</span>(<span class="number">5</span>); <span class="comment">//C++也允许直接用指针名字加（）调用函数，不同流派的矛盾而已</span></span><br><span class="line">    <span class="built_in">pam</span>(<span class="number">5</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="指针的其他注意事项"><a class="markdownIt-Anchor" href="#指针的其他注意事项"></a> 指针的其他注意事项</h1>
<h2 id="指针与strcpy"><a class="markdownIt-Anchor" href="#指针与strcpy"></a> 指针与<code>strcpy()</code></h2>
<p>错误代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> * p;</span><br><span class="line"><span class="built_in">strcpy</span>(p, <span class="string">&quot;hello&quot;</span>);<span class="comment">//错误</span></span><br></pre></td></tr></table></figure>
<p><code>strcpy()</code>使用的时候第一个参数的范围要大于第二个参数的范围，否则内存将会溢出。p只是一个指针它的长度仅仅是它自己的长度，而hello长度是6，所以编译器会报错。</p>
<p>正确代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> p[<span class="number">50</span>];<span class="comment">//或者 char *p = new char[50];</span></span><br><span class="line"><span class="built_in">strcpy</span>(p, <span class="string">&quot;hello&quot;</span>);<span class="comment">//正确</span></span><br></pre></td></tr></table></figure>
<p><strong>一定要注意第一个参数的范围要大于第二个参数的范围</strong></p>
]]></content>
  </entry>
  <entry>
    <title>条件判断</title>
    <url>/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/</url>
    <content><![CDATA[<h1 id="条件判断"><a class="markdownIt-Anchor" href="#条件判断"></a> 条件判断</h1>
<h2 id="按文件类型判断"><a class="markdownIt-Anchor" href="#按文件类型判断"></a> 按文件类型判断</h2>
<p><img src="/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E6%96%87%E4%BB%B6%E7%B1%BB%E5%9E%8B%E5%88%A4%E6%96%AD.png" alt></p>
<h2 id="按文件权限判断"><a class="markdownIt-Anchor" href="#按文件权限判断"></a> 按文件权限判断</h2>
<p><img src="/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E6%96%87%E4%BB%B6%E6%9D%83%E9%99%90%E5%88%A4%E6%96%AD.png" alt></p>
<h2 id="两个文件之间判断"><a class="markdownIt-Anchor" href="#两个文件之间判断"></a> 两个文件之间判断</h2>
<p><img src="/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E4%B8%A4%E4%B8%AA%E6%96%87%E4%BB%B6%E4%B9%8B%E9%97%B4%E8%BF%9B%E8%A1%8C%E6%AF%94%E8%BE%83.png" alt></p>
<h2 id="两个整数比较"><a class="markdownIt-Anchor" href="#两个整数比较"></a> 两个整数比较</h2>
<p><img src="/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E4%B8%A4%E4%B8%AA%E6%95%B4%E6%95%B0%E4%B9%8B%E9%97%B4%E6%AF%94%E8%BE%83.png" alt></p>
<h2 id="字符串的判断"><a class="markdownIt-Anchor" href="#字符串的判断"></a> 字符串的判断</h2>
<p><img src="/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%88%A4%E6%96%AD.png" alt></p>
<h2 id="多重条件判断"><a class="markdownIt-Anchor" href="#多重条件判断"></a> 多重条件判断</h2>
<p><img src="/2021/05/12/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%A4%9A%E9%87%8D%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD.png" alt></p>
<h2 id="判断"><a class="markdownIt-Anchor" href="#判断"></a> 判断</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">test</span> -e /root/install.log</span><br><span class="line">[ -e /root/install/<span class="built_in">log</span> ]</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[ -d /root ] &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;yes&quot;</span> || <span class="built_in">echo</span> <span class="string">&quot;no&quot;</span> <span class="comment">#将判断的结果输出</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>格式化</title>
    <url>/2021/05/12/%E6%A0%BC%E5%BC%8F%E5%8C%96/</url>
    <content><![CDATA[<h1 id="格式化"><a class="markdownIt-Anchor" href="#格式化"></a> 格式化</h1>
<h2 id="dec-hex-oct"><a class="markdownIt-Anchor" href="#dec-hex-oct"></a> <code>dec hex oct</code></h2>
<p>iostream是由ios类派生，ios类是由ios_base类派生，而dec hex oct控制符位于ios_base里，对他的派生类iostream同样适用</p>
<p>dec：十进制</p>
<p>hex：十六进制</p>
<p>oct：八进制</p>
<p>使用hex(cout)但是因为iostream下面的ostream重载了&lt;&lt;符号所以也可以cout &lt;&lt; hex;</p>
<h2 id="宽度"><a class="markdownIt-Anchor" href="#宽度"></a> 宽度</h2>
<p>原型</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">width</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">width</span><span class="params">(<span class="keyword">int</span> i)</span></span>;</span><br></pre></td></tr></table></figure>
<p>第一个返回字段宽度的当前设置</p>
<p>第二种将字符卷度设置为i个空格，并返回以前的字段宽度</p>
<p><strong>注意：这个函数只影响下一个语句</strong></p>
<h2 id="精度"><a class="markdownIt-Anchor" href="#精度"></a> 精度</h2>
<p><code>precision(int i)</code></p>
<p>i表示精度值</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">cout.<span class="built_in">precision</span>(<span class="number">2</span>);</span><br></pre></td></tr></table></figure>
<h2 id="保留末尾"><a class="markdownIt-Anchor" href="#保留末尾"></a> 保留末尾</h2>
<p><code>ios_base::showpoint</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">std::cout.<span class="built_in">setf</span>(ios_base::showpoint);</span><br></pre></td></tr></table></figure>
<h2 id="setf"><a class="markdownIt-Anchor" href="#setf"></a> <code>setf()</code></h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">fmtflags <span class="title">setf</span><span class="params">(fmtflags)</span></span>;<span class="comment">//#1</span></span><br></pre></td></tr></table></figure>
<p>fmtflags是bitmask类型的typdef名称该名称是在ios_base中定义的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">fmtflags <span class="title">setf</span><span class="params">(fmtflags, fmtflags)</span></span>;<span class="comment">//#2</span></span><br></pre></td></tr></table></figure>
<p>第二个接受两个参数并返回以前的设置，第一个就是需要要设置的，第二个参数是要清除第一个参数的那些位</p>
]]></content>
  </entry>
  <entry>
    <title>检测输入合法</title>
    <url>/2021/05/12/%E6%A3%80%E6%B5%8B%E8%BE%93%E5%85%A5%E5%90%88%E6%B3%95/</url>
    <content><![CDATA[<h1 id="检测输入合法"><a class="markdownIt-Anchor" href="#检测输入合法"></a> 检测输入合法</h1>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> factor;</span><br><span class="line">        <span class="keyword">while</span>(!(cin &gt;&gt; factor))</span><br><span class="line">        &#123;</span><br><span class="line">            cin.<span class="built_in">clear</span>();</span><br><span class="line">            <span class="keyword">while</span>(cin.<span class="built_in">get</span>() != <span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;Bad input; Please enter a number: &quot;</span>;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title>模板函数和模板类“坑”</title>
    <url>/2021/05/12/%E6%A8%A1%E6%9D%BF%E5%87%BD%E6%95%B0%E5%92%8C%E6%A8%A1%E6%9D%BF%E7%B1%BB%E2%80%9C%E5%9D%91%E2%80%9D/</url>
    <content><![CDATA[<h1 id="模板函数和模板类坑"><a class="markdownIt-Anchor" href="#模板函数和模板类坑"></a> 模板函数和模板类“坑”</h1>
<p>模板函数和模板类的函数定义<strong>一定一定要和头文件一起</strong>，不能一个头文件一个源文件</p>
]]></content>
  </entry>
  <entry>
    <title>模板派生类访问基类保护成员的方式</title>
    <url>/2021/05/12/%E6%A8%A1%E6%9D%BF%E6%B4%BE%E7%94%9F%E7%B1%BB%E8%AE%BF%E9%97%AE%E5%9F%BA%E7%B1%BB%E4%BF%9D%E6%8A%A4%E6%88%90%E5%91%98%E7%9A%84%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="模板派生类访问基类保护成员的方式"><a class="markdownIt-Anchor" href="#模板派生类访问基类保护成员的方式"></a> 模板派生类访问基类保护成员的方式</h1>
<p>一个模板基类派生出模板派生类</p>
<p>其中模板派生类访问基类的保护成员时需要加上类名</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>;</span><br><span class="line">    <span class="keyword">protected</span>:</span><br><span class="line">    T a;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">public</span> A&lt;T&gt;</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="built_in">B</span>() &#123;c = A&lt;T&gt;::a;&#125; <span class="comment">//这里必须加上限定符号</span></span><br><span class="line">    <span class="keyword">protected</span>:</span><br><span class="line">    T c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>如果需要过多类名可以用</strong></p>
<p><code>using A::a</code></p>
]]></content>
  </entry>
  <entry>
    <title>日志管理</title>
    <url>/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="日志管理"><a class="markdownIt-Anchor" href="#日志管理"></a> 日志管理</h1>
<h2 id="常见日志"><a class="markdownIt-Anchor" href="#常见日志"></a> 常见日志</h2>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%B8%B8%E8%A7%81%E6%97%A5%E5%BF%97.png" alt="常见日志"></p>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E5%B8%B8%E8%A7%81%E6%97%A5%E5%BF%972.png" alt="常见日志2"></p>
<p><strong>源码包安装的日志是在指定目录中，rpm包安装的在/var/log中</strong></p>
<h2 id="rsyslogd日志"><a class="markdownIt-Anchor" href="#rsyslogd日志"></a> rsyslogd日志</h2>
<h3 id="格式"><a class="markdownIt-Anchor" href="#格式"></a> 格式</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">时间	产生事件的服务器主机名	产生时间的服务名或程序名	事件的具体信息</span><br><span class="line">Sep 29 23:12:04 izbp1clp96b0qjo5whikc9z sshd[4630]: Failed password <span class="keyword">for</span> invalid user admin from 80.11.39.203 port 31491 ssh2</span><br></pre></td></tr></table></figure>
<h3 id="rsyslogd的配置文件"><a class="markdownIt-Anchor" href="#rsyslogd的配置文件"></a> rsyslogd的配置文件</h3>
<p><code>/etc/rsyslog.conf</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">服务名称[连接服务]日志等级				日志记录位置</span><br><span class="line">cron.*                                 /var/<span class="built_in">log</span>/cron</span><br></pre></td></tr></table></figure>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E6%97%A5%E5%BF%97%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6%E6%9C%8D%E5%8A%A1%E5%90%8D%E7%A7%B0.png" alt="日志配置文件服务名称"></p>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E6%97%A5%E5%BF%97%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6%E6%9C%8D%E5%8A%A1%E5%90%8D%E7%A7%B02.png" alt="日志配置文件服务名称2"></p>
<h3 id="连接符号"><a class="markdownIt-Anchor" href="#连接符号"></a> 连接符号</h3>
<p><code>*	.	.=	.!</code>	<br>
<img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E8%BF%9E%E6%8E%A5%E7%AC%A6%E5%8F%B7.png" alt="连接符号"></p>
<h3 id="日志等级"><a class="markdownIt-Anchor" href="#日志等级"></a> 日志等级</h3>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E6%97%A5%E5%BF%97%E7%AD%89%E7%BA%A7.png" alt="日志等级"></p>
<h3 id="日志记录位置"><a class="markdownIt-Anchor" href="#日志记录位置"></a> 日志记录位置</h3>
<ol>
<li>日志文件的绝对路径</li>
<li>系统设备文件</li>
<li>转发给远程主机</li>
<li>用户名，发给某个用户</li>
<li>忽略或丢掉日志</li>
</ol>
<h2 id="日志轮替"><a class="markdownIt-Anchor" href="#日志轮替"></a> 日志轮替</h2>
<p>轮替包含日志切割和旧日志删除</p>
<h3 id="日志命名"><a class="markdownIt-Anchor" href="#日志命名"></a> 日志命名</h3>
<ol>
<li>如果logrotate配置文件中拥有“dateext”参数，那么日志会用日期来作为日志文件的后缀，例如“secure-20130605”。这样的话日志文件名不会重叠，所以也就不需要日志文件的改名，只需要保存指定的日志个数，删除多余的日志文件即可。</li>
<li>如果配置文件中没有“dateext”参数，那么日志文件就需要进行改名了。当第一次进行日志轮替时，当前的“secure”日志会自动改名为<br>
“secure.1”，然后新建“secure”日志，用来保存新的日志。当第二次进行日志轮替时，“secure.1”会自动改名为“secure.2”，当前的“secure”日志会自动改名为“secure.1”，然后也会新建“secure”日志，用来保存新的日志，以此类推。</li>
</ol>
<h3 id="logrotate配置文件"><a class="markdownIt-Anchor" href="#logrotate配置文件"></a> logrotate配置文件</h3>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Clogrotate%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6.png" alt="logrotate配置文件"></p>
<p><img src="/2021/05/12/%E6%97%A5%E5%BF%97%E7%AE%A1%E7%90%86/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5Clogrotate%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B62.png" alt="logrotate配置文件2"></p>
<p><code>/etc/logrotate.conf</code></p>
<h3 id="加入轮替"><a class="markdownIt-Anchor" href="#加入轮替"></a> 加入轮替</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/<span class="built_in">local</span>/apache2/logs/access_log&#123;</span><br><span class="line">daily</span><br><span class="line">create</span><br><span class="line">rotate 30</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">logrotate [option] [配置文件名]</span><br><span class="line">		 -v：显示日志轮替过程</span><br><span class="line">		 -f：强制进行日志轮替，不管日志轮替条件是什么</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>正则表达式</title>
    <url>/2021/05/12/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="正则表达式"><a class="markdownIt-Anchor" href="#正则表达式"></a> 正则表达式</h1>
<p>\是转义符，以下命令是根据linux shell中格式写的</p>
<p><img src="/2021/05/12/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F.png" alt="正则表达式"></p>
]]></content>
  </entry>
  <entry>
    <title>泛型编程</title>
    <url>/2021/05/12/%E6%B3%9B%E5%9E%8B%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="迭代器"><a class="markdownIt-Anchor" href="#迭代器"></a> 迭代器</h1>
<h2 id="前缀后缀的区分"><a class="markdownIt-Anchor" href="#前缀后缀的区分"></a> 前缀后缀++的区分</h2>
<p>c<ins>为了区分前缀的</ins>和后缀的++</p>
<p>前缀++ operator++()</p>
<p>后缀++ operator++(int)</p>
<h2 id="迭代器的类型"><a class="markdownIt-Anchor" href="#迭代器的类型"></a> 迭代器的类型</h2>
<p><em><strong>*迭代器(iterator)*</strong></em> 是一种****指针类型****, 也分****const指针本身(地址)**** 和 ****const指针所指的值****, 两种情况;</p>
<p>但是写法和const内置指针有所不同;</p>
<p>****char * const 相当于 const container&lt;&gt;::iterator****; 可以修改指针所指的值, 但不能修改指针的地址;</p>
<p>****const char * 相当于 container&lt;&gt;::const_iterator****; 可以修改指针地址, 但不能修改指针所指的值;</p>
<p>注意代码示例, 两种const和迭代器;</p>
<h3 id="输入迭代器"><a class="markdownIt-Anchor" href="#输入迭代器"></a> 输入迭代器</h3>
<p>输入迭代器必须能够访问容器中所有的值， 这是通过支持＋＋运算符（前缀格式和后缀格式） 来实现的。如果将输入迭代器设置为指向</p>
<p>容器中的第一个元素， 并不断将其递增， 直到到达超尾位置， 则它将依次指向容器中的每一个元素。顺便说一句， 并不能保证输入迭代</p>
<p>器第二次遍历容器时， 顺序不变。 另 外， 输入迭代器被递增后， 也不能保证其先前的值仍然可以被解除引用。基千输入迭代器的任何</p>
<p>算法都 应当是单通行 (single-pass) 的， 不依赖千前一次遍历时的迭代器值， 也不依赖千本次遍历中前面的迭代器值。</p>
<p>输入迭代器是单项迭代器，可以递增但不可以倒退</p>
<h3 id="输出迭代器"><a class="markdownIt-Anchor" href="#输出迭代器"></a> 输出迭代器</h3>
<p>STL 使用术语 “ 输出” 来指用于将信息从程序传输给容器的迭代器，因此程序的输出就是容器的输入。</p>
<p>输出迭代器与输入迭代器相似， 只是解除引用让程序能修改容器值， 而不能读取。也许您会感到奇怪， 能够写， 却不能读。发送到显示</p>
<p>器上的输出就是如此， cout 可以修改发送到显示器的字符流， 却不能读取屏幕上的内容。 STL足够通用， 其容器可以表示输出设备， 因</p>
<p>此容器也可能如此。 另外， 如果算法不用读取作容器的内容就可以修改它（如通过生成要存储的新值）， 则没有理由要求它使用能够读</p>
<p>取内容的迭代器。</p>
<p>简而言之， 对千单通行、 只读算法， 可以使用输入迭代器；而对千单通行、 只写算法， 则可以使用输出迭代器。</p>
<h3 id="正向迭代器"><a class="markdownIt-Anchor" href="#正向迭代器"></a> 正向迭代器</h3>
<p>与输入迭代器和输出迭代器相似，正向迭代器只使用扞运算符来遍历容器，所以它每次沿容器向前移动一 一个元素；然而， 与输入和输</p>
<p>出迭代器不同的是， 它总是按相同的顺序遍历系列值。 另外，将正向迭代器递增后，仍然可以对前面的迭代器值解除引用（如果保存了</p>
<p>它），并可以得到相同的值。这些特征使得多次通行算法成为可能。</p>
<p>正向迭代器既可以使得能够读取和修改数据， 也可以使得只能读取数据</p>
<h3 id="双向迭代器"><a class="markdownIt-Anchor" href="#双向迭代器"></a> 双向迭代器</h3>
<p>假设算法需要能够双向遍历容器， 情况将如何呢？例如， reverse 函数可以交换第一个个元素和最后 一个元素、将指向第一个元素的指</p>
<p>针加1、将指向第二个元素的指针减一，并重复这一过程，双向迭代器具有正向迭代器的所有特性，同时支持两种递减运算符。</p>
<h3 id="随机访问迭代器"><a class="markdownIt-Anchor" href="#随机访问迭代器"></a> 随机访问迭代器</h3>
<p>有些算法（如标准排序和二分检索）要求能够直接跳到容器中的任何 个元素，这叫做随机访问，需要随机访问迭代器。 随机访问迭代器</p>
<p>具有双向迭代器的所有特性，同时添加了支持随机访问的操作（如指针增加运算） 和用千对元素进行排序的关系运算符。 表16.3列出了</p>
<p>除双向迭代器的操作外，随机访问迭代 器还支持的操作。 其中， X表示随机迭代器类型，T表示被指向的类型，a和b都是迭代器值， n为</p>
<p>整数， r为随机迭代器变量或引用。</p>
<table>
<thead>
<tr>
<th>表达式</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>a+n</td>
<td>指向a所指向的元素后的第n个元素</td>
</tr>
<tr>
<td>n+a</td>
<td>与a+n相同</td>
</tr>
<tr>
<td>a-n</td>
<td>指向a所指向的元素前的第n个元素</td>
</tr>
<tr>
<td>r += n</td>
<td>等价千r=r+n</td>
</tr>
<tr>
<td>r -= n</td>
<td>等价于r=r-n</td>
</tr>
<tr>
<td>a[n]</td>
<td>等价千＊（a+n)</td>
</tr>
<tr>
<td>b - a</td>
<td>结果为这样的n值， 即b=a+n</td>
</tr>
<tr>
<td>a &lt; b</td>
<td>如果b - a &gt; 0则为真</td>
</tr>
<tr>
<td>a &gt; b</td>
<td>如果b &lt; a则为真</td>
</tr>
<tr>
<td>a &gt;= b</td>
<td>如果!(a &lt; b)则为真</td>
</tr>
<tr>
<td>a &lt;= b</td>
<td>如果!(a &gt; b)则为真</td>
</tr>
</tbody>
</table>
<h2 id="关于ostream_iterator迭代器"><a class="markdownIt-Anchor" href="#关于ostream_iterator迭代器"></a> 关于<code>ostream_iterator</code>迭代器</h2>
<p><code>ostream_iterator</code>利用了迭代器的解引用来实现输出</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">ostream_iterator&lt;<span class="keyword">int</span>, <span class="keyword">char</span>&gt; <span class="title">iter</span><span class="params">(cout, <span class="string">&quot; &quot;</span>)</span></span>;</span><br><span class="line">*iterator++ = <span class="number">15</span>; <span class="comment">//相当于cout &lt;&lt; 15 &lt;&lt; &quot; &quot;;</span></span><br></pre></td></tr></table></figure>
<h2 id="back_insert_iterator和front_insert_iterator和insert_iterator"><a class="markdownIt-Anchor" href="#back_insert_iterator和front_insert_iterator和insert_iterator"></a> <code>back_insert_iterator和front_insert_iterator和insert_iterator</code></h2>
<p>这些迭代器将容器类型作为模板</p>
<p><code>back_insert_ iterator</code>将元素插入到容器尾部， 而 <code>front_ insert_ iterator</code>将元素插入到容器的前端。<code>insert_iterator</code>将元</p>
<p>素插入到<code>insert_iterator</code>构造函数的参数指定的位置前面</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">back_insert_iterator&lt;vector&lt;<span class="keyword">int</span>&gt;&gt; <span class="built_in">back_iter</span>(dice);</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>私有成员 共有成员 保护成员</title>
    <url>/2021/05/12/%E7%A7%81%E6%9C%89%E6%88%90%E5%91%98%20%E5%85%B1%E6%9C%89%E6%88%90%E5%91%98%20%E4%BF%9D%E6%8A%A4%E6%88%90%E5%91%98/</url>
    <content><![CDATA[<h1 id="私有成员-公有成员-保护成员"><a class="markdownIt-Anchor" href="#私有成员-公有成员-保护成员"></a> 私有成员 公有成员 保护成员</h1>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>;<span class="comment">//公有成员</span></span><br><span class="line"><span class="keyword">private</span>:<span class="comment">//私有成员</span></span><br><span class="line"><span class="keyword">protected</span>:<span class="comment">//保护成员</span></span><br></pre></td></tr></table></figure>
<p>当一个类是私有继承时</p>
<p>他只能访问基类中的公有成员不能访问私有成员</p>
<p>但是当这个类是保护继承时，派生类依然不能访问基类中的私有成员，但是可以访问基类中的保护成员。</p>
<p>因此保护成员的目地主要是方便派生类访问基类，只要把基类中的私有成员定义为保护成员即可。</p>
<h1 id="使用using使用基类的方法"><a class="markdownIt-Anchor" href="#使用using使用基类的方法"></a> 使用using使用基类的方法</h1>
<p>using Student::get_name</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">int</span> age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Student</span>(std::string n = <span class="string">&quot;none&quot;</span>, <span class="keyword">int</span> a=<span class="number">0</span>) &#123; name = n; age = a; &#125;</span><br><span class="line">	~<span class="built_in">Student</span>() &#123;&#125;</span><br><span class="line">	<span class="function">std::string&amp; <span class="title">get_name</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">	<span class="function">std::string&amp; <span class="title">get_name_pro</span><span class="params">()</span></span>;</span><br><span class="line">	std::string name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Badguy</span> :</span> <span class="keyword">private</span> Student</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">double</span> badlevel;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Badguy</span>(std::string n = <span class="string">&quot;none&quot;</span>, <span class="keyword">int</span> a = <span class="number">0</span>, <span class="keyword">double</span> bl = <span class="number">0.0</span>) : <span class="built_in">Student</span>(n, a) &#123; badlevel = bl; &#125;</span><br><span class="line">	~<span class="built_in">Badguy</span>() &#123;&#125;;</span><br><span class="line">	<span class="function"><span class="keyword">double</span>&amp; <span class="title">get_badlevel</span><span class="params">()</span></span>;</span><br><span class="line">	<span class="keyword">using</span> Student::get_name; <span class="comment">//如此get_name就变成了派生类的公有方法</span></span><br><span class="line">	</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>如此get_name就变成了派生类的公有方法（即使派生类是私有继承）</p>
<table>
<thead>
<tr>
<th style="text-align:center">特征</th>
<th style="text-align:center">公有继承</th>
<th style="text-align:center">保护继承</th>
<th style="text-align:center">私有继承</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">公有成员变成</td>
<td style="text-align:center">派生类的保护成员</td>
<td style="text-align:center">派生类的保护成员</td>
<td style="text-align:center">派生类的私有成员</td>
</tr>
<tr>
<td style="text-align:center">保护成员变成</td>
<td style="text-align:center">派生类的保护成员</td>
<td style="text-align:center">派生类的保护成员</td>
<td style="text-align:center">派生类的私有成员</td>
</tr>
<tr>
<td style="text-align:center">私有成员变成</td>
<td style="text-align:center">只能通基类接口访问</td>
<td style="text-align:center">只能通过基类接口访问</td>
<td style="text-align:center">只能通过基类接口访问</td>
</tr>
<tr>
<td style="text-align:center">能否隐式向上转换</td>
<td style="text-align:center">是</td>
<td style="text-align:center">是（只能在派生类中）</td>
<td style="text-align:center">否</td>
</tr>
</tbody>
</table>
]]></content>
  </entry>
  <entry>
    <title>目前安装的插件</title>
    <url>/2021/05/12/%E7%9B%AE%E5%89%8D%E5%AE%89%E8%A3%85%E7%9A%84%E6%8F%92%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="hexo-wordcount"><a class="markdownIt-Anchor" href="#hexo-wordcount"></a> hexo-wordcount</h1>
<h1 id="hexo-symbols-count-time"><a class="markdownIt-Anchor" href="#hexo-symbols-count-time"></a> hexo-symbols-count-time</h1>
]]></content>
      <categories>
        <category>hexo相关</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>next</tag>
      </tags>
  </entry>
  <entry>
    <title>类和动态内存分配</title>
    <url>/2021/05/12/%E7%B1%BB%E5%92%8C%E5%8A%A8%E6%80%81%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D/</url>
    <content><![CDATA[<h1 id="类和动态内存分配"><a class="markdownIt-Anchor" href="#类和动态内存分配"></a> 类和动态内存分配</h1>
<p>c++自动提供以下成员函数（如果未在类中声明）</p>
<ul>
<li>默认构造函数</li>
<li>默认析构函数</li>
<li>复制构造函数</li>
<li>赋值运算符</li>
<li>地址运算符</li>
</ul>
<h2 id="复制构造函数"><a class="markdownIt-Anchor" href="#复制构造函数"></a> 复制构造函数</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">StringBad</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	<span class="keyword">char</span> * str;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="built_in">StringBad</span>();</span><br><span class="line">    	<span class="comment">//StringBad(const StringBad &amp;)   //复制构造函数    </span></span><br><span class="line">    	~<span class="built_in">StringBad</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="调用复制构造函数"><a class="markdownIt-Anchor" href="#调用复制构造函数"></a> 调用复制构造函数</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//motto是StingBad的一个对象</span></span><br><span class="line">StringBad a = motto;</span><br><span class="line"><span class="function">StringBad <span class="title">a</span><span class="params">(motto)</span></span>;</span><br><span class="line">StringBad a = <span class="built_in">StringBad</span>(motto);</span><br><span class="line">StringBad* p = <span class="keyword">new</span> <span class="built_in">StringBad</span>(motto);</span><br></pre></td></tr></table></figure>
<h2 id="默认的复制构造函数功能浅复制"><a class="markdownIt-Anchor" href="#默认的复制构造函数功能浅复制"></a> 默认的复制构造函数功能（浅复制）</h2>
<p>默认的复制构造函数<strong>复制成员的值</strong>。例如</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">StringBad <span class="title">a</span><span class="params">(<span class="string">&quot;字符串&quot;</span>)</span></span>;</span><br><span class="line">StringBad b = a; <span class="comment">//复制a</span></span><br></pre></td></tr></table></figure>
<p>但是由于类中<code>str</code>是一个指针它的值是一个地址，所以复制的时候<code>a.str</code>的**值(地址)**会复制给<code>b.str</code>。因此他们的值(地址)相同，也就是指向同一个字符串。</p>
<p><img src="/2021/05/12/%E7%B1%BB%E5%92%8C%E5%8A%A8%E6%80%81%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D/E:%5Cmarkdown%5Cimages%5Cimgs%5C%E5%A4%8D%E5%88%B6%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E6%B5%85%E5%A4%8D%E5%88%B6-1585629924652.png" alt></p>
<h2 id="深复制"><a class="markdownIt-Anchor" href="#深复制"></a> 深复制</h2>
<p>用户可以自己定义一个复制构造函数实现深复制。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">StringBad::<span class="built_in">StringBad</span>(<span class="keyword">const</span> StringBad &amp; s)</span><br><span class="line">&#123;</span><br><span class="line">    std::<span class="built_in">strcpy</span>(str, s.str); <span class="comment">//拷贝副本</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/2021/05/12/%E7%B1%BB%E5%92%8C%E5%8A%A8%E6%80%81%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D/E:%5Cmarkdown%5Cimages%5Cimgs%5C%E5%A4%8D%E5%88%B6%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E6%B7%B1%E5%A4%8D%E5%88%B6.png" alt></p>
<p>由于复制构造函数中使用了<code>strcpy</code>函数拷贝了一个字符副本并让<code>str</code>指向这个副本，所以这是一个深复制。</p>
]]></content>
  </entry>
  <entry>
    <title>类和对象</title>
    <url>/2021/05/12/%E7%B1%BB%E5%92%8C%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="类和对象"><a class="markdownIt-Anchor" href="#类和对象"></a> 类和对象</h1>
<h2 id="类和结构"><a class="markdownIt-Anchor" href="#类和结构"></a> 类和结构</h2>
<p>类的默认是private，而结构是public</p>
<p>通过私有和共有来实现外部的访问控制。</p>
<h2 id="成员函数"><a class="markdownIt-Anchor" href="#成员函数"></a> 成员函数</h2>
<h3 id="成员函数的全称"><a class="markdownIt-Anchor" href="#成员函数的全称"></a> 成员函数的全称</h3>
<ul>
<li>类的成员函数的全称(限定名)是<strong>类名::函数名()</strong></li>
<li>成员函数的定义需要用作用域解析符来标识所属的类</li>
<li>成员函数可以访问类的私有部分</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Stock::update</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<h2 id="方法和对象"><a class="markdownIt-Anchor" href="#方法和对象"></a> 方法和对象</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STOCK00_H_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STOCK00_H_</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Stock</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	<span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">buy</span><span class="params">(<span class="keyword">int</span> n)</span><span class="comment">//内联函数</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            count = n;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//main</span></span><br><span class="line">...;</span><br><span class="line">Stock a;</span><br><span class="line">a.<span class="built_in">buy</span>(<span class="number">5</span>);</span><br></pre></td></tr></table></figure>
<p><strong>对象调用buy方法时，所用的count对象实际是<code>a.count</code></strong></p>
<h2 id="构造函数"><a class="markdownIt-Anchor" href="#构造函数"></a> 构造函数</h2>
<p>构造函数没有声明，构造函数的名字和类的名字相同，构造函数不需要用户调用，用户在创建类对象时，会自动调用构造函数。<strong>构造函数用来初始化</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STOCK00_H_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STOCK00_H_</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Stock</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	std::string name;</span><br><span class="line">    	<span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">buy</span><span class="params">(<span class="keyword">int</span> n)</span><span class="comment">//内联函数</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            count = n;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//源代码文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;头文件&quot;</span></span></span><br><span class="line">Stock::<span class="built_in">Stock</span>(std::string s, <span class="keyword">int</span> c = <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    name = s;</span><br><span class="line">    n = c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="使用构造函数"><a class="markdownIt-Anchor" href="#使用构造函数"></a> 使用构造函数</h2>
<ol>
<li><code>Stock a(&quot;名字&quot;, 7);</code>隐式声明</li>
<li><code>Stock a = Stock(&quot;名字&quot;, 7);</code>显式声明</li>
</ol>
<h3 id="默认构造函数"><a class="markdownIt-Anchor" href="#默认构造函数"></a> 默认构造函数</h3>
<ol>
<li>为构造函数提供默认值</li>
<li>函数重载</li>
</ol>
<h2 id="析构函数"><a class="markdownIt-Anchor" href="#析构函数"></a> 析构函数</h2>
<p>用类创建对象后，对象使用完过期后，同过析构函数来完成清理工作。如果不提供析构函数，则编译器将隐式的声明一个。</p>
<h3 id="格式"><a class="markdownIt-Anchor" href="#格式"></a> 格式</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STOCK00_H_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STOCK00_H_</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Stock</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	std::string name;</span><br><span class="line">    	<span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">buy</span><span class="params">(<span class="keyword">int</span> n)</span><span class="comment">//内联函数</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            count = n;</span><br><span class="line">        &#125;</span><br><span class="line">    	~<span class="built_in">Stock</span>()<span class="comment">//析构函数</span></span><br><span class="line">        &#123;</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p>析构函数没有内容</p>
<h3 id="何时使用"><a class="markdownIt-Anchor" href="#何时使用"></a> 何时使用</h3>
<p>编译器会分三种情况自动调用</p>
<ol>
<li>如果对象时静态的，则编译器将在程序结束时调用</li>
<li>如果时自动存储对象，则编译器将在执行完对象所在代码块时调用</li>
<li>如果对象是通过new创建的，则编译器将在delete时释放</li>
</ol>
<h2 id="c11列表初始化"><a class="markdownIt-Anchor" href="#c11列表初始化"></a> C++11列表初始化</h2>
<p>如果函数原型<code>void show(const char *, int, double)</code>，则可以使用列表初始化。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Stock example &#123;<span class="string">&quot;字符串&quot;</span>， <span class="number">1</span>, <span class="number">1.0</span>&#125;;</span><br><span class="line"><span class="comment">//或者</span></span><br><span class="line">Stock example = &#123;<span class="string">&quot;字符串&quot;</span>， <span class="number">1</span>， <span class="number">1.0</span>&#125;；</span><br></pre></td></tr></table></figure>
<h2 id="const成员函数"><a class="markdownIt-Anchor" href="#const成员函数"></a> <code>const</code>成员函数</h2>
<p>C++为了保证成员函数不会修改对象也是借用const关键字，而const关键字通常在函数名字后面。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STOCK00_H_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STOCK00_H_</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Stock</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	std::string name;</span><br><span class="line">    	<span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">buy</span><span class="params">(<span class="keyword">int</span> n)</span><span class="comment">//内联函数</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            count = n;</span><br><span class="line">        &#125;</span><br><span class="line">    	~<span class="built_in">Stock</span>()<span class="comment">//析构函数</span></span><br><span class="line">        &#123;</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show</span><span class="params">()</span></span>;<span class="comment">// void show() const</span></span><br><span class="line">        </span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//源代码</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Stock::show</span><span class="params">()</span> <span class="comment">//void Stock::show() const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;count is &quot;</span> &lt;&lt; count &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果不希望函数修改对象，则需要加<code>const</code>关键字。但是<code>const</code>的位置很关键。</p>
<p><code>const void Stock::show()</code>这种用法是错误的，应该用<code>void Stock::show() const</code>这种写法。</p>
<p><strong>而且如果类方法不修改调用对象，则应该将其声明为<code>const</code>。</strong></p>
<p><strong>如果在定义过程中要在const成员函数里面调用函数，则调用的函数类型也必须是const</strong></p>
<h2 id="静态成员"><a class="markdownIt-Anchor" href="#静态成员"></a> 静态成员</h2>
<p>在类中成员前前面加<code>static</code>关键字使其成为静态变量。<strong>无论创建多少个类对象所有类对象只享用一个该变量。</strong></p>
<p><strong>而且不能在类中初始化静态变量，声明是描述了如何分配内存但还没有分配内存所以不能赋值，除了静态数据成员是整型或者枚举型<code>const</code></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	<span class="comment">//static int grade = 0; 错误</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	.....;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而是在源文件中声明</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//源文件</span></span><br><span class="line"><span class="keyword">int</span> Student::grade = <span class="number">0</span>;</span><br><span class="line">Student::<span class="built_in">Student</span>()&#123;&#125;</span><br><span class="line">Student::~<span class="built_in">Student</span>()&#123;&#125;</span><br></pre></td></tr></table></figure>
<h2 id="this指针"><a class="markdownIt-Anchor" href="#this指针"></a> this指针</h2>
<p>在类中每一个成员函数都有一个this指针指向它调用对象的地址。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//头文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STUENT</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STUDENT</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	std::string name;</span><br><span class="line">    	<span class="keyword">double</span> length;</span><br><span class="line">    	<span class="keyword">double</span> weight;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="built_in">Student</span>(); <span class="comment">//构造函数</span></span><br><span class="line">    	<span class="built_in">Student</span>(std::string, <span class="keyword">double</span>, <span class="keyword">double</span>); <span class="comment">//构造函数</span></span><br><span class="line">    	~<span class="built_in">Student</span>(); <span class="comment">//析构函数</span></span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show_name</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show_length</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    	<span class="function"><span class="keyword">void</span> <span class="title">show_weight</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    	<span class="function"><span class="keyword">const</span> <span class="keyword">double</span> &amp; <span class="title">taller</span><span class="params">(<span class="keyword">const</span> Student &amp;)</span> <span class="keyword">const</span></span>; <span class="comment">//返回身高的引用</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//源代码</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line">Student::~<span class="built_in">Student</span>() <span class="comment">//析构函数需要定义</span></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Student::<span class="built_in">Student</span>(std::string n, <span class="keyword">double</span> l, <span class="keyword">double</span> w)</span><br><span class="line">&#123;</span><br><span class="line">    name = n;</span><br><span class="line">    length = l;</span><br><span class="line">    weight = w;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Student::show_name</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; name &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Student::show_length</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; length &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Student::show_weight</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; weight &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">const</span> <span class="keyword">double</span> &amp; <span class="title">Student::taller</span><span class="params">(<span class="keyword">const</span> Student &amp; s)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (s.length &gt; length)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;taller is &quot;</span> &lt;&lt; s.name &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; s.length &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> s.length;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (s.length &lt; length)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;taller is &quot;</span> &lt;&lt; name &lt;&lt; <span class="string">&quot; length: &quot;</span> &lt;&lt; length &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">return</span> (*<span class="keyword">this</span>).length; <span class="comment">//this指针指的是调用对象的地址</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (s.length == length)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;the same length\n&quot;</span>;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//main函数</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;test.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Student A:\n&quot;</span>;</span><br><span class="line">    Student A = &#123;<span class="string">&quot;小明&quot;</span>, <span class="number">1.75</span>, <span class="number">90</span>&#125;;</span><br><span class="line">    A.<span class="built_in">show_name</span>();</span><br><span class="line">    A.<span class="built_in">show_length</span>();</span><br><span class="line">    A.<span class="built_in">show_weight</span>();</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Student B:\n&quot;</span>;</span><br><span class="line">    Student B &#123;<span class="string">&quot;小王&quot;</span>, <span class="number">1.80</span>, <span class="number">80</span>&#125;;</span><br><span class="line">    B.<span class="built_in">show_name</span>();</span><br><span class="line">    B.<span class="built_in">show_length</span>();</span><br><span class="line">    B.<span class="built_in">show_weight</span>();</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;-----------------------------\n&quot;</span>;</span><br><span class="line">    A.<span class="built_in">taller</span>(B);<span class="comment">//在这里this指的是B对象的地址，所以(*this).length相当于s.length(s是引用见源代码)</span></span><br><span class="line">    B.<span class="built_in">taller</span>(A);<span class="comment">//在这里this指的是A对象的地址，所以(*this).length相当于s.length(s是引用见源代码)</span></span><br><span class="line">    <span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="对象数组"><a class="markdownIt-Anchor" href="#对象数组"></a> 对象数组</h2>
<p>可以用类创建数组对象并且初始化</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Student NO1[<span class="number">5</span>]; <span class="comment">//创建5个Student对象</span></span><br><span class="line">Student NO1[<span class="number">5</span>] &#123;<span class="built_in">Student</span>(),<span class="built_in">Student</span>(<span class="string">&quot;小明&quot;</span>, <span class="number">1.75</span>, <span class="number">90.3</span>&#125;; <span class="comment">//创建5个Student对象,并将前两个分别用Studnet()和Student(std::string n, double l, double w)初始化</span></span><br></pre></td></tr></table></figure>
<h2 id="类作用域"><a class="markdownIt-Anchor" href="#类作用域"></a> 类作用域</h2>
<p>类中定义的所有数据和成员函数的作用域是<strong>整个类</strong>，他们的名称在类内是已知，在类外是<strong>未知</strong>。在类外使用成员是需要用类对象和<code>. -&gt; ::</code>符号来使用，定义成员函数时使用<code>::</code>解析符。</p>
<h2 id="作用域为类的常量c11"><a class="markdownIt-Anchor" href="#作用域为类的常量c11"></a> 作用域为类的常量(c++11)</h2>
<p>创建一个所有类对象可以共享的常量。c++11提供关键字<code>static</code>，创建的常量与其他静态变量储存在一起，而不是储存在对象中的。</p>
<h2 id="作用域内枚举c11"><a class="markdownIt-Anchor" href="#作用域内枚举c11"></a> 作用域内枚举(c++11)</h2>
<p>c++11提供一种新的枚举，可以解决枚举成员相同名称的问题。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="keyword">class</span> <span class="title">A</span> &#123;</span>c, d, e, f&#125;;</span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="keyword">class</span> <span class="title">B</span> &#123;</span>c, d, e, f&#125;; <span class="comment">//calss 可以用struct代替</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//使用</span></span><br><span class="line">A::c;</span><br><span class="line">B::c;<span class="comment">//不会发生冲突</span></span><br></pre></td></tr></table></figure>
<p>默认状态下c++11作用域枚举的底层类型时int但可以通过另一种方式来修改</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="keyword">class</span> :</span> <span class="keyword">short</span> A &#123;c, d, e, f&#125;; <span class="comment">//底层类型时short</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>类模板</title>
    <url>/2021/05/12/%E7%B1%BB%E6%A8%A1%E6%9D%BF/</url>
    <content><![CDATA[<h1 id="类模板"><a class="markdownIt-Anchor" href="#类模板"></a> 类模板</h1>
<h2 id="格式"><a class="markdownIt-Anchor" href="#格式"></a> 格式</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span> <span class="comment">//或者template &lt;typename T&gt;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">IC</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    T v;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">IC</span>(<span class="keyword">const</span> T &amp; val) : <span class="built_in">v</span>(val) &#123;&#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>T 是占字符随后实例化过程将被替代。</p>
<h2 id="使用"><a class="markdownIt-Anchor" href="#使用"></a> 使用</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    IC&lt;<span class="keyword">int</span>&gt; a;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>T被替换为int</p>
]]></content>
  </entry>
  <entry>
    <title>流程控制</title>
    <url>/2021/05/12/%E6%B5%81%E7%A8%8B%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<h1 id="流程控制"><a class="markdownIt-Anchor" href="#流程控制"></a> 流程控制</h1>
<h2 id="if"><a class="markdownIt-Anchor" href="#if"></a> if</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> [ condition ] <span class="comment">#中括号与条件两边要有空格</span></span><br><span class="line">	<span class="keyword">then</span></span><br><span class="line">		<span class="comment">#program</span></span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> [ condition ] <span class="comment">#中括号与条件两边要有空格</span></span><br><span class="line">	<span class="keyword">then</span></span><br><span class="line">		<span class="comment">#program</span></span><br><span class="line"><span class="keyword">elif</span> [ condition ]</span><br><span class="line">	<span class="keyword">then</span></span><br><span class="line">		<span class="comment">#program</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">	<span class="comment">#program</span></span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>
<h2 id="case"><a class="markdownIt-Anchor" href="#case"></a> case</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="variable">$variable</span> <span class="keyword">in</span></span><br><span class="line">	<span class="string">&quot;value1&quot;</span>)</span><br><span class="line">		<span class="comment">#program1</span></span><br><span class="line">		;;</span><br><span class="line">	<span class="string">&quot;value&quot;</span>)</span><br><span class="line">		<span class="comment">#program2</span></span><br><span class="line">		;;</span><br><span class="line">	.........</span><br><span class="line">    *)  <span class="comment">#相当于c++中default</span></span><br><span class="line">    	<span class="comment">#program3</span></span><br><span class="line">		;;</span><br><span class="line"><span class="keyword">esac</span></span><br></pre></td></tr></table></figure>
<h2 id="for"><a class="markdownIt-Anchor" href="#for"></a> for</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> variable <span class="keyword">in</span> value1 value2 value3 ........</span><br><span class="line">	<span class="keyword">do</span> </span><br><span class="line">		<span class="comment">#progame</span></span><br><span class="line">	<span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> variable <span class="keyword">in</span> $(cat file)</span><br><span class="line">	<span class="keyword">do</span></span><br><span class="line">		<span class="comment">#program</span></span><br><span class="line">	<span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (( i=1;i&lt;100;i=i+1 ))</span><br><span class="line">	<span class="keyword">do</span></span><br><span class="line">		<span class="comment">#program</span></span><br><span class="line">	<span class="keyword">done</span></span><br></pre></td></tr></table></figure>
<h2 id="while"><a class="markdownIt-Anchor" href="#while"></a> while</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> condition</span><br><span class="line">	<span class="keyword">do</span></span><br><span class="line">    	statements</span><br><span class="line">	<span class="keyword">done</span>	</span><br></pre></td></tr></table></figure>
<h2 id="until"><a class="markdownIt-Anchor" href="#until"></a> until</h2>
<p>与while相反</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">until condition</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">    statements</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>类继承</title>
    <url>/2021/05/12/%E7%B1%BB%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<h1 id="类继承需要注意的地方"><a class="markdownIt-Anchor" href="#类继承需要注意的地方"></a> 类继承需要注意的地方</h1>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="comment">//基类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">people</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string name;</span><br><span class="line">    <span class="keyword">int</span> age;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">people</span>(string  n, <span class="keyword">int</span> a) &#123;name = n; age = a;&#125;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">people</span>() &#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;name : &quot;</span> &lt;&lt; name &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;age : &quot;</span> &lt;&lt; age &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//派生类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">teacher</span> :</span> <span class="keyword">public</span> people</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="keyword">double</span> salary;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        <span class="built_in">teacher</span>(string  n, <span class="keyword">int</span> a, <span class="keyword">double</span> s) : <span class="built_in">people</span>(n, a), <span class="built_in">salary</span>(s) &#123;&#125;</span><br><span class="line">        <span class="built_in">teacher</span>(<span class="keyword">const</span> people &amp; p, <span class="keyword">double</span> s) : <span class="built_in">people</span>(p), <span class="built_in">salary</span>(s) &#123;&#125;</span><br><span class="line">        <span class="keyword">virtual</span> ~<span class="built_in">teacher</span>() &#123;&#125;</span><br><span class="line">       <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">       </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;name : &quot;</span> &lt;&lt; name &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;age : &quot;</span> &lt;&lt; age &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;salary : &quot;</span> &lt;&lt; salary &lt;&lt; endl;</span><br><span class="line">       &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h2 id="指针的例外"><a class="markdownIt-Anchor" href="#指针的例外"></a> 指针的例外</h2>
<p>通常使用指针，指针的类型和赋予的类型要求一致，但在基类和派生类中是个例外。</p>
<h3 id="单向的特点"><a class="markdownIt-Anchor" href="#单向的特点"></a> 单向的特点</h3>
<p>可以将派生类的地址赋给指向基类的指针，<strong>但是不可以将基类的地址赋给指向派生类的指针。</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">teacher <span class="title">a</span><span class="params">(<span class="string">&quot;小明&quot;</span>, <span class="number">32</span>, <span class="number">5000</span>)</span></span>;</span><br><span class="line">people* p1 = &amp;a;</span><br></pre></td></tr></table></figure>
<h3 id="指针和成员函数"><a class="markdownIt-Anchor" href="#指针和成员函数"></a> 指针和成员函数</h3>
<p><strong>但是p1只能使用基类的方法，不能使用派生类的方法。</strong></p>
<h1 id="多态"><a class="markdownIt-Anchor" href="#多态"></a> 多态</h1>
<h2 id="公有继承的特点"><a class="markdownIt-Anchor" href="#公有继承的特点"></a> ★公有继承的特点</h2>
<ul>
<li>公有继承是一种<strong>is-a-kind-of</strong>的类型。例如可以从<code>Fruit</code>类派生<code>Banana</code>类，用为<strong>Banana is-a Fruit</strong></li>
<li>但它既不是<strong>has-a</strong>类型，例如<code>Lunch</code>中有<code>Fruit</code>，但<code>Fruit</code>不是<code>Lunch</code>，所以不能用<code>Fruit</code>派生<code>Lunch</code>类</li>
<li>也不是<strong>is-like-a</strong>，例如<code>Monkey</code>像<code>Human</code>，但<code>Monkey</code>不是<code>Human</code>，所以不能从<code>Monkey</code>派生<code>Human</code></li>
<li>更不是<strong>is-implemented-as-a（作为·····来实现）</strong>，例如数组可以实现栈，但<code>Array</code>类派生<code>Stack</code>是不合适的，应为<code>Stack</code>不是<code>Array</code>，比如数组索引就不是栈的属性</li>
<li>也不是<strong>uses-a</strong>，例如<code>Computer</code>可以使用<code>Printer</code>，但<code>Computer</code>派生<code>Printer</code>是没有意义的</li>
</ul>
<p>所以<strong>is-a</strong>关系是派生类拥有基类的所有特性，<code>Banana</code>拥有<code>Fruit</code>的所有特性（应为<code>Fruit</code>是一个广泛的概念，它是统筹考虑各种水果的特性而下的定义，它是所有水果的交集）香蕉是水果，水果不是香蕉。</p>
<p><strong>通常考虑两个类是否可以有基类和派生类的关系只需要考虑满不满足</strong>is-a<strong>的关系。</strong></p>
<h2 id="虚方法"><a class="markdownIt-Anchor" href="#虚方法"></a> 虚方法</h2>
<p>虚方法需要用到关键字virtual</p>
<p>如果方法不是通过对象调用的，而是通过指针或者引用调用则关键字virtual将跟据指针或引用的类型决定使用基类还是派生类的方法。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">people* p[<span class="number">2</span>] = &#123;<span class="keyword">new</span> teacher <span class="built_in">a</span>(<span class="string">&quot;小明&quot;</span>, <span class="number">32</span>, <span class="number">5000</span>), <span class="keyword">new</span> people <span class="built_in">b</span>(<span class="string">&quot;小红&quot;</span>, <span class="number">18</span>, <span class="number">3000</span>)&#125;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++)</span><br><span class="line">&#123;</span><br><span class="line">    p[i] -&gt; <span class="built_in">show</span>();<span class="comment">//p[0] -&gt; show(); //使用的是teacher类的show方法 p[1] -&gt; show(); //使用的是people类的show方法</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">delete</span> p[i]; <span class="comment">//delete p[0] //使用的是teacher类的析构函数 delete p[1]使用的是people类的析构函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="动态联编和静态联编"><a class="markdownIt-Anchor" href="#动态联编和静态联编"></a> 动态联编和静态联编</h2>
<p>静态联编效率高</p>
<h2 id="关于虚函数的注意事项"><a class="markdownIt-Anchor" href="#关于虚函数的注意事项"></a> ★关于虚函数的注意事项</h2>
<ul>
<li>基类方法的声明使用关键字<code>virtual</code>可使该方法在基类以及所有的派生类（以及派生类的派生类）中是虚的。</li>
<li>使用指向对象的<strong>指针</strong>或者<strong>引用</strong>将使用对象的方法，称为动态联编。</li>
<li>在派生类中重新定义的函数，在基类和派生类中都需要声明为<code>virtual</code></li>
<li>构造函数不能是虚函数，在主程序调用派生类的构造函数时会先使用派生类的构造函数再使用在派生类构造函数中使用的基类的构造函数，这种顺序不能乱</li>
<li>析构函数应该是虚函数（除非类没有设定派生类），例如</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">people* p = <span class="keyword">new</span> teacher;</span><br><span class="line">·····;</span><br><span class="line"><span class="keyword">delete</span> p; <span class="comment">//如果析构函数是虚函数，则这个操作会先调用teacher的析构函数，再调用people的析构函数</span></span><br></pre></td></tr></table></figure>
<p><strong>通常做法是给基类提供一个虚析构函数，即使它并不需要析构函数</strong></p>
<ul>
<li>只有成员函数才可以声明为虚方法，友元函数不是成员函数因此没有虚方法，但在设计是可以让友元函数使用虚成员函数来解决一些需求</li>
<li>重新定义将隐藏方法（<strong>1. 如果重新定义继承方法，应确保与原来的原型完全一样，但如果方法是返回基类的指针或引用，则派生类方法重定义时应返回派生类的指针或引用</strong>，<strong>2. 如果基类声明被重载了，则应该在派生类中重新定义所有的基类版本</strong>）</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">people</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	·····;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;···;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">teacher</span> :</span> <span class="keyword">public</span> people</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    	·······;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">(<span class="keyword">int</span> a)</span> </span>&#123;····;&#125; </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    teacher a;</span><br><span class="line">    a.<span class="built_in">show</span>(<span class="number">5</span>); <span class="comment">//可以， teacher的show方法隐藏了原有的</span></span><br><span class="line">    a.<span class="built_in">show</span>(); <span class="comment">//不可以</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="访问控制protected"><a class="markdownIt-Anchor" href="#访问控制protected"></a> 访问控制(protected)</h2>
<p>将基类成员声明为protected时，则在派生类中可以直接访问。</p>
<h1 id="抽象基类abc编程"><a class="markdownIt-Anchor" href="#抽象基类abc编程"></a> 抽象基类(ABC编程)</h1>
<p>将两个类的共同点抽象出来设计一个基类。</p>
<h2 id="纯虚函数"><a class="markdownIt-Anchor" href="#纯虚函数"></a> 纯虚函数</h2>
<p>纯虚函数声明的结尾是=0</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">virtual</span> <span class="keyword">double</span> <span class="title">Area</span><span class="params">()</span> <span class="keyword">const</span> </span>=<span class="number">0</span></span><br></pre></td></tr></table></figure>
<p>当类中含有纯虚函数时，则不能使用这个类创建对象，包含纯虚函数的类只能做基类。纯虚函数可以不定义也可以定义</p>
]]></content>
  </entry>
  <entry>
    <title>系统资源控制</title>
    <url>/2021/05/12/%E7%B3%BB%E7%BB%9F%E8%B5%84%E6%BA%90%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<h1 id="系统资源控制"><a class="markdownIt-Anchor" href="#系统资源控制"></a> 系统资源控制</h1>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vmstat [刷新延迟 刷新次数]</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dmesg <span class="comment">#开机时内核检测信息</span></span><br><span class="line">dmesg | grep CPU</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">free [-b|-k|-m|-g] <span class="comment">#查看内存使用状态</span></span><br><span class="line">	 -b：以字节为单位显示</span><br><span class="line">	 -k：以kb为单位显示，默认</span><br><span class="line">	 -m：以MB为单位显示</span><br><span class="line">	 -g：以GB为单位显示</span><br></pre></td></tr></table></figure>
<h2 id="缓冲和缓存"><a class="markdownIt-Anchor" href="#缓冲和缓存"></a> 缓冲和缓存</h2>
<p>缓存（cache）是用来加速数据的，从硬盘中“读取”的</p>
<p>缓冲（buffer）是又来加速数据“写入”硬盘的</p>
<hr>
<h2 id="查看cpu"><a class="markdownIt-Anchor" href="#查看cpu"></a> 查看CPU</h2>
<p><code>/proc/cpuinfo</code>文件</p>
<hr>
<h2 id="显示系统的启动时间和平均负载"><a class="markdownIt-Anchor" href="#显示系统的启动时间和平均负载"></a> 显示系统的启动时间和平均负载</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uptime <span class="comment">#显示系统的启动时间和平均负载 w命令</span></span><br></pre></td></tr></table></figure>
<hr>
<h2 id="查看系统内核版本"><a class="markdownIt-Anchor" href="#查看系统内核版本"></a> 查看系统内核版本</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uname [option]</span><br><span class="line">	  -a：查看系统所有信息</span><br><span class="line">	  -r：查看内核版本</span><br><span class="line">	  -s：查看内核名称</span><br></pre></td></tr></table></figure>
<hr>
<h2 id="查看系统位数"><a class="markdownIt-Anchor" href="#查看系统位数"></a> 查看系统位数</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">file 随便一个命令的绝对路径</span><br><span class="line">例如：</span><br><span class="line">file /bin/ls</span><br></pre></td></tr></table></figure>
<hr>
<h2 id="查看当前系统发行版本"><a class="markdownIt-Anchor" href="#查看当前系统发行版本"></a> 查看当前系统发行版本</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsb_release -a <span class="comment">#查看当前系统发行版本</span></span><br></pre></td></tr></table></figure>
<hr>
<h2 id="列出进程和打开或使用的文件信息"><a class="markdownIt-Anchor" href="#列出进程和打开或使用的文件信息"></a> 列出进程和打开或使用的文件信息</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsof [option]</span><br><span class="line">	-c 字符串：只列出以字符串开头的进程打开的文件</span><br><span class="line">	-u 用户名：只列出某个用户的进程打开的文件</span><br><span class="line">	-p pid：列出某个pid进程打开的文件</span><br></pre></td></tr></table></figure>
<h1 id="系统定时任务"><a class="markdownIt-Anchor" href="#系统定时任务"></a> 系统定时任务</h1>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">crontab [option]</span><br><span class="line">		-e：编辑定时任务</span><br><span class="line">		-l：查询任务</span><br><span class="line">		-r：删除所有任务</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">* * * * * 命令</span><br></pre></td></tr></table></figure>
<p><img src="/2021/05/12/%E7%B3%BB%E7%BB%9F%E8%B5%84%E6%BA%90%E6%8E%A7%E5%88%B6/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C2020-10-01_143051.png" alt="2020-10-01_143051"></p>
<p><img src="/2021/05/12/%E7%B3%BB%E7%BB%9F%E8%B5%84%E6%BA%90%E6%8E%A7%E5%88%B6/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C2020-10-01_143158.png" alt="2020-10-01_143158"></p>
<p><img src="/2021/05/12/%E7%B3%BB%E7%BB%9F%E8%B5%84%E6%BA%90%E6%8E%A7%E5%88%B6/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C2020-10-01_143248.png" alt="2020-10-01_143248"></p>
]]></content>
  </entry>
  <entry>
    <title>运算符重载</title>
    <url>/2021/05/12/%E8%BF%90%E7%AE%97%E7%AC%A6%E9%87%8D%E8%BD%BD/</url>
    <content><![CDATA[<h1 id="运算符重载"><a class="markdownIt-Anchor" href="#运算符重载"></a> 运算符重载</h1>
<p>使用<code>operator +()</code>重载+运算符，<code>使用operator *()</code>重载*运算符，但是不能使用c++没有的符号例如@</p>
<h2 id="注意事项"><a class="markdownIt-Anchor" href="#注意事项"></a> 注意事项</h2>
<ol>
<li>
<p>重载后的运算符必须至少有一个操作数是用户定义的类型， 这将防止用户为标准类型重载运算符。<br>
因此， 不能将减法运算符（－ ）重载为计算两个double值的和， 而不是它们的差。虽然这种限制将对创造<br>
性有所影响， 但可以确保程序正常运行。</p>
</li>
<li>
<p>使用运算符时不能违反运算符原来的句法规则。例如， 不能将求模运算符（％）重载成使用一个操<br>
作数：</p>
</li>
<li>
<pre class="highlight"><code class="c++"><span class="hljs-keyword">int</span> x;
Time shiva;
% x; <span class="hljs-comment">// invalid for modulus operator</span>
% shiva; <span class="hljs-comment">// invalid for overloaded operator</span>
</code></pre>
<p>同样， 不能修改运算符的优先级。因此， 如果将加号运算符重载成将两个类相加， 则新的运算符与原<br>
来的加号具有相同的优先级。</p>
</li>
<li>
<p>不能创建新运算符。例如， 不能定义<code>operator**()</code>函数来表示求幕。</p>
</li>
<li>
<p>不能重载下面的运算符。</p>
</li>
</ol>
<ul>
<li><code>size of</code>: <code>sizeof</code>运算符。</li>
<li><code>.</code>： 成员运算符。</li>
<li><code>.*</code>： 成员指针运算符。</li>
<li><code>::</code>： 作用域解析运算符。</li>
<li><code>?:</code>： 条件运算符。</li>
<li><code>typeid</code>: 一个RTTI运算符。</li>
<li><code>const_cast</code>: 强制类型转换运算符。</li>
<li><code>dynamic_cast</code>: 强制类型转换运算符。</li>
<li><code>reinterpret_ cast</code>: 强制类型转换运算符。</li>
<li><code>static_cast</code>: 强制类型转换运算符。</li>
</ul>
<ol start="6">
<li>可以重载的运算符</li>
</ol>
<table>
<thead>
<tr>
<th style="text-align:center">+</th>
<th style="text-align:center">-</th>
<th style="text-align:center">*</th>
<th style="text-align:center">/</th>
<th style="text-align:center">%</th>
<th style="text-align:center">^</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">&amp;</td>
<td style="text-align:center">|</td>
<td style="text-align:center">~=</td>
<td style="text-align:center">!</td>
<td style="text-align:center">=</td>
<td style="text-align:center">&lt;</td>
</tr>
<tr>
<td style="text-align:center">&gt;</td>
<td style="text-align:center">+=</td>
<td style="text-align:center">-=</td>
<td style="text-align:center">*=</td>
<td style="text-align:center">/=</td>
<td style="text-align:center">%=</td>
</tr>
<tr>
<td style="text-align:center">^=</td>
<td style="text-align:center">&amp;=</td>
<td style="text-align:center">|=</td>
<td style="text-align:center">&lt;&lt;</td>
<td style="text-align:center">&gt;&gt;</td>
<td style="text-align:center">&gt;&gt;=</td>
</tr>
<tr>
<td style="text-align:center">&lt;&lt;=</td>
<td style="text-align:center">==</td>
<td style="text-align:center">!=</td>
<td style="text-align:center">&lt;=</td>
<td style="text-align:center">&gt;=</td>
<td style="text-align:center">&amp;&amp;</td>
</tr>
<tr>
<td style="text-align:center">||</td>
<td style="text-align:center">++</td>
<td style="text-align:center">–</td>
<td style="text-align:center">,</td>
<td style="text-align:center">-&gt;*</td>
<td style="text-align:center">-&gt;</td>
</tr>
<tr>
<td style="text-align:center">()</td>
<td style="text-align:center">[]</td>
<td style="text-align:center">new</td>
<td style="text-align:center">delete</td>
<td style="text-align:center">new []</td>
<td style="text-align:center">delete[]</td>
</tr>
</tbody>
</table>
<p><strong>运算符左边是调用对象</strong></p>
]]></content>
  </entry>
  <entry>
    <title>重载运算符时第二个参数问题(加const)</title>
    <url>/2021/05/12/%E9%87%8D%E8%BD%BD%E8%BF%90%E7%AE%97%E7%AC%A6%E6%97%B6%E7%AC%AC%E4%BA%8C%E4%B8%AA%E5%8F%82%E6%95%B0%E9%97%AE%E9%A2%98(%E5%8A%A0const)/</url>
    <content><![CDATA[<h1 id="重载运算符时第二个参数问题加const"><a class="markdownIt-Anchor" href="#重载运算符时第二个参数问题加const"></a> 重载&lt;&lt;运算符时第二个参数问题(加const)</h1>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">friend</span> std::ostream&amp; <span class="title">operator</span><span class="params">(std::ostream&amp; os, <span class="keyword">const</span> <span class="keyword">typename</span>&amp; a)</span></span>; <span class="comment">//const</span></span><br></pre></td></tr></table></figure>
<p>这里必须加上const，输出的东西是不能修改的。</p>
<p>但是如果参数列表<code>(std::ostream&amp; os, typename a)</code>也是可以的，这里会调用typename类的复制构造函数，不会对原来对象造成影响。</p>
<h1 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h1>
<p>第二个参数必须写成</p>
<ol>
<li>
<p>const typename&amp; a</p>
<p>或</p>
</li>
<li>
<p>typename a</p>
</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>阿里源：</title>
    <url>/2021/05/12/%E9%98%BF%E9%87%8C%E6%BA%90%EF%BC%9A/</url>
    <content><![CDATA[<p>阿里源：<br>
deb <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic main restricted universe multiverse<br>
deb <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-proposed main restricted universe multiverse<br>
deb <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
deb-src <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic main restricted universe multiverse<br>
deb-src <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb-src <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb-src <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-proposed main restricted universe multiverse<br>
deb-src <a href="http://mirrors.aliyun.com/ubuntu/">http://mirrors.aliyun.com/ubuntu/</a> bionic-backports main restricted universe multiverse</p>
<p>中科大源：<br>
deb <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic main restricted universe multiverse<br>
deb-src <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic main restricted universe multiverse<br>
deb <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb-src <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
deb-src <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
deb <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb-src <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-proposed main restricted universe multiverse<br>
deb-src <a href="https://mirrors.ustc.edu.cn/ubuntu/">https://mirrors.ustc.edu.cn/ubuntu/</a> bionic-proposed main restricted universe multiverse</p>
<p>163源：<br>
deb <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic main restricted universe multiverse<br>
deb <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-proposed main restricted universe multiverse<br>
deb <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
deb-src <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic main restricted universe multiverse<br>
deb-src <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb-src <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb-src <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-proposed main restricted universe multiverse<br>
deb-src <a href="http://mirrors.163.com/ubuntu/">http://mirrors.163.com/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
清华源：<br>
deb <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic main restricted universe multiverse<br>
deb-src <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic main restricted universe multiverse<br>
deb <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb-src <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-updates main restricted universe multiverse<br>
deb <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
deb-src <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-backports main restricted universe multiverse<br>
deb <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb-src <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-security main restricted universe multiverse<br>
deb <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-proposed main restricted universe multiverse<br>
deb-src <a href="https://mirrors.tuna.tsinghua.edu.cn/ubuntu/">https://mirrors.tuna.tsinghua.edu.cn/ubuntu/</a> bionic-proposed main restricted universe multiverse</p>
<p>使用方法：</p>
<ol>
<li>cd /etc/apt</li>
<li>cp sources.list  sources.list.bak, 备份原来的自带的源</li>
<li>sudo vi  /etc/apt/sources.list, 将上面的源挑选一个黏贴进文本，保存</li>
<li>sudo apt-get update</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>静态变量的类型和链接性</title>
    <url>/2021/05/12/%E9%9D%99%E6%80%81%E5%8F%98%E9%87%8F%E7%9A%84%E7%B1%BB%E5%9E%8B%E5%92%8C%E9%93%BE%E6%8E%A5%E6%80%A7/</url>
    <content><![CDATA[<h1 id="静态变量的类型和链接性"><a class="markdownIt-Anchor" href="#静态变量的类型和链接性"></a> 静态变量的类型和链接性</h1>
<p>静态变量至始至终都存在，一直会在内存中存在。</p>
<h2 id="静态外链性"><a class="markdownIt-Anchor" href="#静态外链性"></a> 静态，外链性</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">int</span> a;<span class="comment">//静态，外部链接性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在其他文件使用<code>extern</code>来使用。</p>
<h2 id="静态内链性"><a class="markdownIt-Anchor" href="#静态内链性"></a> 静态，内链性</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> a;<span class="comment">//静态，内部链接性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其他文件无法访问，只能在本文件中使用。</p>
<h2 id="静态无链接性"><a class="markdownIt-Anchor" href="#静态无链接性"></a> 静态，无链接性</h2>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> a;<span class="comment">//静态，无链接性</span></span><br><span class="line">    ...;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>转换函数</title>
    <url>/2021/05/12/%E8%BD%AC%E6%8D%A2%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h1 id="转换函数"><a class="markdownIt-Anchor" href="#转换函数"></a> 转换函数</h1>
<ul>
<li>转换函数必须是类方法</li>
<li>转换函数不能指定返回类型</li>
<li>转换函数不能有参数</li>
</ul>
<h2 id="格式"><a class="markdownIt-Anchor" href="#格式"></a> 格式</h2>
<p><code>operator typeName()</code></p>
<p>转换double类型的函数原型</p>
<p><code>operator double()</code></p>
]]></content>
  </entry>
  <entry>
    <title>Shell</title>
    <url>/2021/05/12/Shell/</url>
    <content><![CDATA[<h1 id="shell"><a class="markdownIt-Anchor" href="#shell"></a> Shell</h1>
<p><img src="/2021/05/12/Shell/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5CShell.png" alt="Shell"></p>
<p>Shell命令解释器：将人输入的命令解释为机器语言传给内核</p>
<p>内核：使用机器语言调用硬件，并将硬件返回的信息传给Shell，再由Shell转换为人能看懂的语言</p>
<p><strong>Shell也是一个编程语言，Shell是解释执行的脚本语言</strong></p>
<h2 id="shell分类"><a class="markdownIt-Anchor" href="#shell分类"></a> Shell分类</h2>
<p>Shell分为Bourne Shell和C Shell两种Shell不兼容</p>
<p>Bourne Shell包含：sh、Bash、psh、zsh</p>
<p>C家族包含：csh、tcsh</p>
<p>如今使用linux使用的Shell主流就是Bash作为用户的基本Shell</p>
<h2 id="查看linux支持的shell"><a class="markdownIt-Anchor" href="#查看linux支持的shell"></a> 查看linux支持的Shell</h2>
<p>/etc/shells文件**</p>
<h2 id="shell脚本执行方式"><a class="markdownIt-Anchor" href="#shell脚本执行方式"></a> Shell脚本执行方式</h2>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">输出命令</span></span><br><span class="line">echo [选项] [内容]   </span><br><span class="line">	 -e 支持反斜杠控制的字符转换</span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">!/bin/bash</span></span><br><span class="line"><span class="meta">#</span><span class="bash">The first program</span></span><br><span class="line"><span class="meta">#</span><span class="bash">Author: xiaoma</span></span><br><span class="line"></span><br><span class="line">echo -e &quot;hello world&quot;</span><br></pre></td></tr></table></figure>
<p><strong><code>#!/bin/bash</code>必须写</strong></p>
<h3 id="脚本执行方法"><a class="markdownIt-Anchor" href="#脚本执行方法"></a> 脚本执行方法</h3>
<ol>
<li>赋予执行权限，直接运行</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod 755 hello.sh</span><br><span class="line">./hello.sh</span><br></pre></td></tr></table></figure>
<ol start="2">
<li>调用Bash执行</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bash hello.sh</span><br></pre></td></tr></table></figure>
<p><strong>windows和linux的回车符是不一样的，在linux中回车符是<code>$</code>号在windows是<code>^M$</code>所以在window上写的shell命令在linux运行不了</strong></p>
<h2 id="历史命令与命令补全"><a class="markdownIt-Anchor" href="#历史命令与命令补全"></a> 历史命令与命令补全</h2>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">history</span> [选项] [历史命令保存文件]</span><br><span class="line">		-c 	   清空历史命令</span><br><span class="line">		-w	   把缓存中的历史命令写入历史命令保存文件~/.bash_history（在每个用户的家目录中）</span><br></pre></td></tr></table></figure>
<p>查看历史命令</p>
<p>历史命令默认保存1000条，在/etc/profile中配置的。</p>
<h3 id="历史命令调用"><a class="markdownIt-Anchor" href="#历史命令调用"></a> 历史命令调用</h3>
<ol>
<li>
<p>使用上下箭头调用以前的历史命令</p>
</li>
<li>
<p>使用<code>!n</code>重复执行第n条历史命令</p>
</li>
<li>
<p>使用<code>!!</code>重复执行上一条命令</p>
</li>
<li>
<p>使用<code>!字符</code>重复执行最后一条以该字串开头的命令</p>
</li>
</ol>
<h3 id="命令补全"><a class="markdownIt-Anchor" href="#命令补全"></a> 命令补全</h3>
<p>在bash中使用Tab键补全命令</p>
<h2 id="命令别名与常用快捷键"><a class="markdownIt-Anchor" href="#命令别名与常用快捷键"></a> 命令别名与常用快捷键</h2>
<h3 id="命令别名"><a class="markdownIt-Anchor" href="#命令别名"></a> 命令别名</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> 命令别名=<span class="string">&#x27;原命令&#x27;</span>   例如<span class="built_in">alias</span> vi = <span class="string">&#x27;vim&#x27;</span></span><br></pre></td></tr></table></figure>
<p>别名的优先级高于它本身的命令</p>
<p>想要永久生效需要写入配置文件</p>
<p><code>vim	/root/.bashrc</code></p>
<p><code>vim /user1/.bashrc</code></p>
<h3 id="bash常用快捷键"><a class="markdownIt-Anchor" href="#bash常用快捷键"></a> bash常用快捷键</h3>
<table>
<thead>
<tr>
<th style="text-align:left">快捷键</th>
<th style="text-align:left">作用</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">ctrl+A</td>
<td style="text-align:left">把光标移动到命令行开头。如果我们输入的命令过长，想要把光标移动到命令行开头时使用。</td>
</tr>
<tr>
<td style="text-align:left">ctrl+E</td>
<td style="text-align:left">把光标移动到命令行结尾。</td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+C</strong></td>
<td style="text-align:left"><strong>强制终止当前的命令。</strong></td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+L</strong></td>
<td style="text-align:left"><strong>清屏，相当于clear命令。</strong></td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+U</strong></td>
<td style="text-align:left"><strong>删除或剪切光标之前的命令。我输入了一行很长的命令，不用使用退格键一个一个字符的删除，使用这个快捷键会更加方便</strong></td>
</tr>
<tr>
<td style="text-align:left">ctrl+K</td>
<td style="text-align:left">删除或剪切光标之后的内容。</td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+Y</strong></td>
<td style="text-align:left"><strong>粘贴ctrl+U或ctul+K剪切的内容。</strong></td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+R</strong></td>
<td style="text-align:left"><strong>在历史命令中搜索，按下ctrl+R之后，就会出现搜索界面，只要输入搜索内容，就会从历史命令中搜索。</strong></td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+D</strong></td>
<td style="text-align:left"><strong>退出当前终端</strong>。</td>
</tr>
<tr>
<td style="text-align:left"><strong>ctrl+Z</strong></td>
<td style="text-align:left">暂停，并放入后台。这个快捷键牵扯工作管理的内容，我们在系统管理章节详细介绍。</td>
</tr>
<tr>
<td style="text-align:left">ctrl+S</td>
<td style="text-align:left">暂停屏幕输出。</td>
</tr>
<tr>
<td style="text-align:left">ctrl+Q</td>
<td style="text-align:left">恢复屏幕输出。</td>
</tr>
</tbody>
</table>
<h2 id="输入输出出重定向"><a class="markdownIt-Anchor" href="#输入输出出重定向"></a> 输入输出出重定向</h2>
<h3 id="标准输入输出"><a class="markdownIt-Anchor" href="#标准输入输出"></a> 标准输入输出</h3>
<table>
<thead>
<tr>
<th>文件描述符</th>
<th>文件名</th>
<th>类型</th>
<th>硬件</th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>/dev/stdin</td>
<td>标准输入文件</td>
<td>键盘</td>
</tr>
<tr>
<td>1</td>
<td>/dev/stdout</td>
<td>标准输出文件</td>
<td>显示器</td>
</tr>
<tr>
<td>2</td>
<td>/dev/stderr</td>
<td>标准错误输出文件</td>
<td>显示器</td>
</tr>
</tbody>
</table>
<h3 id="输出重定向"><a class="markdownIt-Anchor" href="#输出重定向"></a> 输出重定向</h3>
<p><img src="/2021/05/12/Shell/E:%5Cmarkdown%5C%E5%9B%BE%E5%BA%8A%5C%E8%BE%93%E5%87%BA%E9%87%8D%E5%AE%9A%E5%90%91.png" alt="输出重定向"></p>
<p>只有有输出的命令才能使用重定向</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &gt;&gt; abc   </span><br><span class="line">lst 2&gt;&gt;abc	<span class="comment">#错误输出重定向必须加2，而且不能有空格</span></span><br></pre></td></tr></table></figure>
<p>经常使用的</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &amp;&gt;&gt;文件 </span><br><span class="line">ls &gt;&gt;文件 2&gt;&amp;1</span><br><span class="line">ls &gt;&gt;def 2&gt;&gt;efg</span><br></pre></td></tr></table></figure>
<p>/dev/null是黑洞垃圾箱，不要的可以重定向到null</p>
<h3 id="输入重定向"><a class="markdownIt-Anchor" href="#输入重定向"></a> 输入重定向</h3>
<table>
<thead>
<tr>
<th>符号</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>command &lt;file</td>
<td>将 file 文件中的内容作为 command 的输入。</td>
</tr>
<tr>
<td>command &lt;&lt;END</td>
<td>从标准输入（键盘）中读取数据，直到遇见分界符 END 才停止（分界符可以是任意的字符串，用户自己定义）。</td>
</tr>
<tr>
<td>command <file1>file2</file1></td>
<td>将 file1 作为 command 的输入，并将 command 的处理结果输出到 file2。</td>
</tr>
</tbody>
</table>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Linux wc 命令可以用来对文本进行统计，包括单词个数、行数、字节数</span><br><span class="line">wc [选项] [文件名]</span><br><span class="line">	-c选项统计字节数</span><br><span class="line">	-w选项统计单词数</span><br><span class="line">	-l选项统计行数</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">例如 wc &lt; anaconda-ks.cfg</span><br></pre></td></tr></table></figure>
<h2 id="多命令顺序执行与管道符"><a class="markdownIt-Anchor" href="#多命令顺序执行与管道符"></a> 多命令顺序执行与管道符</h2>
<h3 id="多命令顺序执行"><a class="markdownIt-Anchor" href="#多命令顺序执行"></a> 多命令顺序执行</h3>
<table>
<thead>
<tr>
<th>多命令执行符</th>
<th>格式</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>；</td>
<td>命令1； 命令2</td>
<td>多个命令顺序执行，命令之间没有任何逻辑联系</td>
</tr>
<tr>
<td>&amp;&amp;</td>
<td>命令1&amp;&amp;命令2</td>
<td>逻辑与，命令1正确执行，命令2才会正确执行，命令1不正确执行，命令2也不执行</td>
</tr>
<tr>
<td>||</td>
<td>命令1||命令2</td>
<td>逻辑或，命令1不正确执行，命令2才会正确执行，命令1正确执行，命令2不执行</td>
</tr>
</tbody>
</table>
<h3 id="管道符"><a class="markdownIt-Anchor" href="#管道符"></a> 管道符</h3>
<p>命令1| 命令2<br>
将命令1的<strong>正确输出</strong>作为命令2的操作对象</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ll -a /etc/ |more</span><br></pre></td></tr></table></figure>
<h2 id="通配符与其他特殊符号"><a class="markdownIt-Anchor" href="#通配符与其他特殊符号"></a> 通配符与其他特殊符号</h2>
<h3 id="通配符"><a class="markdownIt-Anchor" href="#通配符"></a> 通配符</h3>
<p>通配符是用来匹配文件名的</p>
<table>
<thead>
<tr>
<th>通配符</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td>？</td>
<td>匹配一个任意字符</td>
</tr>
<tr>
<td>*</td>
<td>匹配0和或多个任意字符，也就是匹配任何内容</td>
</tr>
<tr>
<td>[]</td>
<td>匹配中括号中任意一个字符，例如[abc]代表一定匹配一个字符，或者是a或者是b或者是c</td>
</tr>
<tr>
<td>[-]</td>
<td>匹配中括号中任意一个字符，-代表一个范围。例如：[a-z]代表匹配一个小写字母</td>
</tr>
<tr>
<td>[^]</td>
<td>逻辑非，表示匹配不是中括号内的一个字符，例如<code>[^0-9]</code>代表匹配一个不是数字的字符</td>
</tr>
</tbody>
</table>
<h3 id="其他特殊符号"><a class="markdownIt-Anchor" href="#其他特殊符号"></a> 其他特殊符号</h3>
<table>
<thead>
<tr>
<th><code>#</code></th>
<th>注释符号(Hashmark[Comments])1.在shell文件的行首，作为shebang标记，#!/bin/bash;2. 其他地方作为注释使用，在一行中，#后面的内容并不会被执行，除非；3. 但是用单/双引号包围时，#作为#号字符本身，不具有注释作用。</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>;</code></td>
<td>作为多语句的分隔符(Command separator [semicolon])。多个语句要放在同一行的时候，可以使用分号分隔。注意，有时候分号需要转义。</td>
</tr>
<tr>
<td><strong><code>;;</code></strong></td>
<td><strong>连续分号(Terminator [double semicolon])。在使用case选项的时候，作为每个选项的终结符。在Bash version 4+ 的时候，还可以使用[;;&amp;], [;&amp;]</strong></td>
</tr>
<tr>
<td><code>.</code></td>
<td>点号(dot command [period])。1. 相当于bash内建命令source，如：<code>#!/bin/bash. data-file#包含data-file;</code>2. 作为文件名的一部分，在文件名的开头，表示该文件为隐藏文件，ls一般不显示出来（ls -a 可以显示）；3. 作为目录名，一个点代表当前目录，两个点号代表上层目录（当前目录的父目录）。注意，两个以上的点不出现，除非你用引号（单/双）包围作为点号字符本身；4. 正则表达式中，点号表示任意一个字符。</td>
</tr>
<tr>
<td><strong><code>&quot;</code></strong></td>
<td><strong>双引号（partial quoting [double quote]）。部分引用。双引号包围的内容可以允许变量扩展，也允许转义字符的存在。如果字符串内出现双引号本身，需要转义，因此不一定双引号是成对的。</strong></td>
</tr>
<tr>
<td><strong><code>'</code></strong></td>
<td><strong>单引号(full quoting [single quote])。单引号括住的内容，被视为单一字符串，引号内的禁止变量扩展，所有字符均作为字符本身处理（除单引号本身之外），单引号必须成对出现。</strong></td>
</tr>
<tr>
<td><code>,</code></td>
<td>逗号(comma operator [comma])。1. 用在连接一连串的数学表达式中，这串数学表达式均被求值，但只有最后一个求值结果被返回。如：<code>#!/bin/bashlet t1=((a=5+1, b=7+2))echo t1=$t1, a=$a, b=$b## 这个$t1=$b；</code>2. 用于参数替代中，表示首字母小写，如果是两个逗号，则表示全部小写，注意，这个特性在bash version 4的时候被添加的。例子：<code>a=&quot;ATest&quot;echo $&#123;a,&#125;echo $&#123;a,,&#125;## 前面输出aTest，后面输出的是atest。</code></td>
</tr>
<tr>
<td><code>\</code></td>
<td>反斜线，反斜杆(escape [backslash])。1. 放在特殊符号之前，转义特殊符号的作用，仅表示特殊符号本身，这在字符串中常用；2. 放在一行指令的最末端，表示紧接着的回车无效（其实也就是转义了Enter），后继新行的输入仍然作为当前指令的一部分。</td>
</tr>
<tr>
<td><code>/</code></td>
<td>斜线，斜杆（Filename path separator [forward slash]）。1.作为路径的分隔符，路径中仅有一个斜杆表示根目录，以斜杆开头的路径表示从根目录开始的路径；2.在作为运算符的时候，表示除法符号。如：<code>a=4/2</code></td>
</tr>
<tr>
<td><strong>```</strong>                  | <strong>反引号，后引号（Command substitution[backquotes])。命令替换。这个引号包围的为命令，可以执行包围的命令，并将执行的结果赋值给变量。如：<code>a=</code>dirname ‘/tmp/x.log’`` 。后面dirname返回的结果会赋值给a，注意，此处Mitchell特地使用了反引号和单引号，注意区别。</strong></td>
<td></td>
</tr>
<tr>
<td><code>:</code></td>
<td>冒号(null command [colon])。空命令，这个命令什么都不做，但是有返回值，返回值为0（即：true）。这个命令的作用非常奇妙。1. 可做while死循环的条件；2. 在if分支中作为占位符（即某一分支什么都不做的时候）；3. 放在必须要有两元操作的地方作为分隔符，如：<code>: $&#123;username=</code>whoami<code>&#125;</code>4. 在参数替换中为字符串变量赋值，在重定向操作(&gt;)中，把一个文件长度截断为0（:&gt;&gt;这样用的时候，目标存在则什么都不做），这个只能在普通文件中使用，不能在管道，符号链接和其他特殊文件中使用；5. 甚至你可以用来注释（#后的内容不会被检查，但:后的内容会被检查，如果有语句如果出现语法错误，则会报错）；6. 你也可以作为域分隔符，比如环境变量$PATH中，或者passwd中，都有冒号的作为域分隔符的存在；7. 你也可以将冒号作为函数名，不过这个会将冒号的本来意义转变（如果你不小心作为函数名，你可以使用<code>unset -f :</code>来取消function的定义）。</td>
</tr>
<tr>
<td><code>!</code></td>
<td>感叹号（reverse (or negate) [bang],[exclamation mark])。取反一个测试结果或退出状态。1. 表示反逻辑，比如后面的!=,这个是表示不等于；2. 表示取反，如：ls a[!0-9] #表示a后面不是紧接一个数字的文件；3. 在不同的环境里面，感叹号也可以出现在间接变量引用里面；4. 在命令行中，可以用于历史命令机制的调用，你可以试试!$,!#，或者!-3看看，不过要注意，这点特性不能在脚本文件里面使用（被禁用）。</td>
</tr>
<tr>
<td><code>*</code></td>
<td>星号（wildcard/arithmetic operator[asterisk])。1. 作为匹配文件名扩展的一个通配符，能自动匹配给定目录下的每一个文件；2. 正则表达式中可以作为字符限定符，表示其前面的匹配规则匹配任意次；3. 算术运算中表示乘法。</td>
</tr>
<tr>
<td><code>**</code></td>
<td>双星号(double asterisk)。算术运算中表示求幂运算。</td>
</tr>
<tr>
<td><code>?</code></td>
<td>问号（test operator/wildcard[Question mark])。1. 表示条件测试；2. 在双括号内表示C风格的三元操作符((condition?true-result:false-result))；3. 参数替换表达式中用来测试一个变量是否设置了值；4. 作为通配符，用于匹配文件名扩展特性中，用于匹配单个字符；5. 正则表达式中，表示匹配其前面规则0次或者1次。</td>
</tr>
<tr>
<td><code>$</code></td>
<td>美元符号(Variable substitution[Dollar sign])。1. 作为变量的前导符，用作变量替换，即引用一个变量的内容，比如：<code>echo $PATH</code>；2. 在正则表达式中被定义为行末（End of line）。</td>
</tr>
<tr>
<td><code>$&#123;&#125;</code></td>
<td>参数替换(Variable substitution)。用于在字符串中表示变量。</td>
</tr>
<tr>
<td><code>$‘...’</code></td>
<td>引用内容展开，执行单引号内的转义内容（单引号原本是原样引用的），这种方式会将引号内的一个或者多个[]转义后的八进制，十六进制值展开到ASCII或Unicode字符。</td>
</tr>
<tr>
<td><code>$*``$@</code></td>
<td>位置参数(Positional Parameters)。这个在使用脚本文件的时候，在传递参数的时候会用到。两者都能返回调用脚本文件的所有参数，但<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mo>∗</mo><mi mathvariant="normal">是</mi><mi mathvariant="normal">将</mi><mi mathvariant="normal">所</mi><mi mathvariant="normal">有</mi><mi mathvariant="normal">参</mi><mi mathvariant="normal">数</mi><mi mathvariant="normal">作</mi><mi mathvariant="normal">为</mi><mi mathvariant="normal">一</mi><mi mathvariant="normal">个</mi><mi mathvariant="normal">整</mi><mi mathvariant="normal">体</mi><mi mathvariant="normal">返</mi><mi mathvariant="normal">回</mi><mi mathvariant="normal">（</mi><mi mathvariant="normal">字</mi><mi mathvariant="normal">符</mi><mi mathvariant="normal">串</mi><mi mathvariant="normal">）</mi><mi mathvariant="normal">，</mi><mi mathvariant="normal">而</mi></mrow><annotation encoding="application/x-tex">*是将所有参数作为一个整体返回（字符串），而</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.46528em;vertical-align:0em;"></span><span class="mord">∗</span><span class="mord cjk_fallback">是</span><span class="mord cjk_fallback">将</span><span class="mord cjk_fallback">所</span><span class="mord cjk_fallback">有</span><span class="mord cjk_fallback">参</span><span class="mord cjk_fallback">数</span><span class="mord cjk_fallback">作</span><span class="mord cjk_fallback">为</span><span class="mord cjk_fallback">一</span><span class="mord cjk_fallback">个</span><span class="mord cjk_fallback">整</span><span class="mord cjk_fallback">体</span><span class="mord cjk_fallback">返</span><span class="mord cjk_fallback">回</span><span class="mord cjk_fallback">（</span><span class="mord cjk_fallback">字</span><span class="mord cjk_fallback">符</span><span class="mord cjk_fallback">串</span><span class="mord cjk_fallback">）</span><span class="mord cjk_fallback">，</span><span class="mord cjk_fallback">而</span></span></span></span>@是将每个参数作为单元返回一个参数列表。注意，在使用的时候需要用双引号将<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mo>∗</mo><mo separator="true">,</mo></mrow><annotation encoding="application/x-tex">*,</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6597200000000001em;vertical-align:-0.19444em;"></span><span class="mord">∗</span><span class="mpunct">,</span></span></span></span>@括住。这两个变量受到$IFS的影响，如果在实际应用中，要考虑其中的一些细节。</td>
</tr>
<tr>
<td><code>$#</code></td>
<td>表示传递给脚本的参数数量。</td>
</tr>
<tr>
<td><code>$?</code></td>
<td>此变量值在使用的时候，返回的是最后一个命令、函数、或脚本的退出状态码值，如果没有错误则是0，如果为非0，则表示在此之前的最后一次执行有错误。</td>
</tr>
<tr>
<td><code>$$</code></td>
<td>进程ID变量，这个变量保存了运行当前脚本的进程ID值。</td>
</tr>
<tr>
<td><code>()</code></td>
<td>圆括号(parentheses)。1， 命令组（Command group）。由一组圆括号括起来的命令是命令组，命令组中的命令实在子shell（subshell）中执行。因为是在子shell内运行，因此在括号外面是没有办法获取括号内变量的值，但反过来，命令组内是可以获取到外面的值，这点有点像局部变量和全局变量的关系，在实作中，如果碰到要cd到子目录操作，并在操作完成后要返回到当前目录的时候，可以考虑使用subshell来处理；2. 用于数组的初始化。</td>
</tr>
<tr>
<td><code>&#123;x,y,z,...&#125;</code></td>
<td>花括号扩展(Brace Expansion)。在命令中可以用这种扩展来扩展参数列表，命令将会依照列表中的括号分隔开的模式进行匹配扩展。注意的一点是，这花括号扩展中不能有空格存在，如果确实有必要空格，则必须被转义或者使用引号来引用。例子：<code>echo &#123;a,b,c&#125;-&#123;\ d,&quot; e&quot;,' f'&#125;</code></td>
</tr>
<tr>
<td><code>&#123;a..z&#125;</code></td>
<td>在Bash version 3时添加了这种花括号扩展的扩展，可以使用{A…Z}表示A-Z的所有字符列表，这种方式的扩展Mitchell测试了一下，好像仅适用于A-Z，a-z，还有数字{最小…最大}的这种方式扩展。</td>
</tr>
<tr>
<td><code>&#123;&#125;</code></td>
<td>代码块(curly brackets)。这个是匿名函数，但是又与函数不同，在代码块里面的变量在代码块后面仍能访问。注意：花括号内侧需要有空格与语句分隔。另外，在<code>xargs -i</code>中的话，还可以作为文本的占位符，用以标记输出文本的位置。</td>
</tr>
<tr>
<td><code>&#123;&#125; \;</code></td>
<td>这个{}是表示路径名，这个并不是shell内建的，现在接触到的情况看，好像只用在find命令里。注意后面的分号，这个是结束<code>find</code>命令中<code>-exec</code>选项的命令序列，在实际使用的时候，要转义一下以免被shell理解错误。</td>
</tr>
<tr>
<td><code>[]</code></td>
<td>中括号（brackets）。1. 测试的表示，Shell会测试在[]内的表达式，需要注意的是，[]是Shell内建的测试的一部分，而非使用外部命令<code>/usr/bin/test</code>的链接；2. 在数组的上下文中，表示数组元素，方括号内填上数组元素的位置就能获得对应位置的内容，如：<code>Array[1]=xxxecho $&#123;Array[1]&#125;;</code>3. 表示字符集的范围，在正表达式中，方括号表示该位置可以匹配的字符集范围。</td>
</tr>
<tr>
<td><code>[[]]</code></td>
<td>双中括号(double brackets)。这个结构也是测试，测试[[]]之中的表达式(Shell的关键字)。这个比单中括号更能防止脚本里面的逻辑错误，比如：&amp;&amp;,||,&lt;,&gt;操作符能在一个[[]]里面测试通过，但是在[]却不能通过。[[]]里面没有文件名扩展(filename expansion）或是词分隔符(Word splitting)，但是可以用参数扩展(Parameter expansion)和命令替换(command substitution)。不用文件名通配符和像空白这样的分隔符。注意，这里面如果出现了八进制，十六进制等，shell会自动执行转换比较。</td>
</tr>
<tr>
<td><code>$[...]</code></td>
<td>词表达表示整数扩展(integer expansion)。在方括号里面执行整数表达式。例：<code>a=3b=7echo $[$a+$b]echo $[$a*$b]##返回是10和21</code></td>
</tr>
<tr>
<td><code>(())</code></td>
<td>双括号(double parentheses)。表示整数扩展（integer expansion）。功能和上面的<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mo stretchy="false">[</mo><mo stretchy="false">]</mo><mi mathvariant="normal">差</mi><mi mathvariant="normal">不</mi><mi mathvariant="normal">多</mi><mi mathvariant="normal">，</mi><mi mathvariant="normal">但</mi><mi mathvariant="normal">是</mi><mi mathvariant="normal">需</mi><mi mathvariant="normal">要</mi><mi mathvariant="normal">注</mi><mi mathvariant="normal">意</mi><mi mathvariant="normal">的</mi><mi mathvariant="normal">是</mi><mi mathvariant="normal">，</mi></mrow><annotation encoding="application/x-tex">[]差不多，但是需要注意的是，</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mopen">[</span><span class="mclose">]</span><span class="mord cjk_fallback">差</span><span class="mord cjk_fallback">不</span><span class="mord cjk_fallback">多</span><span class="mord cjk_fallback">，</span><span class="mord cjk_fallback">但</span><span class="mord cjk_fallback">是</span><span class="mord cjk_fallback">需</span><span class="mord cjk_fallback">要</span><span class="mord cjk_fallback">注</span><span class="mord cjk_fallback">意</span><span class="mord cjk_fallback">的</span><span class="mord cjk_fallback">是</span><span class="mord cjk_fallback">，</span></span></span></span>[]是会返回里面表达式的值的，而(())只是执行，并不会返回值。两者执行后如果变量值发生变化，都会影响到后继代码的运行。可对变量赋值，可以对变量进行一目操作符操作，也可以是二目，三目操作符。</td>
</tr>
<tr>
<td><code>&gt;``&amp;&lt;``&gt;&amp;``&gt;&gt;``&lt;``&lt;&gt;</code></td>
<td>重定向(redirection)。<code>scriptname &gt;filename</code> 重定向scriptname的输出到文件filename中去，如果文件存在则覆盖；<code>command &amp;&gt;filename</code> 重定向command的标准输出(stdout)和标准错误(stderr)到文件filename中；<code>command &gt;&amp;2</code> 把command的标准输出(stdout)重定向到标准错误(stderr)中；<code>scriptname &gt;&gt;filename</code> 把scriptname的输出（同&gt;)追加到文件filenmae中，如果文件不存在则创建。<code>[i]&lt;&gt;filename</code> 打开filename这个文件用来读或者写，并且给文件指定i为它的文件描述符(file descriptor)，文件不存在就会创建。</td>
</tr>
<tr>
<td><code>(command)&gt;``&lt;(command)</code></td>
<td>这是进程替换(Process Substitution)。使用的时候注意，括号和&lt;,&gt;之间是不能有空格的，否则报错。其作用有点类似通道，但和管道在用法上又有些不同，管道是作为子进程的方式来运行的，这个命令会在<code>/dev/fd/</code>下面产生类似<code>/dev/fd/63</code>,<code>/dev/fd/62</code>这类临时文件，用来传递数据。Mitchell个人猜测之所以用这种方法来传递，是因为前后两个不属于同一个进程，因此需要用共享文件的方式来传递资料(这么说其实管道也应该有同样的文件?)。网上有人说这个只是共享文件而已，但是经过测试，发现虽然有<code>/dev/fd/63</code>这样的文件产生，但是这个文件其实是指向pipe:[43434]这样的通道的链接。</td>
</tr>
<tr>
<td><code>&lt;&lt;</code></td>
<td>双小于号(here-document[double less then marks])。这个也被称为Here-document，用来将后继的内容重定向到左侧命令的stdin中。&lt;&lt;可以节省格式化时间，别且使命令执行的处理更容易。在实作的时候只需要输入&lt;&lt;和终止标志符，而后（一般是回车后）你就可以输入任何内容，只要在最后的新行中输入终止标志符，即可完成数据的导入。使用here-document的时候，你可以保留空格，换行等。如果要让shell脚本更整洁一点，可以在&lt;&lt;和终止符之间放上一个连字符(-)。</td>
</tr>
<tr>
<td><code>&lt;&lt;&lt;</code></td>
<td>三个小于号(here-strings)。Here-字串和Here-document类似，here-strings语法：<code>command [args] &lt;&lt;&lt;[&quot;]$word[&quot;]</code>；$word会展开并作为command的stdin。</td>
</tr>
<tr>
<td><code>&lt;``&gt;</code></td>
<td>小于，大于号(ASCII Comparison)。ASCII比较，进行的是变量的ASCII比较，字串？数字?呃…这个…不就是ASCII比较么？</td>
</tr>
<tr>
<td><code>\&lt;...\&gt;</code></td>
<td>词界符(word boundary)。这个是用在正则表达式中的一个特殊分隔符，用来标记单词的分界。比如：the会匹配there，another，them等等，如果仅仅要匹配the，就可以使用这个词界符，&lt;the&gt;就只能匹配the了。</td>
</tr>
<tr>
<td><code>|</code></td>
<td>管道(pipe)。管道是Linux，Unix都有的概念，是非常基础，也是非常重要的一个概念。它的作用是将管道前（左边）的命令产生的输出(stdout)作为管道后（右边）的命令的输入(stdin)。如：<code>ls | wc l</code>，使用管道就可以将命令连接在一起。注意：管道是每一个进程的标准输出都会作为下一个命令的标准输入，期间的标准输出不能跨越管道作为后继命令的标准输入，如： <code>cat filename | ls -al | sort</code> 。想想这个的输出? 同时，管道是以子进程来运行的，所以管道并不能引起变量改变。</td>
</tr>
<tr>
<td><code>&gt;|</code></td>
<td>强制重定向(force redirection)。这会强制重写已经存在的文件。</td>
</tr>
<tr>
<td><code>&amp;</code></td>
<td>与号(Run job in background[ampersand])。如果命令后面跟上一个&amp;符号，这个命令将会在后台运行。有的时候，脚本中在一条在后台运行的命令可能会引起脚本挂起，等待输入，出现这种情况可以在原有的脚本后面使用wait命令来修复。</td>
</tr>
<tr>
<td><code>&amp;&amp;``||</code></td>
<td>逻辑操作符(logical operator)。在测试结构中，可以用这两个操作符来进行连接两个逻辑值。||是当测试条件有一个为真时返回0（真），全假为假；&amp;&amp;是当测试条件两个都为真时返回真(0)，有假为假。</td>
</tr>
<tr>
<td><code>-</code></td>
<td>减号，连字符(Hyphen/minus/dash)。1. 作为选项，前缀[option, prefix]使用。用于命令或者过滤器的选项标志；操作符的前缀。如：<code>## COMMAND -[选项列表]ls -alsort -dfu $fileset -- $variable if [ $file -ot $file2 ]then echo &quot;$file is older than $file2.&quot;fi</code>2. 用于stdin或者stdout的重定向的源或目的[dash].在tar没有bunzip2的程序补丁时，我们可以这样： <code>bunzip2 linux-2.6.13.tar.bz2 | tar xvf -</code>。将前面解压的数据作为tar的标准输入（这里使用一个-表示）注意：在实作的时候，如果文件名是以[-]开头的，那么在加上这个作为定向操作符的时候，可能会出错，此时应该为文件加上合适的前缀路径，以避免这种情况发生，同样的，在echo变量的时候，如果变量是以[-]开始，那么可能也会产生意想不到的结果，为了保险起见，可以使用双引号引用标量：<code>var=&quot;-n&quot;echo $var## 试试看有什么输出？</code>还有，这种表示方法不是Bash内建的，要达到此点的这种效果，需要看你使用的软件是否支持这种操作；3. 表示先前的工作目录(previous working directory)，因此，如果你cd到其他目录下要放回前一个路径的时候，可以使用cd -来达到目的，其实，这里的[-]使用的是环境变量的$OLDPWD，注意：这里的[-]和前一点是不同的；4. 减号或者负号，用在算术操作中。</td>
</tr>
<tr>
<td><code>=</code></td>
<td>等号(Equals)。1. 赋值操作，给变量赋值，么有空格在等号两侧；2. 在比较测试中作为比较符出现，这里要注意，如果在中括号中作为比较出现，需要有空格符在等号左右两侧。</td>
</tr>
<tr>
<td><code>+</code></td>
<td>加号(Plus)。1. 算术操作符，表示加法；2. 在正则表达式中，表示的是其前的这个匹配规则匹配最少一次;3.在命令或过滤器中作为选项标记，在某些命令或者内置命令中使用+来启用某些选项，使用-来禁止；4. 在参数替换(parameter substitution)中，+前缀表示替代值(当变量为空的时候，使用+后面的值)</td>
</tr>
<tr>
<td><code>%</code></td>
<td>百分号(modulo[percent sign])。1.在算术运算中，这个是求模操作符，即两个数进行除法运算后的余数；2. 在参数替换(parameter substitution)中，可以作为模式匹配。例子：<code>p=b*9var=&quot;abcd12345abc479&quot;echo $&#123;var%p&#125;, $&#123;var%%p&#125;##从右边开始查找(想想从左是那个符号?)##任何在b和9之间的内容（含）##第一个是找到最短的符合匹配项##后一个是找最大符合的匹配项（贪婪匹配?)</code></td>
</tr>
<tr>
<td><code>~</code></td>
<td>波浪号(Home directory[tilde])。这个和内部变量$HOME是一样的。默认表示当前用户的家目录（主目录），这个和~/效果一致，如果波浪号后面跟用户名，表示是该用户的家目录。</td>
</tr>
<tr>
<td><code>~+</code></td>
<td>当前的工作目录(current working directory)。这个和内置变量$PWD一样。</td>
</tr>
<tr>
<td><code>~-</code></td>
<td>前一个工作目录(previous working directory)。这个和内部变量$OLDPWD一致，之前的[-]也一样。</td>
</tr>
<tr>
<td><code>=~</code></td>
<td>Bash 版本3中有介绍，这个是正则表达式匹配。可用在[[]]测试中，比如：<code>var=&quot;this is a test message.&quot;[[ &quot;$var&quot; =~ tf*message ]] &amp;&amp; echo &quot;Sir. Found that.&quot; || echo &quot;Sorry Sir. No match be found.&quot;##你可以修改中间的正则表达式匹配项，正则表达式可以但不一定需要使用双引号括起来。</code></td>
</tr>
<tr>
<td><code>^</code></td>
<td>脱字符(caret)。1. 在正则表达式中，作为一行的行首(beginning-of-line)位置标志符；2. 在参数替换(Parameter substitution)中，这个用法有两种，一个脱字符(<code>$&#123;var^&#125;</code>)，或两个(<code>$&#123;var^^&#125;</code>)，分别表示第一个字母大写，全部大写的意思(Bash version &gt;=4)。</td>
</tr>
<tr>
<td><code>空白</code></td>
<td>空白符(Whitespace)。空白符不仅仅是指空格(spaces)，还包括制表符(tabs)，空行(blank lines)，或者这几种的组合。可用做函数的分隔符,分隔命令或变量，空行不会影响脚本的行为，因此可以用它来规划脚本代码，以增加可读性，在内置的特殊变量$IFS可以用来针对某些命令进行输入的参数进行分割，其默认就是空白符。在字符串或变量中如果有空白符，可以使用引号来规避可能的错误。</td>
</tr>
</tbody>
</table>
]]></content>
  </entry>
  <entry>
    <title>selenium</title>
    <url>/2021/05/12/selenium/</url>
    <content><![CDATA[<h1 id="selenium"><a class="markdownIt-Anchor" href="#selenium"></a> selenium</h1>
<h2 id="webdriver对象"><a class="markdownIt-Anchor" href="#webdriver对象"></a> WebDriver对象</h2>
<p><code>webdriver</code>就是浏览器本身</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">wd = webdriver.Edge(<span class="string">&quot;path to webdriver.exe&quot;</span>) //实例化，选择对应浏览器的webdriver.exe 返回值是WebDriver对象</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="built_in">type</span>(wd)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>&lt;<span class="class"><span class="keyword">class</span> &#x27;<span class="title">selenium</span>.<span class="title">webdriver</span>.<span class="title">edge</span>.<span class="title">webdriver</span>.<span class="title">WebDriver</span>&#x27;&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="webelement对象"><a class="markdownIt-Anchor" href="#webelement对象"></a> WebElement对象</h2>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">wd.get(<span class="string">&quot;https://www.baidu.com&quot;</span>)</span><br><span class="line">element = wd.find_element_by_id(<span class="string">&quot;kw&quot;</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="built_in">type</span>(element)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>&lt;<span class="class"><span class="keyword">class</span> &#x27;<span class="title">selenium</span>.<span class="title">webdriver</span>.<span class="title">remote</span>.<span class="title">webelement</span>.<span class="title">WebElement</span>&#x27;&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="implicitly_wait"><a class="markdownIt-Anchor" href="#implicitly_wait"></a> implicitly_wait()</h2>
<p>程序执行的速度是很快的，有时候会超过服务器的响应时间，导致定位元素会出现报错，<code>NoSuchElementException</code></p>
<p><code>implicitly_wait(10)</code>可以隐式的设置等待描述，一但程序设置了<code>implicitly_wait()</code>那么定位元素的操作在服务器响应速度慢的时候不会立刻抛出错误，而是每隔半秒去操作，直至10秒（设置的描述）过后或者没有找到元素的时候才抛出错误</p>
<h2 id="操纵元素"><a class="markdownIt-Anchor" href="#操纵元素"></a> 操纵元素</h2>
<h3 id="webelement方法"><a class="markdownIt-Anchor" href="#webelement方法"></a> WebElement方法</h3>
<p><code>click()</code></p>
<p><code>text</code></p>
<h3 id="webdriver方法"><a class="markdownIt-Anchor" href="#webdriver方法"></a> WebDriver方法</h3>
<p><code>send_keys()</code></p>
<p><code>quit()</code></p>
<h3 id="获取整个元素对应的html"><a class="markdownIt-Anchor" href="#获取整个元素对应的html"></a> 获取整个元素对应的HTML</h3>
<p>要获取整个元素对应的HTML文本内容，可以使用 <code>element.get_attribute('outerHTML')</code></p>
<p>如果，只是想获取某个元素 <code>内部</code> 的HTML文本内容，可以使用 <code>element.get_attribute('innerHTML')</code></p>
<h2 id="css-selector"><a class="markdownIt-Anchor" href="#css-selector"></a> CSS Selector</h2>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">find_element_by_css_selector(CSS Selector参数)</span><br></pre></td></tr></table></figure>
<p>选择所有元素的方法是</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">find_elements_by_css_selector(CSS Selector参数)</span><br></pre></td></tr></table></figure>
<h3 id="根据-tag名-id-class-选择元素"><a class="markdownIt-Anchor" href="#根据-tag名-id-class-选择元素"></a> 根据 tag名、id、class 选择元素</h3>
<h4 id="class"><a class="markdownIt-Anchor" href="#class"></a> class</h4>
<p>注意<code>.</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">elements = wd.find_elements_by_css_selector(<span class="string">&#x27;.plant&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h4 id="tag"><a class="markdownIt-Anchor" href="#tag"></a> tag</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">elements = wd.find_elements_by_css_selector(<span class="string">&#x27;div&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h4 id="id"><a class="markdownIt-Anchor" href="#id"></a> id</h4>
<p>注意<code>#</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">element = wd.find_element_by_css_selector(<span class="string">&#x27;#searchtext&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="选择-子元素-和-后代元素"><a class="markdownIt-Anchor" href="#选择-子元素-和-后代元素"></a> 选择 子元素 和 后代元素</h3>
<p>直接子元素是父亲和儿子关系的元素，<em><strong>必须相邻</strong></em></p>
<p>后代元素是孙子，重孙子等关系，不用相邻</p>
<h4 id="如果-元素2-是-元素1-的-直接子元素"><a class="markdownIt-Anchor" href="#如果-元素2-是-元素1-的-直接子元素"></a> 如果 <code>元素2</code> 是 <code>元素1</code> 的 <em><strong>直接子元素</strong></em></h4>
<p>CSS Selector 选择子元素的语法是这样的</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">元素<span class="number">1</span> &gt; 元素<span class="number">2</span></span><br></pre></td></tr></table></figure>
<p>中间用一个大于号 （我们可以理解为箭头号）</p>
<p>注意，最终选择的元素是 <strong>元素2</strong>， 并且要求这个 <strong>元素2</strong> 是 <strong>元素1</strong> 的***直接子元素***</p>
<p>也支持更多层级的选择， 比如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">元素1 &gt; 元素2 &gt; 元素3 &gt; 元素4</span><br></pre></td></tr></table></figure>
<p>就是选择 <code>元素1</code> 里面的子元素 <code>元素2</code> 里面的子元素 <code>元素3</code> 里面的子元素 <code>元素4</code> ， 最终选择的元素是 <strong>元素4</strong></p>
<h4 id="如果-元素2-是-元素1-的-后代元素"><a class="markdownIt-Anchor" href="#如果-元素2-是-元素1-的-后代元素"></a> 如果 <code>元素2</code> 是 <code>元素1</code> 的 <em><strong>后代元素</strong></em>，</h4>
<p>CSS Selector 选择后代元素的语法是这样的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">元素1   元素2</span><br></pre></td></tr></table></figure>
<p>中间是一个或者多个空格隔开</p>
<p>最终选择的元素是 <strong>元素2</strong> ， 并且要求这个 <strong>元素2</strong> 是 <strong>元素1</strong> 的***后代元素***。</p>
<p>也支持更多层级的选择， 比如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">元素1   元素2   元素3  元素4</span><br></pre></td></tr></table></figure>
<p>最终选择的元素是 <strong>元素4</strong></p>
<h3 id="其他属性"><a class="markdownIt-Anchor" href="#其他属性"></a> 其他属性</h3>
<p>id、class 都是web元素的 <code>属性</code> ，因为它们是很常用的属性，所以css选择器专门提供了根据 id、class 选择的语法。</p>
<p><em><strong>其他属性可以根据方括号 <code>[]</code>来选择</strong></em></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">element = wd.find_element_by_css_selector(<span class="string">&#x27;[href=&quot;http://www.miitbeian.gov.cn&quot;]&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="组合使用"><a class="markdownIt-Anchor" href="#组合使用"></a> 组合使用</h3>
<p>如果一个元素具有多个属性</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;misc&quot;</span> <span class="attr">ctype</span>=<span class="string">&quot;gun&quot;</span>&gt;</span>沙漠之鹰<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>CSS 选择器 可以指定 选择的元素要 同时具有多个属性的限制，像这样 <code>div[class=misc][ctype=gun]</code></p>
<h3 id="组选择"><a class="markdownIt-Anchor" href="#组选择"></a> 组选择</h3>
<p>同时选择所有class 为 plant <code>和</code> class 为 animal 的元素。</p>
<p>这种情况，css选择器可以 使用 <code>逗号</code> ，称之为 组选择（<mark><strong>或</strong>的关系</mark>）</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">.plant , .animal</span><br></pre></td></tr></table></figure>
<p>再比如，我们要同时选择所有tag名为div的元素 <code>和</code> id为BYHY的元素，就可以像这样写</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">div,#BYHY</span><br></pre></td></tr></table></figure>
<p>对应的selenium代码如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">elements = wd.find_elements_by_css_selector(<span class="string">&#x27;div,#BYHY&#x27;</span>)</span><br><span class="line"><span class="keyword">for</span> element <span class="keyword">in</span> elements:</span><br><span class="line">    <span class="built_in">print</span>(element.text)</span><br></pre></td></tr></table></figure>
<h3 id="按次序选择元素"><a class="markdownIt-Anchor" href="#按次序选择元素"></a> 按次序选择元素</h3>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span>  </span><br><span class="line">   <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&#x27;t1&#x27;</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">h3</span>&gt;</span> 唐诗 <span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">span</span>&gt;</span>李白<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>静夜思<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">span</span>&gt;</span>杜甫<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>春夜喜雨<span class="tag">&lt;/<span class="name">p</span>&gt;</span>              </span><br><span class="line">   <span class="tag">&lt;/<span class="name">div</span>&gt;</span>      </span><br><span class="line">    </span><br><span class="line">   <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&#x27;t2&#x27;</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">h3</span>&gt;</span> 宋词 <span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">span</span>&gt;</span>苏轼<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>赤壁怀古<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>明月几时有<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>江城子·乙卯正月二十日夜记梦<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>蝶恋花·春景<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">span</span>&gt;</span>辛弃疾<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>京口北固亭怀古<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>青玉案·元夕<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">       <span class="tag">&lt;<span class="name">p</span>&gt;</span>西江月·夜行黄沙道中<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">div</span>&gt;</span>             </span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>我们可以指定选择的元素 <code>是父元素的第几个子节点</code></p>
<p><mark><strong>使用 <code>nth-child</code></strong></mark></p>
<p>比如，</p>
<p>我们要选择 唐诗 和宋词 的第一个 作者，</p>
<p>也就是说 选择的是 第2个子元素，并且是span类型</p>
<p>所以这样可以这样写 <code>span:nth-child(2)</code> ，</p>
<p>如果你不加节点类型限制，直接这样写 <code>:nth-child(2)</code></p>
<p>就是选择所有位置为第2个的所有元素，不管是什么类型</p>
<p>也可以反过来， 选择的是父元素的 <code>倒数第几个子节点</code> ，</p>
<p><mark><strong>使用 <code>nth-last-child</code></strong></mark></p>
<p>比如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">p:nth-last-child(1)</span><br></pre></td></tr></table></figure>
<p>就是选择第倒数第1个子元素，并且是p元素</p>
<p>我们可以指定选择的元素 是父元素的第几个 <code>某类型的</code> 子节点</p>
<p><mark><strong>使用 <code>nth-of-type</code></strong></mark></p>
<p>比如，</p>
<p>我们要选择 唐诗 和宋词 的第一个 作者，</p>
<p>可以像上面那样思考：选择的是 第2个子元素，并且是span类型</p>
<p>所以这样可以这样写 <code>span:nth-child(2)</code> ，</p>
<p>还可以这样思考，选择的是 <code>第1个span类型</code> 的子元素</p>
<p>所以也可以这样写 <code>span:nth-of-type(1)</code></p>
<p>当然也可以反过来， 选择父元素的 <code>倒数第几个某类型</code> 的子节点</p>
<p><mark><strong>使用 <code>nth-last-of-type</code></strong></mark></p>
<p>像这样</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">p:nth-last-of-type(2)</span><br></pre></td></tr></table></figure>
<p>如果要选择的是父元素的 <code>偶数节点</code>，</p>
<p><mark><strong>使用 <code>nth-child(even)</code></strong></mark></p>
<p>比如</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">p:nth-child(even)</span><br></pre></td></tr></table></figure>
<p>如果要选择的是父元素的 <code>奇数节点</code>，使用 <code>nth-child(odd)</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">p:nth-child(odd)</span><br></pre></td></tr></table></figure>
<p>如果要选择的是父元素的 <code>某类型偶数节点</code>，使用 <mark><strong><code>nth-of-type(even)</code></strong></mark></p>
<p>如果要选择的是父元素的 <code>某类型奇数节点</code>，使用 <mark><strong><code>nth-of-type(odd)</code></strong></mark></p>
<h2 id="frame切换"><a class="markdownIt-Anchor" href="#frame切换"></a> frame切换</h2>
<p>在html语法中，frame 元素 或者iframe元素的内部 会包含一个 <strong>被嵌入的</strong> 另一份html文档。</p>
<p>在我们使用selenium打开一个网页是， 我们的操作范围 缺省是当前的 html ， 并不包含被嵌入的html文档里面的内容。</p>
<p>如果我们要 操作 被嵌入的 html 文档 中的元素， 就必须 <code>切换操作范围</code> 到 被嵌入的文档中。</p>
<p><code>wd.switch_to.frame(frame_reference)</code></p>
<p>其中， frame_reference 可以是 frame 元素的属性 name 或者 ID 。</p>
<p>如果我们已经切换到某个iframe里面进行操作了，那么后续选择和操作界面元素 就都是在这个frame里面进行的。</p>
<p>这时候，如果我们又需要操作 主html（我们把最外部的html称之为主html） 里面的元素了呢？</p>
<p><strong>怎么切换回原来的主html呢？</strong></p>
<p>很简单，写如下代码即可</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">wd.switch_to.default_content()</span><br></pre></td></tr></table></figure>
<h2 id="窗口切换"><a class="markdownIt-Anchor" href="#窗口切换"></a> 窗口切换</h2>
<p>如果我们要到新的窗口里面操作，该怎么做呢？</p>
<p>可以使用Webdriver对象的switch_to属性的 window方法，如下所示：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">wd.switch_to.window(handle)</span><br></pre></td></tr></table></figure>
<p>其中，参数handle需要传入什么呢？</p>
<p>WebDriver对象有window_handles 属性，这是一个列表对象， 里面包括了当前浏览器里面<strong>所有的窗口句柄</strong>。</p>
<p>所谓句柄，大家可以想象成对应网页窗口的一个ID</p>
<p>那么我们就可以通过 类似下面的代码，</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> handle <span class="keyword">in</span> wd.window_handles:</span><br><span class="line">    <span class="comment"># 先切换到该窗口</span></span><br><span class="line">    wd.switch_to.window(handle)</span><br><span class="line">    <span class="comment"># 得到该窗口的标题栏字符串，判断是不是我们要操作的那个窗口</span></span><br><span class="line">    <span class="keyword">if</span> <span class="string">&#x27;Bing&#x27;</span> <span class="keyword">in</span> wd.title:</span><br><span class="line">        <span class="comment"># 如果是，那么这时候WebDriver对象就是对应的该该窗口，正好，跳出循环，</span></span><br><span class="line">        <span class="keyword">break</span></span><br></pre></td></tr></table></figure>
<p>上面代码的用意就是：</p>
<p>我们依次获取 wd.window_handles 里面的所有 句柄 对象， 并且调用 wd.switch_to.window(handle) 方法，切入到每个窗口，</p>
<p>然后检查里面该窗口对象的属性（可以是标题栏，地址栏），判断是不是我们要操作的那个窗口，如果是，就跳出循环。</p>
<p>同样的，如果我们在新窗口 操作结束后， 还要回到原来的窗口，该怎么办？</p>
<p>我们可以仍然使用上面的方法，依次切入窗口，然后根据 标题栏 之类的属性值判断。</p>
<p>还有更省事的方法。</p>
<p>因为我们一开始就在 原来的窗口里面，我们知道 进入新窗口操作完后，还要回来，可以事先 保存该老窗口的 句柄，使用如下方法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># mainWindow变量保存当前窗口的句柄</span></span><br><span class="line">mainWindow = wd.current_window_handle</span><br></pre></td></tr></table></figure>
<p>切换到新窗口操作完后，就可以直接像下面这样，将driver对应的对象返回到原来的窗口</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#通过前面保存的老窗口的句柄，自己切换到老窗口</span></span><br><span class="line">wd.switch_to.window(mainWindow)</span><br></pre></td></tr></table></figure>
<h2 id="选择框"><a class="markdownIt-Anchor" href="#选择框"></a> 选择框</h2>
<h3 id="radio框"><a class="markdownIt-Anchor" href="#radio框"></a> radio框</h3>
<p>radio框选择选项，直接用WebElement的click方法，模拟用户点击就可以了。</p>
<p>比如, 我们要在下面的html中：</p>
<ul>
<li>先打印当前选中的老师名字</li>
<li>再选择 小雷老师</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;s_radio&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;radio&quot;</span> <span class="attr">name</span>=<span class="string">&quot;teacher&quot;</span> <span class="attr">value</span>=<span class="string">&quot;小江老师&quot;</span>&gt;</span>小江老师<span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;radio&quot;</span> <span class="attr">name</span>=<span class="string">&quot;teacher&quot;</span> <span class="attr">value</span>=<span class="string">&quot;小雷老师&quot;</span>&gt;</span>小雷老师<span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;radio&quot;</span> <span class="attr">name</span>=<span class="string">&quot;teacher&quot;</span> <span class="attr">value</span>=<span class="string">&quot;小凯老师&quot;</span> <span class="attr">checked</span>=<span class="string">&quot;checked&quot;</span>&gt;</span>小凯老师</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>对应的代码如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 获取当前选中的元素</span></span><br><span class="line">element = wd.find_element_by_css_selector(</span><br><span class="line">  <span class="string">&#x27;#s_radio input[checked=checked]&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;当前选中的是: &#x27;</span> + element.get_attribute(<span class="string">&#x27;value&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 点选 小雷老师</span></span><br><span class="line">wd.find_element_by_css_selector(</span><br><span class="line">  <span class="string">&#x27;#s_radio input[value=&quot;小雷老师&quot;]&#x27;</span>).click()</span><br></pre></td></tr></table></figure>
<h3 id="checkbox框"><a class="markdownIt-Anchor" href="#checkbox框"></a> checkbox框</h3>
<p>对checkbox进行选择，也是直接用 WebElement 的 click 方法，模拟用户点击选择。</p>
<p>需要注意的是，要选中checkbox的一个选项，必须 <code>先获取当前该复选框的状态</code> ，如果该选项已经勾选了，就不能再点击。否则反而会取消选择。</p>
<p>比如, 我们要在下面的html中：选中 小雷老师</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;s_checkbox&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">name</span>=<span class="string">&quot;teacher&quot;</span> <span class="attr">value</span>=<span class="string">&quot;小江老师&quot;</span>&gt;</span>小江老师<span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">name</span>=<span class="string">&quot;teacher&quot;</span> <span class="attr">value</span>=<span class="string">&quot;小雷老师&quot;</span>&gt;</span>小雷老师<span class="tag">&lt;<span class="name">br</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">name</span>=<span class="string">&quot;teacher&quot;</span> <span class="attr">value</span>=<span class="string">&quot;小凯老师&quot;</span> <span class="attr">checked</span>=<span class="string">&quot;checked&quot;</span>&gt;</span>小凯老师</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>我们的思路可以是这样：</p>
<ul>
<li>先把 已经选中的选项全部点击一下，确保都是未选状态</li>
<li>再点击 小雷老师</li>
</ul>
<p>示例代码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 先把 已经选中的选项全部点击一下</span></span><br><span class="line">elements = wd.find_elements_by_css_selector(</span><br><span class="line">  <span class="string">&#x27;#s_checkbox input[checked=&quot;checked&quot;]&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> element <span class="keyword">in</span> elements:</span><br><span class="line">    element.click()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 再点击 小雷老师</span></span><br><span class="line">wd.find_element_by_css_selector(</span><br><span class="line">  <span class="string">&quot;#s_checkbox input[value=&#x27;小雷老师&#x27;]&quot;</span>).click()</span><br></pre></td></tr></table></figure>
<h3 id="select框"><a class="markdownIt-Anchor" href="#select框"></a> select框</h3>
<p>radio框及checkbox框都是input元素，只是里面的type不同而已。</p>
<p>select框 则是一个新的select标签，大家可以对照浏览器网页内容查看一下</p>
<p>对于Select 选择框， Selenium 专门提供了一个 <mark><strong><code>Select类</code></strong></mark> 进行操作。</p>
<p>Select类 提供了如下的方法</p>
<ul>
<li>select_by_value</li>
</ul>
<p>根据选项的 <code>value属性值</code> ，选择元素。</p>
<p>比如，下面的HTML，</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">&quot;foo&quot;</span>&gt;</span>Bar<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>就可以根据 foo 这个值选择该选项，</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">s.select_by_value(<span class="string">&#x27;foo&#x27;</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li>select_by_index</li>
</ul>
<p>根据选项的 <code>次序</code> （从0开始），选择元素</p>
<ul>
<li>select_by_visible_text</li>
</ul>
<p>根据选项的 <code>可见文本</code> ，选择元素。</p>
<p>比如，下面的HTML，</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">&quot;foo&quot;</span>&gt;</span>Bar<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>就可以根据 Bar 这个内容，选择该选项</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">s.select_by_visible_text(<span class="string">&#x27;Bar&#x27;</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li>deselect_by_value</li>
</ul>
<p>根据选项的value属性值， <code>去除</code> 选中元素</p>
<ul>
<li>deselect_by_index</li>
</ul>
<p>根据选项的次序，<code>去除</code> 选中元素</p>
<ul>
<li>deselect_by_visible_text</li>
</ul>
<p>根据选项的可见文本，<code>去除</code> 选中元素</p>
<ul>
<li>deselect_all</li>
</ul>
<p><code>去除</code> 选中所有元素</p>
<h3 id="select单选框"><a class="markdownIt-Anchor" href="#select单选框"></a> Select单选框</h3>
<p>对于 select单选框，操作比较简单：</p>
<p>不管原来选的是什么，直接用Select方法选择即可。</p>
<p>例如，选择示例里面的小雷老师，示例代码如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入Select类</span></span><br><span class="line"><span class="keyword">from</span> selenium.webdriver.support.ui <span class="keyword">import</span> Select</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建Select对象</span></span><br><span class="line">select = Select(wd.find_element_by_id(<span class="string">&quot;ss_single&quot;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 通过 Select 对象选中小雷老师</span></span><br><span class="line">select.select_by_visible_text(<span class="string">&quot;小雷老师&quot;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="select多选框"><a class="markdownIt-Anchor" href="#select多选框"></a> Select多选框</h3>
<p>对于select多选框，要选中某几个选项，要注意去掉原来已经选中的选项。</p>
<p>例如，我们选择示例多选框中的 小雷老师 和 小凯老师</p>
<p>可以用select类 的deselect_all方法，清除所有 已经选中 的选项。</p>
<p>然后再通过 select_by_visible_text方法 选择 小雷老师 和 小凯老师。</p>
<p>示例代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入Select类</span></span><br><span class="line"><span class="keyword">from</span> selenium.webdriver.support.ui <span class="keyword">import</span> Select</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建Select对象</span></span><br><span class="line">select = Select(wd.find_element_by_id(<span class="string">&quot;ss_multi&quot;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清除所有 已经选中 的选项</span></span><br><span class="line">select.deselect_all()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 选择小雷老师 和 小凯老师</span></span><br><span class="line">select.select_by_visible_text(<span class="string">&quot;小雷老师&quot;</span>)</span><br><span class="line">select.select_by_visible_text(<span class="string">&quot;小凯老师&quot;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="actionchains"><a class="markdownIt-Anchor" href="#actionchains"></a> ActionChains</h2>
<p>动作模拟</p>
<p>比如：比如 鼠标右键点击、双击、移动鼠标到某个元素、鼠标拖拽等。</p>
<p>这些操作，可以通过 Selenium 提供的 <code>ActionChains</code> 类来实现。</p>
<p><strong><code>from selenium.webdriver.common.action_chains import ActionChains</code></strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"></span><br><span class="line">driver = webdriver.Chrome(<span class="string">r&#x27;f:\chromedriver.exe&#x27;</span>)</span><br><span class="line">driver.implicitly_wait(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line">driver.get(<span class="string">&#x27;https://www.baidu.com/&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> selenium.webdriver.common.action_chains <span class="keyword">import</span> ActionChains</span><br><span class="line"></span><br><span class="line">ac = ActionChains(driver)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 鼠标移动到 元素上</span></span><br><span class="line">ac.move_to_element(</span><br><span class="line">    driver.find_element_by_css_selector(<span class="string">&#x27;[name=&quot;tj_briicon&quot;]&#x27;</span>)</span><br><span class="line">).perform()</span><br></pre></td></tr></table></figure>
<p><mark><strong>一定要有<code>perform()</code></strong></mark></p>
<h2 id="冻结网页"><a class="markdownIt-Anchor" href="#冻结网页"></a> 冻结网页</h2>
<p><code>setTimeout(function()&#123;debugger&#125;, 5000)</code></p>
<p>表示在 5000毫秒后，执行 debugger 命令</p>
<p>执行该命令会 浏览器会进入debug状态。 debug状态有个特性， 界面被冻住， 不管我们怎么点击界面都不会触发事件。</p>
<h2 id="弹出对话框"><a class="markdownIt-Anchor" href="#弹出对话框"></a> 弹出对话框</h2>
<h3 id="alert"><a class="markdownIt-Anchor" href="#alert"></a> Alert</h3>
<p>Alert 弹出框，目的就是显示通知信息，只需用户看完信息后，点击 OK（确定） 就可以了。</p>
<p>那么，自动化的时候，代码怎么模拟用户点击 OK 按钮呢？</p>
<p>selenium提供如下方法进行操作</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.switch_to.alert.accept()</span><br></pre></td></tr></table></figure>
<p>注意：如果我们不去点击它，页面的其它元素是不能操作的。 {: .notice–info}</p>
<p>如果程序要获取弹出对话框中的信息内容， 可以通过 如下代码</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.switch_to.alert.text</span><br></pre></td></tr></table></figure>
<p>示例代码如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line">driver = webdriver.Chrome()</span><br><span class="line">driver.implicitly_wait(<span class="number">5</span>)</span><br><span class="line">driver.get(<span class="string">&#x27;http://cdn1.python3.vip/files/selenium/test4.html&#x27;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># --- alert ---</span></span><br><span class="line">driver.find_element_by_id(<span class="string">&#x27;b1&#x27;</span>).click()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印 弹出框 提示信息</span></span><br><span class="line"><span class="built_in">print</span>(driver.switch_to.alert.text) </span><br><span class="line"></span><br><span class="line"><span class="comment"># 点击 OK 按钮</span></span><br><span class="line">driver.switch_to.alert.accept()</span><br></pre></td></tr></table></figure>
<h3 id="confirm"><a class="markdownIt-Anchor" href="#confirm"></a> Confirm</h3>
<p>Confirm弹出框，主要是让用户确认是否要进行某个操作。</p>
<p>比如：当管理员在网站上选择删除某个账号时，就可能会弹出 Confirm弹出框， 要求确认是否确定要删除。</p>
<p>Confirm弹出框 有两个选择供用户选择，分别是 OK 和 Cancel， 分别代表 确定 和 取消 操作。</p>
<p>那么，自动化的时候，代码怎么模拟用户点击 OK 或者 Cancel 按钮呢？</p>
<p>selenium提供如下方法进行操作</p>
<p>如果我们想点击 OK 按钮， 还是用刚才的 accept方法，如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.switch_to.alert.accept()</span><br></pre></td></tr></table></figure>
<p>如果我们想点击 Cancel 按钮， 可以用 dismiss方法，如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.switch_to.alert.dismiss()</span><br></pre></td></tr></table></figure>
<p>示例代码如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line">driver = webdriver.Chrome()</span><br><span class="line">driver.implicitly_wait(<span class="number">5</span>)</span><br><span class="line">driver.get(<span class="string">&#x27;http://cdn1.python3.vip/files/selenium/test4.html&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># --- confirm ---</span></span><br><span class="line">driver.find_element_by_id(<span class="string">&#x27;b2&#x27;</span>).click()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印 弹出框 提示信息</span></span><br><span class="line"><span class="built_in">print</span>(driver.switch_to.alert.text)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 点击 OK 按钮 </span></span><br><span class="line">driver.switch_to.alert.accept()</span><br><span class="line"></span><br><span class="line">driver.find_element_by_id(<span class="string">&#x27;b2&#x27;</span>).click()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 点击 取消 按钮</span></span><br><span class="line">driver.switch_to.alert.dismiss()</span><br></pre></td></tr></table></figure>
<h3 id="prompt"><a class="markdownIt-Anchor" href="#prompt"></a> Prompt</h3>
<p>出现 Prompt 弹出框 是需要用户输入一些信息，提交上去。</p>
<p>比如：当管理员在网站上选择给某个账号延期时，就可能会弹出 Prompt 弹出框， 要求输入延期多长时间。</p>
<p>可以调用如下方法</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.switch_to.alert.send_keys()</span><br></pre></td></tr></table></figure>
<p>示例代码如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line">driver = webdriver.Chrome()</span><br><span class="line">driver.implicitly_wait(<span class="number">5</span>)</span><br><span class="line">driver.get(<span class="string">&#x27;http://cdn1.python3.vip/files/selenium/test4.html&#x27;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># --- prompt ---</span></span><br><span class="line">driver.find_element_by_id(<span class="string">&#x27;b3&#x27;</span>).click()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取 alert 对象</span></span><br><span class="line">alert = driver.switch_to.alert</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印 弹出框 提示信息</span></span><br><span class="line"><span class="built_in">print</span>(alert.text)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 输入信息，并且点击 OK 按钮 提交</span></span><br><span class="line">alert.send_keys(<span class="string">&#x27;web自动化 - selenium&#x27;</span>)</span><br><span class="line">alert.accept()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 点击 Cancel 按钮 取消</span></span><br><span class="line">driver.find_element_by_id(<span class="string">&#x27;b3&#x27;</span>).click()</span><br><span class="line">alert = driver.switch_to.alert</span><br><span class="line">alert.dismiss()</span><br></pre></td></tr></table></figure>
<p><strong>注意</strong> ： 有些弹窗并非浏览器的alert 窗口，而是<strong>html元素</strong>，这种对话框，只需要通过之前介绍的选择器选中并进行相应的操作就可以了。 {: .notice–info}</p>
<h2 id="窗口大小"><a class="markdownIt-Anchor" href="#窗口大小"></a> 窗口大小</h2>
<p>有时间我们需要获取窗口的属性和相应的信息，并对窗口进行控制</p>
<ul>
<li>获取窗口大小</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.get_window_size()</span><br></pre></td></tr></table></figure>
<ul>
<li>改变窗口大小</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.set_window_size(x, y)</span><br></pre></td></tr></table></figure>
<h2 id="获取当前窗口标题"><a class="markdownIt-Anchor" href="#获取当前窗口标题"></a> 获取当前窗口标题</h2>
<p>浏览网页的时候，我们的窗口标题是不断变化的，可以使用WebDriver的title属性来获取当前窗口的标题栏字符串。</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.title</span><br></pre></td></tr></table></figure>
<h2 id="获取当前窗口url地址"><a class="markdownIt-Anchor" href="#获取当前窗口url地址"></a> 获取当前窗口URL地址</h2>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">driver.current_url</span><br></pre></td></tr></table></figure>
<p>例如，访问网易，并获取当前窗口的标题和URL</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span>  webdriver</span><br><span class="line"></span><br><span class="line">driver = webdriver.Chrome()</span><br><span class="line">driver.implicitly_wait(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打开网站</span></span><br><span class="line">driver.get(<span class="string">&#x27;https://www.163.com&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取网站标题栏文本</span></span><br><span class="line"><span class="built_in">print</span>(driver.title) </span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取网站地址栏文本</span></span><br><span class="line"><span class="built_in">print</span>(driver.current_url) </span><br></pre></td></tr></table></figure>
<h2 id="截屏"><a class="markdownIt-Anchor" href="#截屏"></a> 截屏</h2>
<p>有的时候，我们需要把浏览器屏幕内容保存为图片文件。</p>
<p>比如，做自动化测试时，一个测试用例检查点发现错误，我们可以截屏为文件，以便测试结束时进行人工核查。</p>
<p>可以使用 WebDriver 的 get_screenshot_as_file方法来截屏并保存为图片。</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span>  webdriver</span><br><span class="line"></span><br><span class="line">driver = webdriver.Chrome()</span><br><span class="line">driver.implicitly_wait(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打开网站</span></span><br><span class="line">driver.get(<span class="string">&#x27;https://www.baidu.com/&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 截屏保存为图片文件</span></span><br><span class="line">driver.get_screenshot_as_file(<span class="string">&#x27;1.png&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="手机模式"><a class="markdownIt-Anchor" href="#手机模式"></a> 手机模式</h2>
<p>我们可以通过 <code>desired_capabilities</code> 参数，指定以手机模式打开chrome浏览器</p>
<p>参考代码，如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"></span><br><span class="line">mobile_emulation = &#123; <span class="string">&quot;deviceName&quot;</span>: <span class="string">&quot;Nexus 5&quot;</span> &#125;</span><br><span class="line"></span><br><span class="line">chrome_options = webdriver.ChromeOptions()</span><br><span class="line"></span><br><span class="line">chrome_options.add_experimental_option(<span class="string">&quot;mobileEmulation&quot;</span>, mobile_emulation)</span><br><span class="line"></span><br><span class="line">driver = webdriver.Chrome( desired_capabilities = chrome_options.to_capabilities())</span><br><span class="line"></span><br><span class="line">driver.get(<span class="string">&#x27;http://www.baidu.com&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">input</span>()</span><br><span class="line">driver.quit()</span><br></pre></td></tr></table></figure>
<h2 id="上传文件"><a class="markdownIt-Anchor" href="#上传文件"></a> 上传文件</h2>
<p>有时候，网站操作需要上传文件。</p>
<p>比如，著名的在线图片压缩网站： <a href="https://tinypng.com/">https://tinypng.com/</a></p>
<p>通常，网站页面上传文件的功能，是通过 <code>type</code> 属性 为 <code>file</code> 的 HTML <code>input</code> 元素实现的。</p>
<p>如下所示：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;file&quot;</span> <span class="attr">multiple</span>=<span class="string">&quot;multiple&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>使用selenium自动化上传文件，我们只需要定位到该input元素，然后通过 send_keys 方法传入要上传的文件路径即可。</p>
<p>如下所示：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 先定位到上传文件的 input 元素</span></span><br><span class="line">ele = wd.find_element_by_css_selector(<span class="string">&#x27;input[type=file]&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 再调用 WebElement 对象的 send_keys 方法</span></span><br><span class="line">ele.send_keys(<span class="string">r&#x27;h:\g02.png&#x27;</span>)</span><br></pre></td></tr></table></figure>
<p>如果需要上传多个文件，可以多次调用send_keys，如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">ele = wd.find_element_by_css_selector(<span class="string">&#x27;input[type=file]&#x27;</span>)</span><br><span class="line">ele.send_keys(<span class="string">r&#x27;h:\g01.png&#x27;</span>)</span><br><span class="line">ele.send_keys(<span class="string">r&#x27;h:\g02.png&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="xpath"><a class="markdownIt-Anchor" href="#xpath"></a> Xpath</h2>
<h2 id="xpath语法简介"><a class="markdownIt-Anchor" href="#xpath语法简介"></a> Xpath语法简介</h2>
<p>前面我们学习了CSS 选择元素。</p>
<p>大家可以发现非常灵活、强大。</p>
<p>还有一种 灵活、强大 的选择元素的方式，就是使用 <code>Xpath</code> 表达式。</p>
<p>XPath (XML Path Language) 是由国际标准化组织W3C指定的，用来在 XML 和 HTML 文档中选择节点的语言。</p>
<p>目前主流浏览器 (chrome、firefox，edge，safari) 都支持XPath语法，xpath有 1 和 2 两个版本，目前浏览器支持的是 xpath 1的语法。</p>
<p>既然已经有了CSS，为什么还要学习 Xpath呢？ 因为</p>
<ul>
<li>有些场景 用 css 选择web 元素 很麻烦，而xpath 却比较方便。</li>
<li>另外 Xpath 还有其他领域会使用到，比如 爬虫框架 Scrapy， 手机App框架 Appium。</li>
</ul>
<p>按F12打开调试窗口，点击 Elements标签。</p>
<p>要验证 Xpath 语法是否能成功选择元素，也可以像 验证 CSS 语法那样，按组合键 Ctrl + F ，就会出现 搜索框</p>
<p>xpath 语法中，整个HTML文档根节点用’/‘表示，如果我们想选择的是根节点下面的html节点，则可以在搜索框输入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/html</span><br></pre></td></tr></table></figure>
<p>如果输入下面的表达式</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/html/body/div</span><br></pre></td></tr></table></figure>
<p>这个表达式表示选择html下面的body下面的div元素。</p>
<p>注意 <code>/</code> 有点像 CSS中的 <code>&gt;</code> , 表示直接子节点关系。</p>
<h3 id="绝对路径选择"><a class="markdownIt-Anchor" href="#绝对路径选择"></a> 绝对路径选择</h3>
<p>从根节点开始的，到某个节点，每层都依次写下来，每层之间用 <code>/</code> 分隔的表达式，就是某元素的 <code>绝对路径</code></p>
<p>上面的xpath表达式 <code>/html/body/div</code> ，就是一个绝对路径的xpath表达式， 等价于 css表达式 <code>html&gt;body&gt;div</code></p>
<p>自动化程序要使用Xpath来选择web元素，应该调用 WebDriver对象的方法 <code>find_element_by_xpath</code> 或者 <code>find_elements_by_xpath</code>，像这样：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">elements = driver.find_elements_by_xpath(<span class="string">&quot;/html/body/div&quot;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="相对路径选择"><a class="markdownIt-Anchor" href="#相对路径选择"></a> 相对路径选择</h3>
<p>有的时候，我们需要选择网页中某个元素， <code>不管它在什么位置</code> 。</p>
<p>比如，选择示例页面的所有标签名为 <code>div</code> 的元素，如果使用css表达式，直接写一个 <code>div</code> 就行了。</p>
<p>那xpath怎么实现同样的功能呢？ xpath需要前面加 <code>//</code> , 表示从当前节点往下寻找所有的后代元素,不管它在什么位置。</p>
<p>所以xpath表达式，应该这样写： <code>//div</code></p>
<p>‘//’ 符号也可以继续加在后面,比如，要选择 所有的 div 元素里面的 所有的 p 元素 ，不管div 在什么位置，也不管p元素在div下面的什么位置，则可以这样写 <code>//div//p</code></p>
<p>对应的自动化程序如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">elements = driver.find_elements_by_xpath(<span class="string">&quot;//div//p&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>如果使用CSS选择器，对应代码如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">elements = driver.find_elements_by_css_selector(<span class="string">&quot;div p&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>如果，要选择 所有的 div 元素里面的 直接子节点 p ， xpath，就应该这样写了 <code>//div/p</code></p>
<p>如果使用CSS选择器，则为 <code>div &gt; p</code></p>
<h3 id="通配符"><a class="markdownIt-Anchor" href="#通配符"></a> 通配符</h3>
<p>如果要选择所有div节点的所有直接子节点，可以使用表达式 <code>//div/*</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">*` 是一个通配符，对应任意节点名的元素，等价于CSS选择器 `div &gt; *</span><br></pre></td></tr></table></figure>
<p>代码如下：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">elements = driver.find_elements_by_xpath(<span class="string">&quot;//div/*&quot;</span>)</span><br><span class="line"><span class="keyword">for</span> element <span class="keyword">in</span> elements:</span><br><span class="line">    <span class="built_in">print</span>(element.get_attribute(<span class="string">&#x27;outerHTML&#x27;</span>))</span><br></pre></td></tr></table></figure>
<h2 id="根据属性选择"><a class="markdownIt-Anchor" href="#根据属性选择"></a> 根据属性选择</h2>
<p>Xpath 可以根据属性来选择元素。</p>
<p>根据属性来选择元素 是通过 这种格式来的 <code>[@属性名='属性值']</code></p>
<p>注意：</p>
<ul>
<li>属性名注意前面有个@</li>
<li>属性值一定要用引号， 可以是单引号，也可以是双引号</li>
</ul>
<h3 id="根据id属性选择"><a class="markdownIt-Anchor" href="#根据id属性选择"></a> 根据id属性选择</h3>
<p>选择 id 为 west 的元素，可以这样 <code>//*[@id='west']</code></p>
<h3 id="根据class属性选择"><a class="markdownIt-Anchor" href="#根据class属性选择"></a> 根据class属性选择</h3>
<p>选择所有 select 元素中 class为 single_choice 的元素，可以这样 <code>//select[@class='single_choice']</code></p>
<p>如果一个元素class 有多个，比如</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">id</span>=<span class="string">&quot;beijing&quot;</span> <span class="attr">class</span>=<span class="string">&#x27;capital huge-city&#x27;</span>&gt;</span></span><br><span class="line">    北京    </span><br><span class="line"><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>如果要选 它， 对应的 xpath 就应该是 <code>//p[@class=&quot;capital huge-city&quot;]</code></p>
<p>不能只写一个属性，像这样 <code>//p[@class=&quot;capital&quot;]</code> 则不行</p>
<h3 id="根据其他属性"><a class="markdownIt-Anchor" href="#根据其他属性"></a> 根据其他属性</h3>
<p>同样的道理，我们也可以利用其它的属性选择</p>
<p>比如选择 具有multiple属性的所有页面元素 ，可以这样 <code>//*[@multiple]</code></p>
<h3 id="属性值包含字符串"><a class="markdownIt-Anchor" href="#属性值包含字符串"></a> 属性值包含字符串</h3>
<p>要选择 style属性值 包含 color 字符串的 页面元素 ，可以这样 <code>//*[contains(@style,'color')]</code></p>
<p>要选择 style属性值 以 color 字符串 <code>开头</code> 的 页面元素 ，可以这样 <code>//*[starts-with(@style,'color')]</code></p>
<p>要选择 style属性值 以 某个 字符串 结尾 的 页面元素 ，大家可以推测是 <code>//*[ends-with(@style,'color')]</code>， 但是，很遗憾，这是xpath 2.0 的语法 ，目前浏览器都不支持</p>
<h2 id="按次序选择"><a class="markdownIt-Anchor" href="#按次序选择"></a> 按次序选择</h2>
<p>前面学过css表达式可以根据元素在父节点中的次序选择， 非常实用。</p>
<p>xpath也可以根据次序选择元素。 语法比css更简洁，直接在方括号中使用数字表示次序</p>
<p>比如</p>
<h3 id="某类型-第几个-子元素"><a class="markdownIt-Anchor" href="#某类型-第几个-子元素"></a> 某类型 第几个 子元素</h3>
<p>比如</p>
<p>要选择 p类型第2个的子元素，就是</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//p[<span class="number">2</span>]</span><br></pre></td></tr></table></figure>
<p>注意，选择的是 <code>p类型第2个的子元素</code> ， 不是 <code>第2个子元素，并且是p类型</code> 。</p>
<p>注意体会区别</p>
<p>再比如，要选取父元素为div 中的 p类型 第2个 子元素</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//div/p[<span class="number">2</span>]</span><br></pre></td></tr></table></figure>
<h3 id="第几个子元素"><a class="markdownIt-Anchor" href="#第几个子元素"></a> 第几个子元素</h3>
<p>也可以选择第2个子元素，不管是什么类型，采用通配符</p>
<p>比如 选择父元素为div的第2个子元素，不管是什么类型</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//div/*[<span class="number">2</span>]</span><br></pre></td></tr></table></figure>
<h3 id="某类型-倒数第几个-子元素"><a class="markdownIt-Anchor" href="#某类型-倒数第几个-子元素"></a> 某类型 倒数第几个 子元素</h3>
<p>当然也可以选取倒数第几个子元素</p>
<p>比如：</p>
<ul>
<li>选取p类型倒数第1个子元素</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//p[last()]</span><br></pre></td></tr></table></figure>
<ul>
<li>选取p类型倒数第2个子元素</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//p[last()-<span class="number">1</span>]</span><br></pre></td></tr></table></figure>
<ul>
<li>选择父元素为div中p类型倒数第3个子元素</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//div/p[last()-<span class="number">2</span>]</span><br></pre></td></tr></table></figure>
<h3 id="范围选择"><a class="markdownIt-Anchor" href="#范围选择"></a> 范围选择</h3>
<p>xpath还可以选择子元素的次序范围。</p>
<p>比如，</p>
<ul>
<li>选取option类型第1到2个子元素</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//option[position()&lt;=<span class="number">2</span>]</span><br></pre></td></tr></table></figure>
<p>或者</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//option[position()&lt;<span class="number">3</span>]</span><br></pre></td></tr></table></figure>
<ul>
<li>选择class属性为multi_choice的前3个子元素</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//*[@<span class="class"><span class="keyword">class</span>=&#x27;<span class="title">multi_choice</span>&#x27;]/*[<span class="title">position</span>()&lt;=3]</span></span><br></pre></td></tr></table></figure>
<ul>
<li>选择class属性为multi_choice的后3个子元素</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">//*[@<span class="class"><span class="keyword">class</span>=&#x27;<span class="title">multi_choice</span>&#x27;]/*[<span class="title">position</span>()&gt;=<span class="title">last</span>()-2]</span></span><br></pre></td></tr></table></figure>
<p>为什么不是 <code>last()-3</code> 呢？ 因为</p>
<p><code>last()</code> 本身代表最后一个元素</p>
<p><code>last()-1</code> 本身代表倒数第2个元素</p>
<p><code>last()-2</code> 本身代表倒数第3个元素</p>
<h2 id="组选择-父节点-兄弟节点"><a class="markdownIt-Anchor" href="#组选择-父节点-兄弟节点"></a> 组选择、父节点、兄弟节点</h2>
<h3 id="组选择-2"><a class="markdownIt-Anchor" href="#组选择-2"></a> 组选择</h3>
<p>css有组选择，可以同时使用多个表达式，多个表达式选择的结果都是要选择的元素</p>
<p>css 组选择，表达式之间用 <strong>逗号</strong> 隔开</p>
<p>xpath也有组选择， 是用 <strong>竖线</strong> 隔开多个表达式</p>
<p>比如，要选所有的option元素 和所有的 h4 元素，可以使用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//option | //h4</span><br></pre></td></tr></table></figure>
<p>等同于CSS选择器</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">option , h4</span><br></pre></td></tr></table></figure>
<p>再比如，要选所有的 class 为 single_choice 和 class 为 multi_choice 的元素，可以使用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//*[@class=&#x27;single_choice&#x27;] | //*[@class=&#x27;multi_choice&#x27;]</span><br></pre></td></tr></table></figure>
<p>等同于CSS选择器</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.single_choice , .multi_choice</span><br></pre></td></tr></table></figure>
<h3 id="选择父节点"><a class="markdownIt-Anchor" href="#选择父节点"></a> 选择父节点</h3>
<p>xpath可以选择父节点， 这是css做不到的。</p>
<p>某个元素的父节点用 <code>/..</code> 表示</p>
<p>比如，要选择 id 为 china 的节点的父节点，可以这样写 <code>//*[@id='china']/..</code> 。</p>
<p>当某个元素没有特征可以直接选择，但是它有子节点有特征， 就可以采用这种方法，先选择子节点，再指定父节点。</p>
<p>还可继续找上层父节点，比如 <code>//*[@id='china']/../../..</code></p>
<h3 id="兄弟节点选择"><a class="markdownIt-Anchor" href="#兄弟节点选择"></a> 兄弟节点选择</h3>
<p>前面学过 css选择器，要选择某个节点的后续兄弟节点，用 <strong>波浪线</strong></p>
<p>xpath也可以选择 后续 兄弟节点，用这样的语法 <code>following-sibling::</code></p>
<p>比如，要选择 class 为 single_choice 的元素的所有后续兄弟节点 <code>//*[@class='single_choice']/following-sibling::*</code></p>
<p>等同于CSS选择器 <code>.single_choice ~ *</code></p>
<p>如果，要选择后续节点中的div节点， 就应该这样写 <code>//*[@class='single_choice']/following-sibling::div</code></p>
<p>xpath还可以选择 <code>前面的</code> 兄弟节点，用这样的语法 <code>preceding-sibling::</code></p>
<p>比如，要选择 class 为 single_choice 的元素的所有前面的兄弟节点 <code>//*[@class='single_choice']/preceding-sibling::*</code></p>
<p>而CSS选择器目前还没有方法选择前面的 兄弟节点</p>
<h2 id="selenium-注意点"><a class="markdownIt-Anchor" href="#selenium-注意点"></a> selenium 注意点</h2>
<p>我们来看一个例子</p>
<p>我们的代码：</p>
<ul>
<li>先选择示例网页中，id是china的元素</li>
<li>然后通过这个元素的WebElement对象，使用find_elements_by_xpath，选择里面的p元素，</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 先寻找id是china的元素</span></span><br><span class="line">china = wd.find_element_by_id(<span class="string">&#x27;china&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 再选择该元素内部的p元素</span></span><br><span class="line">elements = china.find_elements_by_xpath(<span class="string">&#x27;//p&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印结果</span></span><br><span class="line"><span class="keyword">for</span> element <span class="keyword">in</span> elements:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;----------------&#x27;</span>)</span><br><span class="line">    <span class="built_in">print</span>(element.get_attribute(<span class="string">&#x27;outerHTML&#x27;</span>))</span><br></pre></td></tr></table></figure>
<p>运行发现，打印的 不仅仅是 china内部的p元素， 而是所有的p元素。</p>
<p>要在某个元素内部使用xpath选择元素， 需要 <code>在xpath表达式最前面加个点</code> 。</p>
<p>像这样</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">elements = china.find_elements_by_xpath(<span class="string">&#x27;.//p&#x27;</span>)</span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title>1022 Digital Library</title>
    <url>/2021/06/07/1022-Digital-Library/</url>
    <content><![CDATA[<p>A Digital Library contains millions of books, stored according to their titles, authors, key words of their abstracts, publishers, and published years. Each book is assigned an unique 7-digit number as its ID. Given any query from a reader, you are supposed to output the resulting books, sorted in increasing order of their ID’s.</p>
<span id="more"></span>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input file contains one test case. For each case, the first line contains a positive integer <em>N</em> (≤104) which is the total number of books. Then <em>N</em> blocks follow, each contains the information of a book in 6 lines:</p>
<ul>
<li>Line #1: the 7-digit ID number;</li>
<li>Line #2: the book title – a string of no more than 80 characters;</li>
<li>Line #3: the author – a string of no more than 80 characters;</li>
<li>Line #4: the key words – each word is a string of no more than 10 characters without any white space, and the keywords are separated by exactly one space;</li>
<li>Line #5: the publisher – a string of no more than 80 characters;</li>
<li>Line #6: the published year – a 4-digit number which is in the range [1000, 3000].</li>
</ul>
<p>It is assumed that each book belongs to one author only, and contains no more than 5 key words; there are no more than 1000 distinct key words in total; and there are no more than 1000 distinct publishers.</p>
<p>After the book information, there is a line containing a positive integer <em>M</em> (≤1000) which is the number of user’s search queries. Then <em>M</em> lines follow, each in one of the formats shown below:</p>
<ul>
<li>1: a book title</li>
<li>2: name of an author</li>
<li>3: a key word</li>
<li>4: name of a publisher</li>
<li>5: a 4-digit number representing the year</li>
</ul>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each query, first print the original query in a line, then output the resulting book ID’s in increasing order, each occupying a line. If no book is found, print <code>Not Found</code> instead.</p>
<h3 id="sample-input"><a class="markdownIt-Anchor" href="#sample-input"></a> Sample Input:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">3</span><br><span class="line">1111111</span><br><span class="line">The Testing Book</span><br><span class="line">Yue Chen</span><br><span class="line">test code debug sort keywords</span><br><span class="line">ZUCS Print</span><br><span class="line">2011</span><br><span class="line">3333333</span><br><span class="line">Another Testing Book</span><br><span class="line">Yue Chen</span><br><span class="line">test code sort keywords</span><br><span class="line">ZUCS Print2</span><br><span class="line">2012</span><br><span class="line">2222222</span><br><span class="line">The Testing Book</span><br><span class="line">CYLL</span><br><span class="line">keywords debug book</span><br><span class="line">ZUCS Print2</span><br><span class="line">2011</span><br><span class="line">6</span><br><span class="line">1: The Testing Book</span><br><span class="line">2: Yue Chen</span><br><span class="line">3: keywords</span><br><span class="line">4: ZUCS Print</span><br><span class="line">5: 2011</span><br><span class="line">3: blablabla</span><br></pre></td></tr></table></figure>
<h3 id="sample-output"><a class="markdownIt-Anchor" href="#sample-output"></a> Sample Output:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1: The Testing Book</span><br><span class="line">1111111</span><br><span class="line">2222222</span><br><span class="line">2: Yue Chen</span><br><span class="line">1111111</span><br><span class="line">3333333</span><br><span class="line">3: keywords</span><br><span class="line">1111111</span><br><span class="line">2222222</span><br><span class="line">3333333</span><br><span class="line">4: ZUCS Print</span><br><span class="line">1111111</span><br><span class="line">5: 2011</span><br><span class="line">1111111</span><br><span class="line">2222222</span><br><span class="line">3: blablabla</span><br><span class="line">Not Found</span><br></pre></td></tr></table></figure>
<h3 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h3>
<p>建立map来一一对应</p>
<h3 id="notice"><a class="markdownIt-Anchor" href="#notice"></a> Notice</h3>
<ul>
<li><code>Input Specification</code>中<code>keywords</code>的IO操作</li>
<li>使用<code>set</code>来记录<code>ID</code>其优点是元素是独一无二的，其次元素是有序的，省略了排序的过程</li>
<li>注意<code>ID</code>要用<code>string</code>来存储，否则<code>00001</code>这种<code>ID</code>会存储成<code>1</code>导致后面测试点不过(大坑)</li>
</ul>
<h3 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;map&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;set&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function">vector&lt;string&gt; <span class="title">split</span><span class="params">(string keyWords)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    vector&lt;string&gt; kw;</span><br><span class="line">    string subString = <span class="string">&quot;&quot;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> x : keyWords)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(x != <span class="string">&#x27; &#x27;</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            subString += x;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            kw.<span class="built_in">push_back</span>(subString);</span><br><span class="line">            subString = <span class="string">&quot;&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    kw.<span class="built_in">push_back</span>(subString);</span><br><span class="line">    <span class="keyword">return</span> kw;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">pair&lt;<span class="keyword">int</span>, string&gt; <span class="title">getQ</span><span class="params">(string q)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string flag, question;</span><br><span class="line">    flag = q.<span class="built_in">substr</span>(<span class="number">0</span>,<span class="number">1</span>);</span><br><span class="line">    question = q.<span class="built_in">substr</span>(<span class="number">3</span>, <span class="built_in"><span class="keyword">int</span></span>(q.<span class="built_in">size</span>()));</span><br><span class="line">    <span class="keyword">return</span> &#123;flag[<span class="number">0</span>] - <span class="string">&#x27;0&#x27;</span>, question&#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a &lt; b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// map&lt;string, vector&lt;int&gt;&gt; bookTitle, author, keyWords, publisher, year;</span></span><br><span class="line">vector&lt;map&lt;string, set&lt;string&gt;&gt;&gt; <span class="built_in">l</span>(<span class="number">5</span>);</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> number; cin &gt;&gt; number;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; number; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        string iD;</span><br><span class="line">        string bT, aT, kW, pL, yE;</span><br><span class="line">        cin &gt;&gt; iD;</span><br><span class="line">        cin.<span class="built_in">get</span>();</span><br><span class="line">        <span class="built_in">getline</span>(cin, bT);</span><br><span class="line">        <span class="built_in">getline</span>(cin, aT);</span><br><span class="line">        <span class="built_in">getline</span>(cin, kW);</span><br><span class="line">        <span class="built_in">getline</span>(cin, pL);</span><br><span class="line">        <span class="built_in">getline</span>(cin, yE);</span><br><span class="line">        l[<span class="number">0</span>][bT].<span class="built_in">insert</span>(iD);</span><br><span class="line">        l[<span class="number">1</span>][aT].<span class="built_in">insert</span>(iD);</span><br><span class="line">        vector&lt;string&gt; subKw = <span class="built_in">split</span>(kW);</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">auto</span> x : subKw)</span><br><span class="line">        &#123;</span><br><span class="line">            l[<span class="number">2</span>][x].<span class="built_in">insert</span>(iD);</span><br><span class="line">        &#125;</span><br><span class="line">        l[<span class="number">3</span>][pL].<span class="built_in">insert</span>(iD);</span><br><span class="line">        l[<span class="number">4</span>][yE].<span class="built_in">insert</span>(iD);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> q; cin &gt;&gt; q;</span><br><span class="line">    cin.<span class="built_in">get</span>();</span><br><span class="line">    vector&lt;pair&lt;<span class="keyword">int</span>, string&gt;&gt; <span class="built_in">query</span>(q);</span><br><span class="line">    string temp;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; q; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">getline</span>(cin, temp);</span><br><span class="line">        query[i] = <span class="built_in">getQ</span>(temp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> x : query)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> flag = x.first; string q = x.second;</span><br><span class="line">        cout &lt;&lt; flag &lt;&lt; <span class="string">&quot;: &quot;</span> &lt;&lt; q &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">if</span>(l[flag - <span class="number">1</span>].<span class="built_in">find</span>(q) != l[flag - <span class="number">1</span>].<span class="built_in">end</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// sort(l[flag - 1][q].begin(), l[flag - 1][q].end(), cmp);</span></span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">auto</span> y : l[flag - <span class="number">1</span>][q])</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(l[flag - <span class="number">1</span>].<span class="built_in">find</span>(q) != l[flag - <span class="number">1</span>].<span class="built_in">end</span>())</span><br><span class="line">                &#123;</span><br><span class="line">                    cout &lt;&lt; y &lt;&lt; endl;</span><br><span class="line">                &#125;  </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;Not Found\n&quot;</span>;</span><br><span class="line">        &#125;  </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>1023 Have Fun with Numbers</title>
    <url>/2021/06/09/1023-Have-Fun-with-Numbers/</url>
    <content><![CDATA[<p>Notice that the number 123456789 is a 9-digit number consisting exactly the numbers from 1 to 9, with no duplication. Double it we will obtain 246913578, which happens to be another 9-digit number consisting exactly the numbers from 1 to 9, only in a different permutation. Check to see the result if we double it again!</p>
<p>Now you are suppose to check if there are more numbers with this property. That is, double a given number with <em>k</em> digits, you are to tell if the resulting number consists of only a permutation of the digits in the original number.</p>
<span id="more"></span>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input contains one test case. Each case contains one positive integer with no more than 20 digits.</p>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each test case, first print in a line “Yes” if doubling the input number gives a number that consists of only a permutation of the digits in the original number, or “No” if not. Then in the next line, print the doubled number.</p>
<h3 id="sample-input"><a class="markdownIt-Anchor" href="#sample-input"></a> Sample Input:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1234567899</span><br></pre></td></tr></table></figure>
<h3 id="sample-output"><a class="markdownIt-Anchor" href="#sample-output"></a> Sample Output:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Yes</span><br><span class="line">2469135798</span><br></pre></td></tr></table></figure>
<h3 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h3>
<p>主要难点在于使用<code>long long</code>会爆，要使用<code>string</code>模拟大数加减。</p>
<p>双倍之后的数字要和原数字个数一致。</p>
<h3 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="comment">// string Double(string originNum)</span></span><br><span class="line"><span class="comment">// &#123;</span></span><br><span class="line"><span class="comment">//     vector&lt;int&gt; carry(originNum.size(), 0);</span></span><br><span class="line"><span class="comment">//     string resultNum = &quot;&quot;;</span></span><br><span class="line"><span class="comment">//     for(int i = int(originNum.size()) - 1; i &gt;= 0; i--)</span></span><br><span class="line"><span class="comment">//     &#123;</span></span><br><span class="line"><span class="comment">//         carry[i] = (originNum[i] - &#x27;0&#x27;) * 2;</span></span><br><span class="line"><span class="comment">//     &#125;</span></span><br><span class="line"><span class="comment">//     for(int i = 0; i &lt; int(originNum.size()); i++)</span></span><br><span class="line"><span class="comment">//     &#123;</span></span><br><span class="line"><span class="comment">//         if(carry[i + 1] &gt;= 10 &amp;&amp; (i+1) &lt; int(originNum.size()))</span></span><br><span class="line"><span class="comment">//         &#123;</span></span><br><span class="line"><span class="comment">//             int onePlace = (carry[i] % 10) + 1;</span></span><br><span class="line"><span class="comment">//             resultNum += (onePlace + &#x27;0&#x27;);</span></span><br><span class="line"><span class="comment">//         &#125;</span></span><br><span class="line"><span class="comment">//         else</span></span><br><span class="line"><span class="comment">//         &#123;</span></span><br><span class="line"><span class="comment">//             int onePlace = (carry[i] % 10);</span></span><br><span class="line"><span class="comment">//             resultNum += (onePlace + &#x27;0&#x27;);</span></span><br><span class="line"><span class="comment">//         &#125;</span></span><br><span class="line"><span class="comment">//     &#125;</span></span><br><span class="line"><span class="comment">//     if(carry[0] &gt;= 10) </span></span><br><span class="line"><span class="comment">//     &#123;</span></span><br><span class="line"><span class="comment">//         resultNum.insert(resultNum.begin(), &#x27;1&#x27;);</span></span><br><span class="line"><span class="comment">//     &#125;</span></span><br><span class="line"><span class="comment">//     return resultNum;</span></span><br><span class="line"><span class="comment">// &#125;</span></span><br><span class="line"><span class="function">string <span class="title">doubleNum</span><span class="params">(string originNum)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string resultNum = <span class="string">&quot;&quot;</span>;</span><br><span class="line">    <span class="built_in">reverse</span>(originNum.<span class="built_in">begin</span>(), originNum.<span class="built_in">end</span>());</span><br><span class="line">    <span class="keyword">int</span> carry = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">char</span> x : originNum)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> doubleCurrent = (x - <span class="string">&#x27;0&#x27;</span>) * <span class="number">2</span> + carry;</span><br><span class="line">        <span class="keyword">if</span>(doubleCurrent &gt;= <span class="number">10</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            carry = <span class="number">1</span>;</span><br><span class="line">            doubleCurrent = doubleCurrent % <span class="number">10</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> </span><br><span class="line">            carry = <span class="number">0</span>;</span><br><span class="line">        resultNum.<span class="built_in">insert</span>(resultNum.<span class="built_in">begin</span>(), doubleCurrent + <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(carry == <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;1&quot;</span> + resultNum;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> resultNum;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string originNum;</span><br><span class="line">    cin &gt;&gt; originNum;</span><br><span class="line">    string num = <span class="built_in">doubleNum</span>(originNum);</span><br><span class="line"></span><br><span class="line">    <span class="function">vector&lt;<span class="keyword">int</span>&gt; <span class="title">n1</span><span class="params">(<span class="number">10</span>,<span class="number">0</span>)</span>, <span class="title">n2</span><span class="params">(<span class="number">10</span>,<span class="number">0</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> x : originNum)</span><br><span class="line">    &#123;</span><br><span class="line">        n1[x - <span class="string">&#x27;0&#x27;</span>]++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> x : num)</span><br><span class="line">    &#123;</span><br><span class="line">        n2[x - <span class="string">&#x27;0&#x27;</span>]++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> flag = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(n1[i] != n2[i])</span><br><span class="line">        &#123;</span><br><span class="line">            flag = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(flag == <span class="number">1</span>) cout &lt;&lt; <span class="string">&quot;Yes\n&quot;</span>;</span><br><span class="line">    <span class="keyword">else</span> cout &lt;&lt; <span class="string">&quot;No\n&quot;</span>;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; num;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>1025 PAT Ranking</title>
    <url>/2021/06/14/1025-PAT-Ranking/</url>
    <content><![CDATA[<p>Programming Ability Test (PAT) is organized by the College of Computer Science and Technology of Zhejiang University. Each test is supposed to run simultaneously in several places, and the ranklists will be merged immediately after the test. Now it is your job to write a program to correctly merge all the ranklists and generate the final rank.</p>
<span id="more"></span>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input file contains one test case. For each case, the first line contains a positive number <em>N</em> (≤100), the number of test locations. Then <em>N</em> ranklists follow, each starts with a line containing a positive integer <em>K</em> (≤300), the number of testees, and then <em>K</em> lines containing the registration number (a 13-digit number) and the total score of each testee. All the numbers in a line are separated by a space.</p>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each test case, first print in one line the total number of testees. Then print the final ranklist in the following format:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">registration_number final_rank location_number local_rank</span><br></pre></td></tr></table></figure>
<p>The locations are numbered from 1 to <em>N</em>. The output must be sorted in nondecreasing order of the final ranks. The testees with the same score must have the same rank, and the output must be sorted in nondecreasing order of their registration numbers.</p>
<h3 id="sample-input"><a class="markdownIt-Anchor" href="#sample-input"></a> Sample Input:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">2</span><br><span class="line">5</span><br><span class="line">1234567890001 95</span><br><span class="line">1234567890005 100</span><br><span class="line">1234567890003 95</span><br><span class="line">1234567890002 77</span><br><span class="line">1234567890004 85</span><br><span class="line">4</span><br><span class="line">1234567890013 65</span><br><span class="line">1234567890011 25</span><br><span class="line">1234567890014 100</span><br><span class="line">1234567890012 85</span><br></pre></td></tr></table></figure>
<h3 id="sample-output"><a class="markdownIt-Anchor" href="#sample-output"></a> Sample Output:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">9</span><br><span class="line">1234567890005 1 1 1</span><br><span class="line">1234567890014 1 2 1</span><br><span class="line">1234567890001 3 1 2</span><br><span class="line">1234567890003 3 1 2</span><br><span class="line">1234567890004 5 1 4</span><br><span class="line">1234567890012 5 2 2</span><br><span class="line">1234567890002 7 1 5</span><br><span class="line">1234567890013 8 2 3</span><br><span class="line">1234567890011 9 2 4</span><br></pre></td></tr></table></figure>
<p><img src="/2021/06/14/1025-PAT-Ranking/20190826224720541.png" alt="img"></p>
<blockquote>
<p><a href="https://blog.csdn.net/qq_43152052/article/details/100082825">图片来源</a></p>
</blockquote>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>1024 Palindromic Number</title>
    <url>/2021/06/14/1024-Palindromic-Number/</url>
    <content><![CDATA[<p>A number that will be the same when it is written forwards or backwards is known as a <strong>Palindromic Number</strong>. For example, 1234321 is a palindromic number. All single digit numbers are palindromic numbers.</p>
<p>Non-palindromic numbers can be paired with palindromic ones via a series of operations. First, the non-palindromic number is reversed and the result is added to the original number. If the result is not a palindromic number, this is repeated until it gives a palindromic number. For example, if we start from 67, we can obtain a palindromic number in 2 steps: 67 + 76 = 143, and 143 + 341 = 484.</p>
<p>Given any positive integer <em>N</em>, you are supposed to find its paired palindromic number and the number of steps taken to find it.</p>
<span id="more"></span>
<h3 id="input-specification"><a class="markdownIt-Anchor" href="#input-specification"></a> Input Specification:</h3>
<p>Each input file contains one test case. Each case consists of two positive numbers <em>N</em> and <em>K</em>, where <em>N</em> (≤1010) is the initial number and <em>K</em> (≤100) is the maximum number of steps. The numbers are separated by a space.</p>
<h3 id="output-specification"><a class="markdownIt-Anchor" href="#output-specification"></a> Output Specification:</h3>
<p>For each test case, output two numbers, one in each line. The first number is the paired palindromic number of <em>N</em>, and the second number is the number of steps taken to find the palindromic number. If the palindromic number is not found after <em>K</em> steps, just output the number obtained at the <em>K</em>th step and <em>K</em> instead.</p>
<h3 id="sample-input-1"><a class="markdownIt-Anchor" href="#sample-input-1"></a> Sample Input 1:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">67 3</span><br></pre></td></tr></table></figure>
<h3 id="sample-output-1"><a class="markdownIt-Anchor" href="#sample-output-1"></a> Sample Output 1:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">484</span><br><span class="line">2</span><br></pre></td></tr></table></figure>
<h3 id="sample-input-2"><a class="markdownIt-Anchor" href="#sample-input-2"></a> Sample Input 2:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">69 3</span><br></pre></td></tr></table></figure>
<h3 id="sample-output-2"><a class="markdownIt-Anchor" href="#sample-output-2"></a> Sample Output 2:</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1353</span><br><span class="line">3</span><br></pre></td></tr></table></figure>
<h3 id="steps"><a class="markdownIt-Anchor" href="#steps"></a> Steps</h3>
<p>大数相加，除了要用<code>string</code>模拟其他没啥特别需要注意的。</p>
<p>大数相加代码中有一种写法会导致3个测试点不过，目前不知道为什么（待补充）。</p>
<h3 id="code"><a class="markdownIt-Anchor" href="#code"></a> Code</h3>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function">string <span class="title">onceOperate</span><span class="params">(string originNum)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string reverseNum = originNum, resultNum = <span class="string">&quot;&quot;</span>;</span><br><span class="line">    <span class="built_in">reverse</span>(originNum.<span class="built_in">begin</span>(), originNum.<span class="built_in">end</span>());</span><br><span class="line">    <span class="keyword">int</span> carry = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in"><span class="keyword">int</span></span>(originNum.<span class="built_in">size</span>()); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> add = (originNum[i] - <span class="string">&#x27;0&#x27;</span>) + (reverseNum[i] - <span class="string">&#x27;0&#x27;</span>) + carry;</span><br><span class="line">        <span class="keyword">if</span>(add &gt;= <span class="number">10</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            carry = add / <span class="number">10</span>;</span><br><span class="line">            add = add % <span class="number">10</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            carry = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">//以下写法有3个测试点不过，不知道为什么</span></span><br><span class="line">        <span class="comment">// int c = carry;</span></span><br><span class="line">        <span class="comment">// int add = (originNum[i] - &#x27;0&#x27;) + (reverseNum[i] - &#x27;0&#x27;);</span></span><br><span class="line">        <span class="comment">// if(add &gt;= 10)</span></span><br><span class="line">        <span class="comment">// &#123;</span></span><br><span class="line">        <span class="comment">//     carry = add / 10;</span></span><br><span class="line">        <span class="comment">//     add = add % 10;</span></span><br><span class="line">        <span class="comment">// &#125;</span></span><br><span class="line">        <span class="comment">// else</span></span><br><span class="line">        <span class="comment">//     carry = 0;</span></span><br><span class="line">        <span class="comment">// resultNum.insert(resultNum.begin(), add + c + &#x27;0&#x27;);</span></span><br><span class="line">        resultNum.<span class="built_in">insert</span>(resultNum.<span class="built_in">begin</span>(), add + <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(carry &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;1&quot;</span> + resultNum;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> resultNum;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isPalindromic</span><span class="params">(string num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> begin = <span class="number">0</span>, end = <span class="built_in"><span class="keyword">int</span></span>(num.<span class="built_in">size</span>() - <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">while</span>(begin &lt; end)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(num[begin++] != num[end--])</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string N;</span><br><span class="line">    <span class="keyword">int</span> K;</span><br><span class="line">    cin &gt;&gt; N &gt;&gt; K;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (!<span class="built_in">isPalindromic</span>(N) &amp;&amp; count &lt; K)</span><br><span class="line">    &#123;</span><br><span class="line">        N = <span class="built_in">onceOperate</span>(N);</span><br><span class="line">        count++;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; N &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; count &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PAT</category>
      </categories>
      <tags>
        <tag>PAT</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Dynamic Programing</title>
    <url>/2021/07/28/Dynamic-Programing/</url>
    <content><![CDATA[<h2 id="1何时使用动态规划"><a class="markdownIt-Anchor" href="#1何时使用动态规划"></a> 1.何时使用动态规划</h2>
<p>动态规划通常用来解决<strong>最优化问题</strong>(optimization problem)，这类问题通常可以有很多可行解，每个解都有一个值，我们希望寻找到<strong>最优解</strong>（例如最小值或最大值）的解。这个解就是问题的一个最优解。</p>
<p>使用动态规划求解最优化问题的前提要符合一下<strong>两个条件</strong>：</p>
<ul>
<li>最优子结构</li>
<li>子问题重叠</li>
</ul>
<p>这里给出最优子结构的定义：<strong>如果一个问题的最优解包含其子问题的最优解，我们就称此问题具有最优子结构性质。</strong></p>
<p>子问题重叠的定义：递归算法会反复地求解相同的子问题，而不是一直生成新的子问题。一般来讲，不同子问题的总数是输入规模的多项式函数为好。<strong>如果递归算法反复求解相同的子问题，我们就称最优化问题具有重叠子问题(overlapping subproblems)性质</strong></p>
<h2 id="2求解动态规划的步骤"><a class="markdownIt-Anchor" href="#2求解动态规划的步骤"></a> 2.求解动态规划的步骤</h2>
<ol>
<li>刻画一个最优解的结构特征</li>
<li>递归地定义最优解的值</li>
<li>计算最优解的值（通常采用自底向上的方法）
<ol>
<li>状态定义（数组代表的含义）</li>
<li>转移方程</li>
<li>初始状态和边界条件</li>
<li>返回值</li>
</ol>
</li>
<li>利用计算出的信息构造一个最优解</li>
</ol>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>algorithm</tag>
        <tag>DP</tag>
      </tags>
  </entry>
</search>
